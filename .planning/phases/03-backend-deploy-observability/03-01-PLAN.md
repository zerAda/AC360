---
phase: 03-backend-deploy-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/telemetry.py
  - tests/backend/test_telemetry_redaction.py
  - tests/backend/test_ready_endpoint.py
  - requirements.txt
  - azure_functions/requirements.txt
autonomous: true
requirements: [OBS-01, OBS-03]
must_haves:
  truths:
    - "RedactingSpanProcessor.on_end masks PII/secret span attributes via safe_logger.redact (no new regex)"
    - "setup_telemetry() is inert when the AppInsights env gate is closed (dev/test/import safe)"
    - "azure-monitor-opentelemetry is pinned in both requirements files"
    - "The /ready test scaffold encodes the 200-ready / 503-degraded / 401-unauth contract (RED until Plan 02)"
  artifacts:
    - path: "scripts/telemetry.py"
      provides: "RedactingSpanProcessor + setup_telemetry (gated, lazy SDK import, redaction-preserving)"
      exports: ["RedactingSpanProcessor", "setup_telemetry"]
      min_lines: 40
    - path: "tests/backend/test_telemetry_redaction.py"
      provides: "Span-attribute redaction unit test (OBS-01, AUD-06)"
      contains: "RedactingSpanProcessor"
    - path: "tests/backend/test_ready_endpoint.py"
      provides: "/ready 200/503/401 contract test scaffold (OBS-03)"
      contains: "/ready"
    - path: "requirements.txt"
      provides: "azure-monitor-opentelemetry pin (gateway)"
      contains: "azure-monitor-opentelemetry"
    - path: "azure_functions/requirements.txt"
      provides: "azure-monitor-opentelemetry pin (Functions worker)"
      contains: "azure-monitor-opentelemetry"
  key_links:
    - from: "scripts/telemetry.py"
      to: "scripts/safe_logger.py"
      via: "from safe_logger import redact (single audited surface)"
      pattern: "from safe_logger import redact"
    - from: "scripts/telemetry.py"
      to: "APPINSIGHTS env gate"
      via: "early-return when neither env var set"
      pattern: "APPLICATIONINSIGHTS_CONNECTION_STRING"
---

<objective>
Wave 0 foundation for Phase 3. Create the executable specs and shared telemetry module that every later wave wires against: the redaction-preserving OpenTelemetry processor + gated `setup_telemetry()` (`scripts/telemetry.py`), the two new offline tests (`test_telemetry_redaction.py`, `test_ready_endpoint.py`), and the `azure-monitor-opentelemetry` dependency pin in both requirements files.

Purpose: Pin OBS-01 (App Insights wiring + redaction preservation) and OBS-03 (/ready contract) as failing/passing offline specs before any app or infra is wired, exactly mirroring the Phase 1 Wave-0 RED-scaffold pattern. This lands the real exporter dependency that Phase 1 explicitly deferred to OBS-01 (STATE: "01-04: real Azure Monitor exporter deferred to Phase 3 OBS-01 behind the same APPINSIGHTS gate").
Output: `scripts/telemetry.py`, two test files, two requirements edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backend-deploy-observability/03-RESEARCH.md
@.planning/phases/03-backend-deploy-observability/03-PATTERNS.md
@.planning/phases/03-backend-deploy-observability/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md

# Analogs (read in the tasks below)
@scripts/audit_trail.py
@scripts/safe_logger.py
@tests/backend/test_safe_logger_redaction.py
@tests/backend/test_security_headers.py
@tests/backend/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/telemetry.py — RedactingSpanProcessor + gated setup_telemetry</name>
  <files>scripts/telemetry.py</files>
  <read_first>
    - scripts/audit_trail.py (full) — copy the AppInsights env gate verbatim: `_APPINSIGHTS_ENV_VARS = ("APPINSIGHTS_INSTRUMENTATIONKEY", "APPLICATIONINSIGHTS_CONNECTION_STRING")` and the `_appinsights_gate_open()` helper (audit_trail.py:39-51); copy the `from __future__ import annotations` + module-docstring + `__all__` discipline (audit_trail.py:24-33).
    - scripts/safe_logger.py:92-122 (`redact`) and :60 (`__all__`) — `redact` is the single audited redaction surface; NO new regex.
    - RESEARCH §Code Example 3 (lines 294-331) — target shape of RedactingSpanProcessor.on_end + setup_telemetry.
    - PATTERNS.md §scripts/telemetry.py (lines 91-117) — analog assignment (gate reuse, redaction reuse, lazy SDK import, __all__).
    - azure_functions/function_app.py:14-33 — guarded/lazy import discipline so pytest collection never requires the heavy SDK.
    - CLAUDE.md Conventions: snake_case, type hints on all params + returns, `from __future__ import annotations`, `__all__` public API, French docstrings permitted.
  </read_first>
  <action>
    Create `scripts/telemetry.py` with `from __future__ import annotations`, a French module docstring, and `__all__ = ["RedactingSpanProcessor", "setup_telemetry"]` (per OBS-01).
    Import only stdlib `os` and `from safe_logger import redact` at module top — do NOT import `azure.monitor.opentelemetry` or any `opentelemetry.*` at module level (lazy import inside setup_telemetry, mirroring function_app.py:26-33), so pytest collection never requires the package.
    Define class `RedactingSpanProcessor` subclassing `opentelemetry.sdk.trace.SpanProcessor` — but to keep import-safety, do NOT import SpanProcessor at module top; either (a) import it lazily and build the class inside setup_telemetry, OR (b) define RedactingSpanProcessor as a plain duck-typed class implementing `on_start(self, span, parent_context=None)`, `on_end(self, span)`, `shutdown(self)`, `force_flush(self, timeout_millis=30000) -> bool: return True` (preferred — testable without the SDK; the distro accepts any object with these methods). Choose (b) and document the choice in a comment referencing AUD-06.
    `on_end(self, span)` must: route `span._name` (if set) and every string value in `span._attributes` through `safe_logger.redact` — the SAME audited surface used by `redact_mapping` at safe_logger.py:137-140; only `isinstance(v, str)` values are scrubbed (non-str left as-is). Wrap the entire scrub body in `try/except Exception: pass` so telemetry scrubbing NEVER breaks the request path (RESEARCH lines 314-315). Add NO new regex (AUD-06 — single audited redaction surface).
    Copy the canonical two-var AppInsights gate from audit_trail.py:39-51 (`_APPINSIGHTS_ENV_VARS` + `_appinsights_gate_open()`), or reuse via import — but inline copy is acceptable and keeps telemetry.py self-contained; cite "même garde qu'à audit_trail.py:47" in a comment.
    Define `def setup_telemetry() -> None:` that early-returns when `_appinsights_gate_open()` is False (identical inert posture to audit_trail.emit_document_access:77-78). When the gate is open, lazily `from azure.monitor.opentelemetry import configure_azure_monitor` and call it with `logger_name="AC360"` (matches safe_logger.logger name) and `span_processors=[RedactingSpanProcessor()]` so redaction runs in the export path (RESEARCH lines 319-330).
    Do NOT call setup_telemetry() at import time inside this module — it is called by api_server.py (Plan 02) at startup.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'scripts'); import telemetry; assert hasattr(telemetry,'RedactingSpanProcessor'); assert hasattr(telemetry,'setup_telemetry'); assert telemetry.setup_telemetry() is None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import sys; sys.path.insert(0,'scripts'); import telemetry"` exits 0 WITHOUT azure-monitor-opentelemetry installed (lazy import proven).
    - `grep -n "from safe_logger import redact" scripts/telemetry.py` matches (single audited surface).
    - `grep -vn '^[[:space:]]*#' scripts/telemetry.py | grep -E "re\.compile|import re" ` returns nothing (no new redaction regex — AUD-06).
    - `grep -n "APPLICATIONINSIGHTS_CONNECTION_STRING" scripts/telemetry.py` matches (gate present).
    - `grep -n "__all__" scripts/telemetry.py` lists RedactingSpanProcessor and setup_telemetry.
  </acceptance_criteria>
  <done>scripts/telemetry.py exists, imports cleanly without the SDK, redacts string span attributes through safe_logger.redact, leaves non-str attributes intact, and setup_telemetry() is inert (returns None) when the AppInsights gate is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/backend/test_telemetry_redaction.py — span-attribute redaction unit test</name>
  <files>tests/backend/test_telemetry_redaction.py</files>
  <read_first>
    - tests/backend/test_safe_logger_redaction.py:17-26 — reuse the fake PII/secret fixtures verbatim: FAKE_JWT, FAKE_SECRET_VALUE, FAKE_EMAIL = "jean.dupont@client-prive.fr", FAKE_IBAN = "FR7630006000011234567890189".
    - tests/backend/conftest.py:16-25 — already adds scripts/ to path and sets TENANT_ID/CLIENT_ID/JOBS_BASE_DIR; no extra fixture needed.
    - PATTERNS.md §test_telemetry_redaction.py (lines 171-177) — assertion shape: build a span with a PII-bearing attribute, run RedactingSpanProcessor().on_end(span), assert raw value gone + a MASQUÉ marker present.
    - RESEARCH Pitfall 3 (lines 574-578) — the behavior under test: PII-bearing span attribute must be masked after on_end.
    - scripts/safe_logger.py:65-67 — mask markers ([SECRET_MASQUÉ], [EMAIL_MASQUÉ], [PII_MASQUÉE]) so assertions can check for "MASQUÉ".
  </read_first>
  <action>
    Create `tests/backend/test_telemetry_redaction.py`. Import `from telemetry import RedactingSpanProcessor` (conftest adds scripts/ to path). Reuse the four fake PII/secret constants from test_safe_logger_redaction.py:17-26.
    Define a minimal stub span class with mutable `_name: str` and `_attributes: dict` (no live exporter, no SDK) — exactly the duck-typed shape RedactingSpanProcessor.on_end touches.
    Test 1 (`test_on_end_masks_string_pii_attributes`): build a span whose attributes include the fake email, fake IBAN, fake JWT, and fake secret value plus one int attribute; run `RedactingSpanProcessor().on_end(span)`; assert none of the four raw values remain anywhere in `str(span._attributes)`, assert "MASQUÉ" appears, and assert the int attribute is unchanged (non-str passthrough).
    Test 2 (`test_on_end_masks_span_name`): set `span._name` to a string containing FAKE_EMAIL; run on_end; assert FAKE_EMAIL not in span._name and "MASQUÉ" present.
    Test 3 (`test_on_end_never_raises`): pass a span whose `_attributes` is None / missing to confirm the try/except guard swallows it (no exception escapes) — proves telemetry scrubbing cannot break the request path.
  </action>
  <verify>
    <automated>python -m pytest tests/backend/test_telemetry_redaction.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/backend/test_telemetry_redaction.py -x` exits 0 (env TENANT_ID=test_tenant CLIENT_ID=test_client PYTHONPATH=scripts — conftest sets these).
    - The file references `RedactingSpanProcessor` and asserts at least one "MASQUÉ" marker.
    - `grep -n "FR7630006000011234567890189" tests/backend/test_telemetry_redaction.py` matches (IBAN fixture reused).
  </acceptance_criteria>
  <done>All three tests pass against Plan-01 telemetry.py; the test asserts PII/secret span attributes are masked, non-str passthrough is preserved, and on_end never raises.</done>
</task>

<task type="auto">
  <name>Task 3: Create /ready test scaffold + pin azure-monitor-opentelemetry in both requirements files</name>
  <files>tests/backend/test_ready_endpoint.py, requirements.txt, azure_functions/requirements.txt</files>
  <read_first>
    - tests/backend/test_security_headers.py:1-11 — TestClient bootstrap pattern: `sys.path.insert(0, ...scripts)`, `import api_server`, `client = TestClient(api_server.app)`.
    - tests/backend/conftest.py:16-25 — env already set (TENANT_ID/CLIENT_ID/JOBS_BASE_DIR + scripts path).
    - RESEARCH §Code Example 6 (lines 353-373) — the /ready contract: Entra-gated, coarse booleans, 200 if ready else 503; keyvault_ref check = OBO_CLIENT_SECRET must not start with "@Microsoft.KeyVault".
    - PATTERNS.md §test_ready_endpoint.py (lines 155-167) — override verify_azure_ad_token via app.dependency_overrides; assert 200 (ready) / 503 (degraded) / 401 (unauth, no override); monkeypatch env to flip readiness.
    - RESEARCH lines 62-65 + PATTERNS.md lines 151 — pin `azure-monitor-opentelemetry>=1.8.8,<2.0.0` (version VERIFIED on PyPI 2026-06-14) in BOTH requirements.txt and azure_functions/requirements.txt.
    - requirements.txt (existing tail: pytest/pytest-asyncio block) and azure_functions/requirements.txt (existing tail: jsonschema) — append the new pin under an "# Observability (OBS-01)" comment, preserving existing pins.
  </read_first>
  <action>
    Create `tests/backend/test_ready_endpoint.py` as a RED scaffold for OBS-03 (mirrors Phase 1 Wave-0 RED scaffolds; /ready does not exist until Plan 02). Use the TestClient bootstrap from test_security_headers.py:7-11.
    Write tests asserting the LOCKED /ready contract so Plan 02 implements against them:
      - `test_ready_unauthenticated_returns_401`: GET /ready with no auth override returns 401 (Entra-gated; verify_azure_ad_token rejects).
      - `test_ready_returns_200_when_dependencies_resolved`: override `api_server.verify_azure_ad_token` via `api_server.app.dependency_overrides` to return a fake oid; monkeypatch env so the readiness checks pass (e.g. OBO_CLIENT_SECRET set to a non-KV-literal value, AZURE_FUNCTION_URL set); assert 200 and JSON `status == "ready"`.
      - `test_ready_returns_503_when_dependency_unresolved`: same auth override; monkeypatch OBO_CLIENT_SECRET to a value starting with "@Microsoft.KeyVault" (unresolved KV reference) OR delete AZURE_FUNCTION_URL; assert 503 and `status == "degraded"`.
      - `test_ready_never_leaks_detail`: assert the response body contains only coarse boolean checks (no secret values, no exception strings) — assert no "@Microsoft.KeyVault" literal and no env secret value appears in the body.
    Always clear `api_server.app.dependency_overrides` in a finally/teardown so tests do not bleed state.
    Mark the file's module docstring noting it is a Wave-0 RED scaffold whose endpoint lands in Plan 02 (acceptable to xfail/skip the 200/503 cases until then IF the endpoint is absent — but prefer writing them to fail clearly; the executor of Plan 02 turns them green). Choose: use `@pytest.mark.xfail(reason="OBS-03 /ready lands in Plan 02", strict=False)` on the 200/503/no-leak cases so this plan's suite is green while encoding the contract; the 401 case (unknown route → 404 today) should also be xfail until the route exists. Document this xfail-until-Plan-02 decision in the docstring.
    Append `azure-monitor-opentelemetry>=1.8.8,<2.0.0` to BOTH requirements.txt and azure_functions/requirements.txt under a clear "# Observability (OBS-01) — real Azure Monitor exporter deferred from Phase 1" comment. Do not remove or reorder existing pins.
  </action>
  <verify>
    <automated>python -m pytest tests/backend/test_ready_endpoint.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/backend/test_ready_endpoint.py -x` exits 0 (xfail cases counted as expected-fail; suite green).
    - `grep -n "azure-monitor-opentelemetry" requirements.txt` matches with `>=1.8.8,<2.0.0`.
    - `grep -n "azure-monitor-opentelemetry" azure_functions/requirements.txt` matches with `>=1.8.8,<2.0.0`.
    - `grep -n "dependency_overrides" tests/backend/test_ready_endpoint.py` matches (Entra-gate override pattern present).
    - `grep -n "/ready" tests/backend/test_ready_endpoint.py` matches.
  </acceptance_criteria>
  <done>The /ready contract (401 unauth, 200 ready, 503 degraded, no-detail-leak) is encoded as tests (xfail until Plan 02), and azure-monitor-opentelemetry is pinned in both requirements files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process span → App Insights exporter | Span attributes (potentially PII/secret-bearing) cross from the app into the Azure Monitor export path |
| env config → telemetry activation | APPINSIGHTS env vars decide whether the exporter is wired (must stay inert in dev/test) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | RedactingSpanProcessor.on_end (telemetry export path) | mitigate | Route span name + every string attribute through safe_logger.redact (single audited surface, AUD-06); unit test asserts email/IBAN/JWT/secret masked (Task 2) |
| T-03-02 | Tampering | telemetry scrubbing crashing the request path | mitigate | Wrap on_end scrub in try/except Exception: pass — scrubbing can never raise into the request path |
| T-03-03 | Information Disclosure | telemetry active in dev/test/import | mitigate | setup_telemetry() early-returns when the two-var AppInsights gate is closed (inert by default); lazy SDK import |
| T-03-SC | Tampering | pip install azure-monitor-opentelemetry | mitigate | First-party Microsoft package, version VERIFIED on PyPI 2026-06-14 (RESEARCH Package Legitimacy Audit, Approved); first install gated by existing ci.yml pip-audit job |
</threat_model>

<verification>
- `python -m pytest tests/backend/test_telemetry_redaction.py tests/backend/test_ready_endpoint.py -x` exits 0 (env: TENANT_ID=test_tenant CLIENT_ID=test_client PYTHONPATH=scripts).
- `python -c "import sys; sys.path.insert(0,'scripts'); import telemetry"` exits 0 with the SDK uninstalled (import-safe).
- Both requirements files contain the pinned `azure-monitor-opentelemetry>=1.8.8,<2.0.0`.
- No new redaction regex introduced (grep for `re.compile` / `import re` in telemetry.py returns nothing outside comments).
</verification>

<success_criteria>
- scripts/telemetry.py provides RedactingSpanProcessor + gated setup_telemetry, redaction via safe_logger.redact only, inert when gate closed, import-safe without the SDK.
- test_telemetry_redaction.py proves PII/secret span attributes are masked and on_end never raises.
- test_ready_endpoint.py encodes the OBS-03 /ready contract (xfail until Plan 02).
- azure-monitor-opentelemetry pinned in both requirements files (OBS-01).
</success_criteria>

<output>
Create `.planning/phases/03-backend-deploy-observability/03-01-SUMMARY.md` when done.
</output>
