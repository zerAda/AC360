---
phase: 03-backend-deploy-observability
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/api_server.py
  - azure_functions/function_app.py
  - azure_functions/host.json
  - tests/backend/test_ready_endpoint.py
autonomous: true
requirements: [OBS-01, OBS-03]
must_haves:
  truths:
    - "The gateway calls setup_telemetry() once at startup, before serving traffic"
    - "GET /ready is Entra-gated and returns 200 (ready) / 503 (degraded) with coarse booleans only"
    - "GET /health stays anonymous and returns 200 (liveness; availability-test target)"
    - "The Functions worker wires configure_azure_monitor with the redaction processor; host.json telemetryMode is OpenTelemetry"
    - "The Functions worker does NOT re-instrument requests (no duplicate request telemetry)"
  artifacts:
    - path: "scripts/api_server.py"
      provides: "setup_telemetry() call at startup + Entra-gated /ready endpoint"
      contains: "/ready"
    - path: "azure_functions/function_app.py"
      provides: "guarded configure_azure_monitor wiring with RedactingSpanProcessor"
      contains: "configure_azure_monitor"
    - path: "azure_functions/host.json"
      provides: "telemetryMode OpenTelemetry"
      contains: "telemetryMode"
  key_links:
    - from: "scripts/api_server.py"
      to: "scripts/telemetry.py"
      via: "import + setup_telemetry() at startup"
      pattern: "setup_telemetry"
    - from: "scripts/api_server.py"
      to: "auth.verify_azure_ad_token"
      via: "/ready Depends(verify_azure_ad_token)"
      pattern: "Depends\\(verify_azure_ad_token\\)"
    - from: "azure_functions/function_app.py"
      to: "scripts/telemetry.py"
      via: "RedactingSpanProcessor + configure_azure_monitor"
      pattern: "configure_azure_monitor"
---

<objective>
Wire the real Azure Monitor exporter and readiness probe into both apps. On the gateway (`scripts/api_server.py`): call `setup_telemetry()` once at startup and add the Entra-gated `GET /ready` readiness endpoint (keeping anonymous `GET /health` as the liveness target). On the Functions worker (`azure_functions/function_app.py` + `host.json`): wire `configure_azure_monitor` with the same `RedactingSpanProcessor`, set host `telemetryMode: OpenTelemetry`, and avoid duplicate request telemetry.

Purpose: Land OBS-01 (App Insights wired on BOTH apps, redaction preserved through the OTel path) and OBS-03 (/ready + /health). This activates — for real — the inert audit_trail emit seam from Phase 1 (STATE 01-04) because once `configure_azure_monitor(logger_name="AC360")` runs, the existing `safe_logger.log_security` INFO logs export as App Insights traces with NO call-site change.
Output: Edits to api_server.py, function_app.py, host.json; the Plan-01 /ready tests turn green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backend-deploy-observability/03-RESEARCH.md
@.planning/phases/03-backend-deploy-observability/03-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

# Depends-on: Wave-0 telemetry module + /ready contract test
@.planning/phases/03-backend-deploy-observability/03-01-SUMMARY.md
@scripts/telemetry.py
@tests/backend/test_ready_endpoint.py

# Analogs (self)
@scripts/api_server.py
@azure_functions/function_app.py
@azure_functions/host.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire setup_telemetry() + add Entra-gated /ready to api_server.py</name>
  <files>scripts/api_server.py</files>
  <behavior>
    - GET /ready with no valid Entra token → 401 (Depends(verify_azure_ad_token) rejects).
    - GET /ready (authed) with all readiness checks passing → 200, JSON status == "ready".
    - GET /ready (authed) with an unresolved KV reference (OBO_CLIENT_SECRET starts with "@Microsoft.KeyVault") or missing AZURE_FUNCTION_URL → 503, JSON status == "degraded".
    - /ready body contains only coarse booleans — no secret values, no exception strings.
    - GET /health remains anonymous 200 with unchanged body (existing test_security_headers tests still pass).
    - setup_telemetry() is invoked exactly once at startup and never raises into request handling.
  </behavior>
  <read_first>
    - scripts/api_server.py:1-22 (imports), :70-74 (app = FastAPI(...)), :85-116 (shutdown hook + AppInsightsMiddleware + add_middleware), :672-679 (/health), :682-686 (Depends(verify_azure_ad_token) usage example).
    - scripts/api_server.py:25-33 (`_redacted_detail` no-leak posture) — /ready must follow the same no-detail-leak discipline.
    - scripts/telemetry.py (Plan 01) — setup_telemetry() signature + gate behavior.
    - RESEARCH §Code Example 6 (lines 353-373) — /ready shape: coarse booleans, 200/503, keyvault_ref check (OBO_CLIENT_SECRET must not start with "@Microsoft.KeyVault"), function_host reachability via AZURE_FUNCTION_URL.
    - PATTERNS.md §api_server.py (lines 121-137) — /health unchanged; /ready Entra-gated via Depends; add JSONResponse to the `from fastapi.responses import FileResponse` import (line 2); reuse the existing APPINSIGHTS gate.
    - tests/backend/test_ready_endpoint.py (Plan 01) — the exact contract this task must satisfy; flip its xfail markers off once green.
  </read_first>
  <action>
    Add `JSONResponse` to the existing `from fastapi.responses import FileResponse` import (api_server.py:2 → `from fastapi.responses import FileResponse, JSONResponse`).
    Import setup_telemetry from the new module: add `from telemetry import setup_telemetry` to the import block (api_server.py:14-22 area, alongside `from audit_trail import emit_document_access`).
    Call `setup_telemetry()` once at startup BEFORE the app serves traffic — call it at module import right after `app = FastAPI(...)` is constructed (api_server.py:70-74), OR register an `@app.on_event("startup")` handler that calls it. Prefer a startup-event handler symmetrical to the existing `@app.on_event("shutdown")` at line 85; document that setup_telemetry is gate-inert so import-time call is also safe. setup_telemetry must never raise into startup (it already early-returns when the gate is closed; wrap the call defensively if needed).
    Add `GET /ready` (Entra-gated readiness), keeping `GET /health` (line 672) exactly as-is (anonymous liveness 200 — the availability-test target). Implement /ready per RESEARCH Code Example 6:
      - signature: `async def readiness(oid: str = Depends(verify_azure_ad_token)):`
      - build a `checks` dict of coarse booleans: `keyvault_ref` = OBO_CLIENT_SECRET env is set AND does not start with "@Microsoft.KeyVault" (i.e. the KV reference resolved at runtime); `function_host` = bool(AZURE_FUNCTION_URL). Optionally a shallow OCR/Fabric reachability bool with a short bounded timeout returning bool only (no PII, no secret) — keep optional and bounded.
      - `ready = all(checks.values())`; return `JSONResponse(status_code=200 if ready else 503, content={"status": "ready" if ready else "degraded", "checks": checks})`.
      - NEVER leak detail: the body returns only the coarse boolean `checks` and the coarse status string — no secret values, no exception text (consistent with `_redacted_detail` at lines 25-33).
    Leave the existing AppInsightsMiddleware (lines 90-116) as belt-and-braces (it gates on APPINSIGHTS_INSTRUMENTATIONKEY and already redacts via redact_mapping). Removing it is permitted per RESEARCH line 332 (Claude's discretion) ONLY if you confirm test_security_headers.py:33-57 still passes — it asserts redact_mapping is called on /health telemetry, so KEEP the middleware to avoid breaking that test. Do not modify the middleware behavior.
    Flip the xfail markers off in tests/backend/test_ready_endpoint.py (Plan 01) now that /ready exists, so the 401/200/503/no-leak cases run as real assertions.
  </action>
  <verify>
    <automated>python -m pytest tests/backend/test_ready_endpoint.py tests/backend/test_security_headers.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/backend/test_ready_endpoint.py tests/backend/test_security_headers.py -x` exits 0 with the /ready cases now passing as real (not xfail).
    - `grep -n "Depends(verify_azure_ad_token)" scripts/api_server.py` matches inside the /ready handler.
    - `grep -n "setup_telemetry" scripts/api_server.py` matches (imported + called).
    - `grep -n "JSONResponse" scripts/api_server.py` matches in the fastapi.responses import.
    - `grep -n '"status": "ready"' scripts/api_server.py` (or equivalent) confirms coarse status; no secret/exception value is interpolated into the /ready body.
    - /health body unchanged: `python -m pytest tests/backend/test_security_headers.py::test_health_has_no_inflated_security_claim -x` exits 0.
  </acceptance_criteria>
  <done>Gateway calls setup_telemetry() at startup; /ready is Entra-gated returning 200/503 coarse booleans with no detail leak; /health stays anonymous 200; the Plan-01 /ready tests pass as real assertions and existing security-header tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Functions-worker telemetry — function_app.py + host.json</name>
  <files>azure_functions/function_app.py, azure_functions/host.json</files>
  <read_first>
    - azure_functions/function_app.py:1-45 — guarded-import header (try/except around azure.* at lines 26-33); the `from audit_pipeline import ...` / `from audit_trail import ...` block (35+) and the load-bearing AUD-07 comment.
    - azure_functions/host.json:1-16 — existing version "2.0", logging.applicationInsights samplingSettings (excludedTypes "Request"), extensionBundle [4.*, 5.0.0).
    - RESEARCH §Code Example 5 (lines 339-351) — host.json `"telemetryMode": "OpenTelemetry"`; function_app.py `configure_azure_monitor(logger_name="AC360", span_processors=[RedactingSpanProcessor()])`; app settings APPLICATIONINSIGHTS_CONNECTION_STRING + PYTHON_APPLICATIONINSIGHTS_ENABLE_TELEMETRY=true (the app settings are wired in Plan 03 Bicep, not here).
    - RESEARCH Pitfall 2 (lines 568-572) — duplicate request telemetry: host emits request telemetry; the worker distro must NOT re-instrument requests. Use host telemetryMode for request spans; worker configure_azure_monitor only for logs/custom events.
    - PATTERNS.md §function_app.py + host.json (lines 141-151) — guarded-import discipline; preserve existing host.json blocks; the duplicate-telemetry guard.
    - scripts/telemetry.py (Plan 01) — RedactingSpanProcessor is importable from telemetry (function_app.py already inserts ../scripts into sys.path at lines 21-24).
  </read_first>
  <action>
    host.json: add `"telemetryMode": "OpenTelemetry"` at the root object, preserving the existing `version`, `logging.applicationInsights` (samplingSettings/excludedTypes/enableLiveMetricsFilters), and `extensionBundle` blocks unchanged. Keep valid JSON.
    function_app.py: wire the worker exporter consistent with the existing guarded-import discipline (lines 26-33) so pytest collection and non-runtime imports never fail:
      - Import `from telemetry import RedactingSpanProcessor, setup_telemetry` (scripts/ is already on sys.path via lines 21-24). Prefer calling the SAME `setup_telemetry()` used by the gateway (single wiring surface) — it lazily imports and calls `configure_azure_monitor(logger_name="AC360", span_processors=[RedactingSpanProcessor()])` and is gate-inert when APPINSIGHTS env is unset, so it is safe to call at worker module load and during pytest collection.
      - Guard the call: wrap `setup_telemetry()` in the existing try/except posture (or its own try/except Exception: pass) so a missing SDK or misconfig never breaks the Functions host load. Place it after the guarded azure.* import block.
      - Do NOT add any FastAPI/request instrumentation on the worker and do NOT set extra request-instrumentation options — the host emits request telemetry (telemetryMode), the worker exports logs/custom events only (Pitfall 2: no duplicate request telemetry).
    Note in a comment (referencing RESEARCH Pitfall 2 + line 350) that the app settings APPLICATIONINSIGHTS_CONNECTION_STRING and PYTHON_APPLICATIONINSIGHTS_ENABLE_TELEMETRY=true are wired by Bicep in Plan 03, not here.
    Do not import azure-monitor-opentelemetry at module top level in function_app.py — rely on telemetry.setup_telemetry()'s lazy import so pytest collection stays SDK-free.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('azure_functions/host.json')); assert d.get('telemetryMode')=='OpenTelemetry'; assert d.get('version')=='2.0'; assert 'extensionBundle' in d; print('host.json OK')" && python -m pytest tests/azure_functions/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; d=json.load(open('azure_functions/host.json')); assert d['telemetryMode']=='OpenTelemetry'"` exits 0 (valid JSON + telemetryMode set; existing blocks intact).
    - `grep -n "configure_azure_monitor\|setup_telemetry" azure_functions/function_app.py` matches (worker wiring present).
    - `grep -vn '^[[:space:]]*#' azure_functions/function_app.py | grep -n "import azure.monitor.opentelemetry"` returns nothing at module top level (lazy import preserved via telemetry.setup_telemetry).
    - `python -m pytest tests/azure_functions/ -x -q` exits 0 (collection + worker tests unaffected; no SDK required).
  </acceptance_criteria>
  <done>host.json sets telemetryMode OpenTelemetry with existing blocks preserved; function_app.py wires the redaction-preserving worker exporter via the guarded setup_telemetry, does not re-instrument requests, and pytest collection still works without the SDK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → /health | anonymous liveness probe (must not expose internal detail) |
| Entra-authed client → /ready | readiness probe; must return coarse booleans only |
| worker span → App Insights exporter | worker logs/custom events cross into Azure Monitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | /ready response body | mitigate | /ready returns coarse booleans + a coarse status string only; no secret values, no exception text (same posture as _redacted_detail); Entra-gated via Depends(verify_azure_ad_token) |
| T-03-05 | Spoofing/Access Control | /ready reachable unauthenticated | mitigate | /ready uses Depends(verify_azure_ad_token) (401 when unauth); test_ready_unauthenticated_returns_401 enforces it |
| T-03-06 | Information Disclosure | worker telemetry leaking PII | mitigate | Worker uses the SAME RedactingSpanProcessor (safe_logger.redact); telemetryMode OpenTelemetry on host; no raw attribute export |
| T-03-07 | DoS/Cost | duplicate request telemetry on Functions | mitigate | Host emits request spans (telemetryMode); worker distro exports logs/custom events only — no request re-instrumentation (Pitfall 2) |
</threat_model>

<verification>
- `python -m pytest tests/backend/test_ready_endpoint.py tests/backend/test_security_headers.py tests/azure_functions/ -x -q` exits 0.
- host.json is valid JSON with telemetryMode == "OpenTelemetry" and all prior blocks intact.
- /ready Entra-gated (401 unauth), 200 ready / 503 degraded, coarse booleans only; /health unchanged anonymous 200.
- No module-top-level import of azure-monitor-opentelemetry in api_server.py or function_app.py (lazy via setup_telemetry).
</verification>

<success_criteria>
- OBS-01: configure_azure_monitor (via setup_telemetry) wired on BOTH gateway and Functions worker, redaction preserved through RedactingSpanProcessor; host.json telemetryMode OpenTelemetry.
- OBS-03: /ready (Entra-gated 200/503 coarse) added; /health (anonymous 200 liveness) preserved.
- No duplicate request telemetry on Functions; pytest collection remains SDK-free.
</success_criteria>

<output>
Create `.planning/phases/03-backend-deploy-observability/03-02-SUMMARY.md` when done.
</output>
