---
phase: 05-rgpd-security-evidence-pack
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]   # shares infra/main.bicep with 05-01 (sequential edit); 05-01 lands the storage rule first
files_modified:
  - infra/observability.bicep
  - infra/main.bicep
  - docs/governance/RGP-04-pii-in-logs-statement.md
autonomous: true
requirements: [RGP-04]
user_setup: []

must_haves:
  truths:
    - "Log Analytics retention is a deliberate, EU-region, parameterized 90-day window (not a silent hardcoded default)"
    - "PII never reaches App Insights telemetry: every string span attribute is routed through the single audited redaction surface before export"
    - "The PII-in-logs handling is written down with the exact processor and retention value as compliance evidence"
  artifacts:
    - path: "infra/observability.bicep"
      provides: "logAnalyticsRetentionDays param (default 90) governing the workspace retentionInDays"
      contains: "logAnalyticsRetentionDays"
    - path: "docs/governance/RGP-04-pii-in-logs-statement.md"
      provides: "PII-in-logs handling statement: RedactingSpanProcessor + safe_logger + 90-day EU retention + 4-field hashed audit trail"
      contains: "RedactingSpanProcessor"
  key_links:
    - from: "infra/main.bicep"
      to: "infra/observability.bicep"
      via: "logAnalyticsRetentionDays threaded into the observability module params"
      pattern: "logAnalyticsRetentionDays"
    - from: "docs/governance/RGP-04-pii-in-logs-statement.md"
      to: "scripts/telemetry.py RedactingSpanProcessor"
      via: "documents the existing processor + tests/backend/test_telemetry_redaction.py"
      pattern: "RedactingSpanProcessor"
---

<objective>
Deliver RGP-04: promote the hardcoded Log Analytics `retentionInDays: 30` to a parameter defaulting to a deliberate, EU-region 90-day window threaded from `main.bicep`, and author the PII-in-logs handling statement that names the existing `RedactingSpanProcessor` (Phase 3) + `safe_logger.redact` as the single audited telemetry-redaction surface, the 4-field hashed (no raw PII) audit trail, and the 90-day retention — all linked to the existing redaction test.

Purpose: RGP-04 (success criterion #3) requires a PII-in-logs handling statement + a telemetry processor + deliberate short EU-region retention. The redaction code and its test already exist (Phase 3); this plan parameterizes retention and documents the posture.
Output: `logAnalyticsRetentionDays` param in `infra/observability.bicep` (threaded from `infra/main.bicep`) + `docs/governance/RGP-04-pii-in-logs-statement.md`.

Wave note: this plan shares `infra/main.bicep` with Plan 05-01 (which adds the storage `managementPolicies` rule). It runs AFTER 05-01 (Wave 2, `depends_on: 05-01`) so the two main.bicep edits never collide — this plan only adds the observability-module param wiring; it does not touch the storage/lifecycle section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md
@.planning/phases/05-rgpd-security-evidence-pack/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: parameterize Log Analytics retention to 90 days (bump-to-param)</name>
  <files>infra/observability.bicep, infra/main.bicep</files>
  <read_first>
    - infra/observability.bicep lines 46-57 — the `law 'Microsoft.OperationalInsights/workspaces@2023-09-01'` resource with the hardcoded `retentionInDays: 30` to promote.
    - .planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md §"Pattern 3: Log Analytics retention bump-to-param" (lines 242-264) — the exact `@minValue(30) @maxValue(730) param logAnalyticsRetentionDays int = 90` form; workspace governs retention (workspace-based App Insights component needs NO retention property).
    - infra/main.bicep — find the `module observability` / observability module invocation block (the call passing `location`, `gatewayId`, `functionId`, `gatewayName`, `alertEmails`, `teamsWebhookUrl`) to thread the new param through.
    - .planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md §Pitfall — do NOT set `RetentionInDays` on the `Microsoft.Insights/components` resource; for workspace-based components it is moot.
  </read_first>
  <action>
    In `infra/observability.bicep`, add `@description('Rétention Log Analytics en jours (RGP-04 : 90 j, EU-region, délibérément courte).') @minValue(30) @maxValue(730) param logAnalyticsRetentionDays int = 90` and change the `law` resource's `retentionInDays: 30` to `retentionInDays: logAnalyticsRetentionDays` (keep the RGP-04 comment). Do NOT add any retention property to the `appi` (`Microsoft.Insights/components`) resource — workspace-based retention is governed by the workspace alone. In `infra/main.bicep`, add a matching `logAnalyticsRetentionDays int = 90` param and pass it into the observability module invocation params block. Keep the soft-EU-retention rationale comment per the locked CONTEXT decision (90 days). Do NOT touch the `infra/main.bicep` storage/`managementPolicies`/lifecycle code (owned by Plan 05-01) — only add the observability-module param + its wiring here.
  </action>
  <verify>
    <automated>az bicep build -f infra/observability.bicep</automated>
  </verify>
  <acceptance_criteria>
    - `az bicep build -f infra/observability.bicep` exits 0.
    - `az bicep build -f infra/main.bicep` exits 0 (module param wiring compiles; 05-01 storage rule still present).
    - `grep -c "logAnalyticsRetentionDays" infra/observability.bicep` returns at least 2 (param decl + usage in `law`).
    - `grep "retentionInDays: logAnalyticsRetentionDays" infra/observability.bicep` matches.
    - `grep "logAnalyticsRetentionDays" infra/main.bicep` matches (threaded through).
    - `grep "managementPolicies" infra/main.bicep` still matches (05-01 storage rule preserved — no clobber).
  </acceptance_criteria>
  <done>Log Analytics retention is a 90-day parameter threaded main.bicep → observability module; both templates compile; no component-level retention added; 05-01's storage rule untouched.</done>
</task>

<task type="auto">
  <name>Task 2: author the PII-in-logs handling statement (RGP-04 evidence)</name>
  <files>docs/governance/RGP-04-pii-in-logs-statement.md</files>
  <read_first>
    - tests/backend/test_telemetry_redaction.py (whole file) — the EXISTING test proving `RedactingSpanProcessor.on_end` redacts every str attribute + span name and never raises; cite it by path as the evidence link (no new test needed — RGP-04 redaction is already covered).
    - scripts/telemetry.py — the `RedactingSpanProcessor` / `setup_telemetry` surface (lazy-import, gate-inert out of prod) to describe accurately.
    - docs/security/SECURITY_POSTURE.md §5 "Surface de rédaction" (lines 103-114) and §6 "Contrat de champs du journal d'audit" (lines 115-135) — the single-audited-redaction-surface framing and the 4-field {user_id_hash, document_id, ts_utc, verdict} no-raw-PII audit-trail contract to reference.
    - .planning/phases/05-rgpd-security-evidence-pack/05-CONTEXT.md §"RGPD Data Lifecycle" RGP-04 (line 30) — locked: confirm RedactingSpanProcessor as the processor + 90-day short EU retention.
  </read_first>
  <action>
    Create `docs/governance/RGP-04-pii-in-logs-statement.md` (French domain doc) as the PII-in-logs handling statement with sections covering: (1) the single audited redaction surface — `safe_logger.redact` / `redact_mapping` and the telemetry-export-time `scripts/telemetry.py RedactingSpanProcessor` (every str span attribute + span name routed through redact before Azure Monitor export; never a second redaction code path — AUD-06); (2) the 4-field hashed audit-trail contract `{user_id_hash = hash_id(oid), document_id, ts_utc, verdict}` with no raw UPN/name/oid crossing the telemetry boundary; (3) deliberate short EU-region Log Analytics retention = 90 days via `logAnalyticsRetentionDays` (cross-reference Plan 05-02 Task 1 / observability.bicep); (4) the honest "journal immuable" framing (append-only + retention + RBAC + resource-lock, NOT WORM) carried from SECURITY_POSTURE.md §7 — do not regress it; (5) evidence link to `tests/backend/test_telemetry_redaction.py` as the proof. Note the per-table audit-trail retention override (`workspaces/tables`) as a documented DPO option (not enabled now).
  </action>
  <verify>
    <automated>powershell -Command "if ((Select-String -Path docs/governance/RGP-04-pii-in-logs-statement.md -Pattern 'RedactingSpanProcessor','90','test_telemetry_redaction','user_id_hash' -SimpleMatch).Count -ge 4) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/governance/RGP-04-pii-in-logs-statement.md` exists.
    - `grep -c "## " docs/governance/RGP-04-pii-in-logs-statement.md` returns at least 4 (multiple sections).
    - `grep "RedactingSpanProcessor" docs/governance/RGP-04-pii-in-logs-statement.md` matches.
    - `grep "test_telemetry_redaction" docs/governance/RGP-04-pii-in-logs-statement.md` matches (evidence link).
    - `grep -E "90" docs/governance/RGP-04-pii-in-logs-statement.md` matches (retention stated).
    - `grep -iE "WORM" docs/governance/RGP-04-pii-in-logs-statement.md` matches (honest immutability framing carried forward).
  </acceptance_criteria>
  <done>The PII-in-logs statement names the existing redaction processor, the 4-field hashed audit trail, the 90-day EU retention, and links to the existing redaction test as evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application span → Azure Monitor / Log Analytics | Telemetry can carry PII (emails, IBAN, JWT) in span names/attributes before export |
| Log Analytics workspace → tenant | Retention window governs how long any (redacted) telemetry persists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | PII in telemetry span attributes/names (LLM02 / OWASP) | mitigate | EXISTING `RedactingSpanProcessor.on_end` routes every str attr + span name through `safe_logger.redact` before export (proven by `tests/backend/test_telemetry_redaction.py`); documented here |
| T-05-07 | Information Disclosure | Telemetry over-retention | mitigate | Deliberate short EU-region 90-day workspace retention via `logAnalyticsRetentionDays` param |
| T-05-08 | Repudiation | Claiming WORM/table immutability for the audit trail | mitigate | RGP-04 doc carries SECURITY_POSTURE.md §7 honest framing (append-only + retention + RBAC + resource-lock, NOT WORM) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `az bicep build -f infra/observability.bicep` exits 0 AND `az bicep build -f infra/main.bicep` exits 0.
- `python -m pytest tests/backend/test_telemetry_redaction.py -x` exits 0 (RGP-04 redaction evidence — existing test, no new test added).
- Structural grep asserts on `docs/governance/RGP-04-pii-in-logs-statement.md` pass.
</verification>

<success_criteria>
RGP-04 satisfied: Log Analytics retention is a deliberate 90-day EU-region parameter; the PII-in-logs statement documents the existing single redaction surface, the 4-field hashed audit trail, and the 90-day retention, linked to the existing redaction test. No package installs.
</success_criteria>

<output>
Create `.planning/phases/05-rgpd-security-evidence-pack/05-02-SUMMARY.md` when done.
</output>
