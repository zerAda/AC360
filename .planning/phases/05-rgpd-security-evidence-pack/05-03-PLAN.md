---
phase: 05-rgpd-security-evidence-pack
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/security/SEC-01-architecture-dataflow.md
  - docs/security/SEC-02-authn-authz.md
autonomous: true
requirements: [SEC-01, SEC-02]
user_setup: []

must_haves:
  truths:
    - "A reader can see the AC360 architecture + PII data-flow with explicit trust boundaries rendered in GitHub"
    - "A reviewer can trace every authN/authZ control (Entra SSO, JWT RS256/JWKS, OBO scope, IDOR, read-only) to a concrete existing test"
  artifacts:
    - path: "docs/security/SEC-01-architecture-dataflow.md"
      provides: "Mermaid architecture + data-flow diagrams, PII flow, trust boundaries"
      contains: "```mermaid"
    - path: "docs/security/SEC-02-authn-authz.md"
      provides: "AuthN/authZ description linked to existing tests by path"
      contains: "test_auth_jwt"
  key_links:
    - from: "docs/security/SEC-02-authn-authz.md"
      to: "tests/backend/test_auth_jwt.py + test_audit_ownership.py + test_job_isolation.py"
      via: "each control row links the proving test by path"
      pattern: "tests/"
---

<objective>
Produce the first two security-evidence-pack documents: SEC-01 (architecture + data-flow with PII flow and trust boundaries, rendered as GitHub Mermaid) and SEC-02 (the authN/authZ description — Entra SSO, JWT RS256/JWKS, OBO scope, IDOR gate, read-only — with every control linked to an existing test by path).

Purpose: SEC-01 + SEC-02 are two of the five components of success criterion #1 (the security evidence pack). Both are pure markdown synthesized from artifacts already in the repo (the RESEARCH ASCII diagram, SECURITY_POSTURE.md, existing auth tests).
Output: `docs/security/SEC-01-architecture-dataflow.md` + `docs/security/SEC-02-authn-authz.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md
@.planning/phases/05-rgpd-security-evidence-pack/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-01 architecture + data-flow diagram (Mermaid, PII flow, trust boundaries)</name>
  <files>docs/security/SEC-01-architecture-dataflow.md</files>
  <read_first>
    - .planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md §"System Architecture Diagram (PII / retention flow)" (lines 68-105) — the canonical ASCII flow to convert to Mermaid: Teams/Copilot → Gateway (B1, 1 worker, JWKS, JWT RS256) → Durable Functions (download→OCR→Fabric→FIC, JOBS_BASE_DIR) → App Insights/Log Analytics; the OBO path to Microsoft Graph; the three retention enforcement points (①②③).
    - CLAUDE.md §Architecture "Component Responsibilities" + "Trust Boundaries" wording — the Entra ID SSO boundary, OBO user-delegated boundary, "PII enters here" at the OCR/document ingestion point.
    - .planning/phases/05-rgpd-security-evidence-pack/05-CONTEXT.md §"Security Evidence Pack" SEC-01 (line 22) — locked: Mermaid in markdown, architecture + data-flow with PII flow + trust boundaries.
    - .planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md §"Recommended Evidence-Pack File Layout" (lines 107-127) — file path docs/security/SEC-01-architecture-dataflow.md; do not overwrite SECURITY_POSTURE.md.
  </read_first>
  <action>
    Create `docs/security/SEC-01-architecture-dataflow.md` with at least two GitHub-rendered ```mermaid``` blocks: (1) a component/architecture diagram (`graph`/`flowchart`) showing Teams/Copilot user → FastAPI gateway (B1, single worker; JWT RS256 via JWKS) → Durable Functions (Flex; single-activity download→OCR(DocIntel)→Fabric→FIC) → App Insights/Log Analytics, plus the OBO user-delegated path to Microsoft Graph/SharePoint; (2) a PII data-flow diagram marking where PII enters (client document → OCR), where it is ephemeral (`JOBS_BASE_DIR`, 30-day TTL — cross-ref RGP-03), and where redacted telemetry flows (RedactingSpanProcessor → Log Analytics, 90-day — cross-ref RGP-04). Use Mermaid `subgraph` blocks to draw explicit TRUST BOUNDARIES: Entra ID SSO boundary, OBO user-delegated boundary (X-MS-Graph-Token never persisted), and the storage/data-plane boundary. Add a prose "Frontières de confiance" section enumerating each boundary and the untrusted input crossing it. French domain prose, English identifiers. This is the SEC-01 source diagram referenced by SEC-03.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content -Raw docs/security/SEC-01-architecture-dataflow.md; if (([regex]::Matches($c,'```mermaid')).Count -ge 2 -and $c -match 'SEC-01') { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/security/SEC-01-architecture-dataflow.md` exists.
    - `grep -c '```mermaid' docs/security/SEC-01-architecture-dataflow.md` returns at least 2.
    - `grep "SEC-01" docs/security/SEC-01-architecture-dataflow.md` matches.
    - `grep -iE "OBO|trust|confiance|boundary|frontière" docs/security/SEC-01-architecture-dataflow.md` matches (trust boundaries present).
    - `grep -iE "JOBS_BASE_DIR|OCR|PII" docs/security/SEC-01-architecture-dataflow.md` matches (PII flow present).
  </acceptance_criteria>
  <done>SEC-01 has ≥2 Mermaid diagrams (architecture + PII data-flow), explicit trust boundaries, and the PII entry/ephemeral/redacted-telemetry flow with RGP-03/RGP-04 cross-refs.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-02 authN/authZ description linked to existing tests</name>
  <files>docs/security/SEC-02-authn-authz.md</files>
  <read_first>
    - docs/security/SECURITY_POSTURE.md §3 "Analyse IDOR — owner_hash sur oid" (lines 71-87) and §4 "Posture OBO" (lines 89-101) — the authoritative IDOR-gate and OBO descriptions to reuse.
    - .planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md §"Security Domain → Applicable ASVS Categories" (lines 463-472) — V2 Authentication, V3 Session, V4 Access Control mapping to the concrete controls (auth.py, _assert_durable_owner, OBO RBAC, read-only).
    - tests/backend/test_auth_jwt.py, tests/backend/test_auth_jwt_real.py, tests/backend/test_wave1_auth_identity.py, tests/backend/test_audit_ownership.py, tests/backend/test_job_isolation.py — the EXISTING tests to cite by exact path (skim each to confirm what it proves: JWT/JWKS validation, oid identity, owner_hash ownership, cross-job isolation).
    - .planning/phases/05-rgpd-security-evidence-pack/05-CONTEXT.md §"Security Evidence Pack" SEC-02 (line 23) — locked: authN/authZ description linked to existing tests.
  </read_first>
  <action>
    Create `docs/security/SEC-02-authn-authz.md` (French domain doc) describing each authN/authZ control mapped to a proving test in a "Contrôle → Description → Test (chemin)" table: (1) Entra ID SSO + JWT RS256 validation via JWKS (audience/issuer/signature checks in `scripts/auth.py`) → `tests/backend/test_auth_jwt.py`, `tests/backend/test_auth_jwt_real.py`; (2) `oid`-based identity (token without `oid` rejected 401) → `tests/backend/test_wave1_auth_identity.py`; (3) authoritative IDOR gate — durable `owner_hash = hash_id(oid)` checked by `_assert_durable_owner` (in-memory map is cache only) → `tests/backend/test_audit_ownership.py`, `tests/backend/test_job_isolation.py`; (4) OBO user-delegated Graph token honoring SharePoint RBAC, never persisted; (5) read-only enforcement (no write/actions). Reference SECURITY_POSTURE.md §3/§4 as the upstream source and tie controls to ASVS V2/V3/V4. Every control row MUST cite at least one `tests/...` path.
  </action>
  <verify>
    <automated>powershell -Command "if ((Select-String -Path docs/security/SEC-02-authn-authz.md -Pattern 'test_auth_jwt','owner_hash','OBO','SEC-02' -SimpleMatch).Count -ge 4) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/security/SEC-02-authn-authz.md` exists.
    - `grep -c "tests/" docs/security/SEC-02-authn-authz.md` returns at least 3 (multiple test links).
    - `grep "test_auth_jwt" docs/security/SEC-02-authn-authz.md` matches AND `grep "test_audit_ownership" docs/security/SEC-02-authn-authz.md` matches.
    - `grep -iE "RS256|JWKS" docs/security/SEC-02-authn-authz.md` matches.
    - `grep -iE "OBO" docs/security/SEC-02-authn-authz.md` matches AND `grep -iE "read-only|lecture seule" docs/security/SEC-02-authn-authz.md` matches.
  </acceptance_criteria>
  <done>SEC-02 maps every authN/authZ control (SSO, JWT RS256/JWKS, oid identity, IDOR gate, OBO, read-only) to at least one existing test cited by path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teams/Copilot client → FastAPI gateway | Untrusted JWT crosses; validated RS256/JWKS at the gateway |
| Gateway → Microsoft Graph (OBO) | User-delegated token honors SharePoint RBAC; never persisted |
| User A → User B's audit job (IDOR surface) | Authoritative durable owner_hash gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Spoofing | Forged/invalid JWT at the gateway | mitigate (documented) | Entra SSO + JWT RS256/JWKS validation in `scripts/auth.py`; SEC-02 cites `test_auth_jwt*` |
| T-05-10 | Elevation of Privilege / Info Disclosure | IDOR — access to another user's audit job | mitigate (documented) | Durable `owner_hash` (oid-derived) authoritative gate `_assert_durable_owner`; SEC-02 cites `test_audit_ownership` / `test_job_isolation` |
| T-05-11 | Information Disclosure | Reviewer cannot verify a control is actually tested | mitigate | SEC-02 links every control row to a concrete existing test by path (traceability) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Structural grep asserts on both `docs/security/SEC-01-architecture-dataflow.md` and `docs/security/SEC-02-authn-authz.md` pass.
- The tests cited by SEC-02 are real and green (already part of the suite): `python -m pytest tests/backend/test_auth_jwt.py tests/backend/test_audit_ownership.py tests/backend/test_job_isolation.py -x` exits 0.
</verification>

<success_criteria>
SEC-01 + SEC-02 evidence docs exist: SEC-01 with ≥2 Mermaid diagrams showing architecture, PII flow, and trust boundaries; SEC-02 mapping every authN/authZ control to an existing test by path. No package installs.
</success_criteria>

<output>
Create `.planning/phases/05-rgpd-security-evidence-pack/05-03-SUMMARY.md` when done.
</output>
