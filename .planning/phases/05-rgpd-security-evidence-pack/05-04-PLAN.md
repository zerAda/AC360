---
phase: 05-rgpd-security-evidence-pack
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/security/SEC-03-threat-coverage-matrix.md
  - docs/security/SEC-04-dependency-posture.md
  - docs/security/SEC-05-accepted-risk-register.md
  - .github/dependabot.yml
autonomous: true
requirements: [SEC-03, SEC-04, SEC-05]
user_setup: []

must_haves:
  truths:
    - "A reviewer can read each OWASP/LLM + STRIDE risk and see its concrete AC360 mitigation and the test that proves it"
    - "The dependency/vuln posture is written down: Dependabot enabled (pip + actions) and the PyJWT/deltalake pin policy"
    - "Every CONCERNS.md item is classified must-fix-done (Phase 1) vs accepted-deferred (with rationale)"
  artifacts:
    - path: "docs/security/SEC-03-threat-coverage-matrix.md"
      provides: "OWASP LLM Top 10 2025 + STRIDE → mitigation → test matrix"
      contains: "LLM01"
    - path: "docs/security/SEC-04-dependency-posture.md"
      provides: "Dependabot posture + PyJWT/deltalake pin policy"
      contains: "dependabot"
    - path: "docs/security/SEC-05-accepted-risk-register.md"
      provides: "CONCERNS.md items classified must-fix-done vs accepted-deferred"
      contains: "SEC-05"
  key_links:
    - from: "docs/security/SEC-03-threat-coverage-matrix.md"
      to: "per-phase <threat_model> blocks + existing tests"
      via: "each risk row cites mitigation + evidence/test"
      pattern: "tests/|GUARDRAILS|validate_copilot"
    - from: "docs/security/SEC-04-dependency-posture.md"
      to: ".github/dependabot.yml"
      via: "documents the existing Dependabot config (do not recreate)"
      pattern: "dependabot.yml"
---

<objective>
Produce the remaining three security-evidence-pack documents: SEC-03 (threat-coverage matrix synthesizing per-phase STRIDE `<threat_model>` blocks + OWASP LLM Top 10 2025 → AC360 mitigation → proving test), SEC-04 (dependency/vuln posture: DOCUMENT the existing `.github/dependabot.yml` + the PyJWT/deltalake pin policy, optionally tighten with a security-only group — do NOT recreate the file), and SEC-05 (accepted-risk / known-issues register classifying every CONCERNS.md item must-fix-done vs accepted-deferred).

Purpose: SEC-03/04/05 complete the five components of success criterion #1 (the security evidence pack).
Output: `docs/security/SEC-03-threat-coverage-matrix.md`, `docs/security/SEC-04-dependency-posture.md`, `docs/security/SEC-05-accepted-risk-register.md`, and an optional `groups`/security tightening to `.github/dependabot.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md
@.planning/phases/05-rgpd-security-evidence-pack/05-CONTEXT.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEC-03 threat-coverage matrix (OWASP LLM 2025 + STRIDE → mitigation → test)</name>
  <files>docs/security/SEC-03-threat-coverage-matrix.md</files>
  <read_first>
    - .planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md §"OWASP LLM Top 10 2025 → AC360 mitigation → test (SEC-03 rows)" (lines 364-376) — the seed rows (LLM01 prompt injection, LLM02 sensitive info disclosure, LLM03 supply chain, LLM05 output handling, LLM06 excessive agency, LLM07 system prompt leakage) and their evidence (GUARDRAILS_VALIDATION.md, test_no_plaintext_secrets, schemas/audit_result.schema.json, test_audit_ownership/test_job_isolation, GOVERNANCE.md §3).
    - .planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md §"Known Threat Patterns" (lines 474-482) — the STRIDE rows (prompt injection→Tampering, PII disclosure→Info Disclosure, IDOR→EoP, PII over-retention→Info Disclosure, OCR PII public endpoint, secrets in error responses).
    - docs/security/GUARDRAILS_VALIDATION.md and docs/security/SECURITY_POSTURE.md — the live-guardrail + redaction evidence to cite.
    - The `<threat_model>` blocks in the Phase 1-5 PLAN.md files (grep `T-0` / `STRIDE Threat Register` across .planning/phases/*/*-PLAN.md) — the per-phase STRIDE registers to synthesize into the matrix per the locked SEC-03 decision.
    - .planning/phases/05-rgpd-security-evidence-pack/05-CONTEXT.md §SEC-03 (line 24) — locked: synthesize from per-phase STRIDE + OWASP LLM Top 10 + existing tests.
  </read_first>
  <action>
    Create `docs/security/SEC-03-threat-coverage-matrix.md` (French domain doc) with two synthesized tables: (1) "OWASP LLM Top 10 (2025) → mitigation AC360 → preuve/test" covering at minimum LLM01 (prompt injection: contentModeration High, useModelKnowledge=false, validator gate → GUARDRAILS_VALIDATION.md / validate_copilot_yaml.py), LLM02 (sensitive info disclosure: safe_logger.redact + RedactingSpanProcessor + 4-field hashed audit trail → tests/security/test_no_plaintext_secrets.py + telemetry test), LLM03 (supply chain: Dependabot + pin policy → .github/dependabot.yml + SEC-04), LLM05 (improper output handling: FIC human-review draft + JSON-schema → schemas/audit_result.schema.json), LLM06 (excessive agency: read-only, OBO RBAC → test_audit_ownership/test_job_isolation), LLM07 (system prompt leakage: additionalInstructions red-teaming → GOVERNANCE.md §3); (2) a STRIDE coverage table synthesized from the per-phase `<threat_model>` registers (cite a few representative T-0x IDs from Phases 1-5 and the AC360 mitigation). Use the OWASP 2025 IDs (not 2023). Every row MUST have a mitigation AND an evidence/test reference. Cross-reference SEC-01 (diagram) and SEC-02 (authn tests).
  </action>
  <verify>
    <automated>powershell -Command "if ((Select-String -Path docs/security/SEC-03-threat-coverage-matrix.md -Pattern 'LLM01','LLM02','STRIDE','SEC-03' -SimpleMatch).Count -ge 4) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/security/SEC-03-threat-coverage-matrix.md` exists.
    - `grep -c "LLM0" docs/security/SEC-03-threat-coverage-matrix.md` returns at least 5 (≥5 OWASP LLM rows).
    - `grep "STRIDE" docs/security/SEC-03-threat-coverage-matrix.md` matches.
    - `grep -E "tests/|GUARDRAILS|validate_copilot|schemas/" docs/security/SEC-03-threat-coverage-matrix.md` matches (evidence references present).
    - `grep "SEC-03" docs/security/SEC-03-threat-coverage-matrix.md` matches.
  </acceptance_criteria>
  <done>SEC-03 has ≥5 OWASP LLM 2025 rows + a STRIDE table, each row carrying a mitigation and an evidence/test reference.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-04 dependency posture — document Dependabot + PyJWT/deltalake pin policy</name>
  <files>docs/security/SEC-04-dependency-posture.md, .github/dependabot.yml</files>
  <read_first>
    - .github/dependabot.yml (whole file) — it ALREADY EXISTS with pip (root + /azure_functions + /scripts) + github-actions. DOCUMENT it; do NOT recreate it. Optionally add a `groups` (security-only) tightening.
    - .planning/codebase/CONCERNS.md §"Dependencies at Risk" (lines 157-177) — PyJWT >= 2.8.0 (pin 2.9.0+ when available), python-Levenshtein, azure-functions-durable, deltalake >= 0.18.0 pin/migration policy to transcribe.
    - .planning/phases/05-rgpd-security-evidence-pack/05-RESEARCH.md §Finding 1 (lines 13) + §"Package Legitimacy Audit" (lines 62-64) — SEC-04 is mostly done (file exists); remaining work is the pin-policy doc + optional groups tightening; no packages installed this phase.
    - .planning/phases/05-rgpd-security-evidence-pack/05-CONTEXT.md §SEC-04 (line 25) — locked: document the existing dependabot.yml + PyJWT/deltalake pin policy.
  </read_first>
  <action>
    Create `docs/security/SEC-04-dependency-posture.md` (French domain doc) documenting: (1) Dependabot is ENABLED — describe the existing `.github/dependabot.yml` (pip ecosystems at `/`, `/azure_functions`, `/scripts` + github-actions, weekly, labelled dependencies/security); (2) the pin policy for the at-risk dependencies from CONCERNS.md — PyJWT (>= 2.8.0 now; pin 2.9.0+ when available/tested; JWT algorithm-confusion rationale), deltalake (>= 0.18.0; Rust bindings; DataFrame input validation), azure-functions-durable (>= 1.2.9; pin specific, test before update), python-Levenshtein (optional, graceful degradation); (3) the vuln-response posture (Dependabot security PRs labelled `security`, quarterly review per GOVERNANCE §3 rotation cadence). Optionally tighten `.github/dependabot.yml` by adding a `groups` block (e.g. a `security` group) for the pip ecosystems — keep all existing entries; if you edit it, it MUST remain valid YAML. NO `pip install` / package additions in this phase (RESEARCH: slopcheck no-op).
  </action>
  <verify>
    <automated>powershell -Command "if ((Select-String -Path docs/security/SEC-04-dependency-posture.md -Pattern 'dependabot','PyJWT','deltalake','SEC-04' -SimpleMatch).Count -ge 4) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/security/SEC-04-dependency-posture.md` exists.
    - `grep -i "dependabot" docs/security/SEC-04-dependency-posture.md` matches.
    - `grep "PyJWT" docs/security/SEC-04-dependency-posture.md` matches AND `grep "deltalake" docs/security/SEC-04-dependency-posture.md` matches.
    - `grep "dependabot.yml" docs/security/SEC-04-dependency-posture.md` matches (references the existing file, not a recreation).
    - `python -c "import yaml,sys; yaml.safe_load(open('.github/dependabot.yml')); print('ok')"` prints `ok` (dependabot.yml remains valid YAML if edited).
  </acceptance_criteria>
  <done>SEC-04 documents the existing Dependabot config and the PyJWT/deltalake/durable/Levenshtein pin policy; dependabot.yml (if tightened) stays valid YAML; no packages installed.</done>
</task>

<task type="auto">
  <name>Task 3: SEC-05 accepted-risk / known-issues register (classify CONCERNS.md)</name>
  <files>docs/security/SEC-05-accepted-risk-register.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (whole file) — every item across Tech Debt, Known Bugs, Security Considerations, Performance, Fragile Areas, Scaling Limits, Test Coverage Gaps to classify.
    - docs/security/SECURITY_POSTURE.md §1 "Table de disposition" (lines 26-45) and §8 "Items ouverts portés en avant" (lines 160-167) — the Phase 1 must-fix-done vs deferred dispositions to reuse as the spine.
    - .planning/phases/01-deep-code-audit-critical-fixes/deferred-items.md and .planning/phases/02-production-infrastructure-provisioning/deferred-items.md — the explicitly deferred items to cite as accepted-deferred with their rationale.
    - .planning/phases/05-rgpd-security-evidence-pack/05-CONTEXT.md §SEC-05 (line 26) — locked: classify CONCERNS.md items must-fix-done (Phase 1 fixes) vs accepted-deferred (ref Phase 1 deferred-items.md).
  </read_first>
  <action>
    Create `docs/security/SEC-05-accepted-risk-register.md` (French domain doc) with a register table classifying EVERY CONCERNS.md item into one of: `must-fix-done` (closed by Phase 1 fix — e.g. IDOR oid, OBO 503, redaction, audit-trail seam, single-instance pin), or `accepted-deferred` (non-launch-blocking — e.g. symlink path-traversal hardening, broad-except sweep, JWKS stale-while-revalidate, Fabric fallback/circuit-breaker, fuzzy O(n) index, in-memory scaling limits) with an explicit acceptance rationale and the deferral source (SECURITY_POSTURE §8 / deferred-items.md / OUT-OF-SCOPE in REQUIREMENTS). Each row: Item | Catégorie CONCERNS | Disposition (must-fix-done / accepted-deferred) | Justification | Source. Carry the honest "journal immuable = pas de WORM" caveat (SECURITY_POSTURE §7) as an accepted-risk row flagged for DPO confirmation. State that no accepted-deferred item is launch-blocking.
  </action>
  <verify>
    <automated>powershell -Command "if ((Select-String -Path docs/security/SEC-05-accepted-risk-register.md -Pattern 'must-fix-done','accepted-deferred','SEC-05' -SimpleMatch).Count -ge 3) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/security/SEC-05-accepted-risk-register.md` exists.
    - `grep -c "accepted-deferred" docs/security/SEC-05-accepted-risk-register.md` returns at least 3 (multiple classified rows).
    - `grep "must-fix-done" docs/security/SEC-05-accepted-risk-register.md` matches.
    - `grep -iE "IDOR|symlink|WORM" docs/security/SEC-05-accepted-risk-register.md` matches (specific items classified).
    - `grep "SEC-05" docs/security/SEC-05-accepted-risk-register.md` matches.
  </acceptance_criteria>
  <done>SEC-05 classifies every CONCERNS.md item must-fix-done vs accepted-deferred with rationale and source; none of the accepted items is launch-blocking; the no-WORM caveat is an accepted-risk row flagged for the DPO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/Copilot input → agent | Prompt-injection surface (OWASP LLM01) |
| Third-party dependencies → runtime | Supply-chain risk (OWASP LLM03) |
| Known-but-accepted weaknesses → production | Residual-risk surface the register makes explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | Prompt injection (LLM01) | mitigate (documented) | SEC-03 maps to contentModeration High + useModelKnowledge=false + validator gate; evidence GUARDRAILS_VALIDATION.md |
| T-05-13 | Tampering | Vulnerable/compromised dependency (LLM03 supply chain) | mitigate (documented) | SEC-04 documents Dependabot (pip + actions) + PyJWT/deltalake pin policy |
| T-05-14 | Repudiation | Undisclosed residual risk shipped silently | mitigate | SEC-05 classifies every CONCERNS.md item; accepted-deferred items carry explicit rationale + source; no-WORM caveat flagged for DPO |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; SEC-04 documents the existing pin policy only (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Structural grep asserts on SEC-03, SEC-04, SEC-05 pass.
- `.github/dependabot.yml` parses as valid YAML if edited.
- No `pip install` / package additions (slopcheck no-op for this phase).
</verification>

<success_criteria>
SEC-03/04/05 evidence docs exist: SEC-03 with ≥5 OWASP LLM 2025 rows + STRIDE, each with mitigation+evidence; SEC-04 documenting the existing Dependabot config + pin policy; SEC-05 classifying every CONCERNS.md item with no launch-blocking accepted item. No package installs.
</success_criteria>

<output>
Create `.planning/phases/05-rgpd-security-evidence-pack/05-04-SUMMARY.md` when done.
</output>
