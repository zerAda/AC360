---
phase: 01-deep-code-audit-critical-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/graph_obo.py
  - tests/backend/test_graph_obo.py
autonomous: false
requirements: [AUD-05]
must_haves:
  truths:
    - "Transient OBO failures (httpx timeout/connect, HTTP 429/503/504) are retried with bounded exponential backoff + jitter, max 3 attempts"
    - "Non-transient failures (4xx auth errors, ValueError config errors) are NOT retried and propagate immediately"
    - "On 429 the Graph Retry-After header is honored as the delay"
    - "On exhaustion the retrying wrapper raises so the caller can map it to HTTP 503 (not 502)"
  artifacts:
    - path: "scripts/graph_obo.py"
      provides: "acquire_obo_graph_token_retrying wrapper preserving the injectable http_post seam"
      contains: "def acquire_obo_graph_token_retrying"
  key_links:
    - from: "scripts/graph_obo.acquire_obo_graph_token_retrying"
      to: "scripts/graph_obo.acquire_obo_graph_token"
      via: "wraps the existing exchange, retry loop only"
      pattern: "acquire_obo_graph_token"
---

<objective>
Add bounded exponential-backoff retry to the OBO token exchange (AUD-05) as a NEW wrapper function that preserves the existing injectable http_post seam, so the inner exchange and its tests are untouched. Classify transient failures (httpx timeout/connect errors + HTTP 429/503/504), honor Graph Retry-After on 429, use full-jitter exponential backoff (3 attempts, ~0.5s base), and let exhaustion raise so the gateway (Plan 01-06) maps it to HTTP 503. graph_obo.py is a mypy-strict module — full type hints are mandatory.

Purpose: Fix the launch-blocking "transient OBO flake mis-surfaced as failure" defect and the wrong status code. Defining the retry wrapper here (Wave 1, no api_server.py dependency) lets Plan 01-06 wire it into the gateway and flip 502 -> 503.
Output: A new acquire_obo_graph_token_retrying() in graph_obo.py + extended transient/non-transient/Retry-After tests using the existing fake-http_post pattern.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@scripts/graph_obo.py
</context>

<artifacts_produced>
New symbols this plan creates:
- `scripts/graph_obo.py`: `acquire_obo_graph_token_retrying(user_assertion, *, attempts=3, base=0.5, ...)` (NEW) — bounded-backoff transient-only retry wrapper around the existing acquire_obo_graph_token.
- `scripts/graph_obo.py`: a transient-classification predicate (e.g. `_is_transient(exc) -> bool`, internal) — NEW.
- (optional, Claude's discretion) an injectable sleep callable parameter on the wrapper so tests run fast without real delays.
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 (checkpoint): Verify OBO delegated Graph scope list on the staging app registration</name>
  <action>PAUSE for human verification. Do not write code in this task — the human inspects the staging OBO app registration's consented delegated Graph scopes and either confirms the list or explicitly defers it to Phase 2 INF-06. Record the answer; either way Task 2 (the retry logic) proceeds since transient classification is scope-independent.</action>
  <what-built>Nothing yet — this is the carried open-verification gate from STATE.md/ROADMAP risk. The OBO retry LOGIC (transient classification) is scope-independent and proceeds in Task 2 regardless; this checkpoint records whether the exact delegated Graph scope list on the live staging app registration is confirmed, so the OBO SUCCESS-PATH assertions can be trusted before prod.</what-built>
  <how-to-verify>
    1. Open the Azure portal -> Entra ID -> App registrations -> the STAGING OBO confidential client used by AC360.
    2. Under "API permissions", confirm the delegated Microsoft Graph scopes actually consented (expected at least: SharePoint file read — Files.Read.All or Sites.Selected per function_app._download docstring — and Planner Tasks.ReadWrite).
    3. Confirm whether the default scope `https://graph.microsoft.com/.default` (graph_obo.py:14) resolves those consented scopes.
    4. Record the confirmed scope list (and any AADSTS65001 consent gap) for Phase 2 INF-06 and the security-posture doc (Plan 01-05).
  </how-to-verify>
  <resume-signal>Type "approved — scopes confirmed: <list>" OR "deferred — staging app registration not yet available; proceed with retry logic only, success-path scope trust deferred to Phase 2 INF-06". Either answer unblocks Task 2 (the retry logic does not depend on the scope list).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded-backoff transient-only OBO retry wrapper (AUD-05)</name>
  <files>scripts/graph_obo.py, tests/backend/test_graph_obo.py</files>
  <read_first>
    - scripts/graph_obo.py (full file — acquire_obo_graph_token lines 36-80 with its injectable http_post; the seam to preserve)
    - .planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md (§Code Examples "Bounded backoff with jitter, transient-only, Retry-After aware (AUD-05)" lines ~264-297; Pitfall 4 lines ~213-216; transient set {429,503,504}+timeout/connect)
    - .planning/phases/01-deep-code-audit-critical-fixes/01-PATTERNS.md (§"scripts/graph_obo.py — bounded backoff retry" + §"tests/backend/test_graph_obo.py (MODIFY)")
    - tests/backend/test_graph_obo.py (existing _FakeResp(payload,status) class lines ~13-24 + fake_post injection ~29-32 — extend, do not rewrite)
    - .planning/codebase/CONVENTIONS.md (graph_obo is mypy-strict: full type hints + keyword-only named args + return type required)
  </read_first>
  <behavior>
    - 429 then 200 (fake http_post sequence): wrapper retries once and returns the token; Retry-After header value (if present) is used as the delay.
    - 503 then 504 then 200: retried up to attempts=3, returns token on the third call.
    - HTTP 401/403 (non-transient): NOT retried — raises on the first occurrence.
    - ValueError (missing assertion/config): NOT retried — raises immediately.
    - All-transient for 3 attempts: raises the last transient exception (caller maps to 503).
    - Backoff uses full jitter (random.uniform(0, base*2**i)) when no Retry-After; tests inject a fake sleep so no real time passes.
  </behavior>
  <action>In scripts/graph_obo.py add a module-level transient set {429, 503, 504} and an internal _is_transient(exc) -> bool predicate that returns True for httpx.TimeoutException / httpx.ConnectError (and ConnectTimeout) and for exceptions whose response.status_code is in the transient set. Add acquire_obo_graph_token_retrying(user_assertion: str, *, attempts: int = 3, base: float = 0.5, ...passthrough kwargs..., sleep: Callable[[float], None] = time.sleep, http_post: Optional[Callable] = None, ...) that loops attempts times calling the existing acquire_obo_graph_token (forwarding http_post so the test fake still works); on a transient exc that is not the last attempt, compute delay = Retry-After (float of resp.headers["Retry-After"], only on 429) else random.uniform(0, base*2**i), call sleep(delay), and retry; on non-transient or last attempt, raise. Do NOT modify acquire_obo_graph_token itself. Use stdlib random/time only — NO new dependency (RESEARCH §Don't Hand-Roll). Full type hints (mypy strict). Extend tests/backend/test_graph_obo.py: add a sequence-returning fake http_post (or _FakeResp with a .headers dict) covering all six behavior cases above, injecting a no-op/recording fake sleep so the suite stays fast.</action>
  <verify>
    <automated>pytest tests/backend/test_graph_obo.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/graph_obo.py` defines acquire_obo_graph_token_retrying with full type hints and a transient predicate; acquire_obo_graph_token is unchanged.
    - Transient cases (429/503/504/timeout/connect) are retried up to 3 attempts; 4xx and ValueError are NOT retried (asserted in tests).
    - Retry-After honored on 429 (asserted with a fake .headers dict).
    - On exhaustion the wrapper RAISES (does not return None) so the caller maps to 503.
    - Tests inject a fake sleep — `pytest tests/backend/test_graph_obo.py -x` exits 0 in well under 60s.
    - `mypy scripts/graph_obo.py` reports no new errors (module is mypy-strict).
  </acceptance_criteria>
  <done>graph_obo exposes a fully typed bounded-backoff transient-only retry wrapper that honors Retry-After and raises on exhaustion; the inner exchange and its existing tests are untouched; new cases green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> Entra token endpoint | OBO POST to login.microsoftonline.com; transient flakiness must not surface as a hard failure or wrong status (AUD-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Denial of Service / availability | OBO token exchange | mitigate | Bounded backoff (3 attempts, full jitter, Retry-After) retries only transient {429,503,504,timeout,connect}; exhaustion raises -> 503 (wired in 01-06). |
| T-01-03-02 | Information disclosure | retry masking real config errors | mitigate | Non-transient 4xx/ValueError are NOT retried — a consent/scope error surfaces immediately (still redacted at the caller) rather than being masked as a 503 (Pitfall 4). |
| T-01-03-03 | Denial of Service | retry storm / thundering herd | mitigate | Full jitter on backoff prevents synchronized retries; attempts capped at 3 with ~0.5s base. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package install — stdlib random/time only (RESEARCH §Don't Hand-Roll). Legitimacy gate not triggered. |
</threat_model>

<verification>
- `pytest tests/backend/test_graph_obo.py -x` exits 0.
- `mypy scripts/graph_obo.py` clean (mypy-strict module).
- grep confirms no new third-party import in graph_obo.py (only stdlib random/time added).
</verification>

<success_criteria>
- Transient OBO failures retried with bounded backoff + jitter (max 3); 4xx/config errors not retried.
- Retry-After honored on 429; exhaustion raises (caller -> 503).
- New tests green; mypy clean; no new dependency.
- OBO scope-verification checkpoint resolved (confirmed or explicitly deferred to Phase 2 INF-06).
</success_criteria>

<output>
Create `.planning/phases/01-deep-code-audit-critical-fixes/01-03-SUMMARY.md` when done.
</output>
