---
phase: 01-deep-code-audit-critical-fixes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/azure_functions/test_jobs_dir_locality.py
  - tests/backend/test_audit_trail.py
autonomous: true
requirements: [AUD-07, AUD-08]
must_haves:
  truths:
    - "A failing test asserts the download->ocr stages share one JOBS_BASE_DIR/{doc_id} dir within a single run_audit call"
    - "A failing test asserts the orchestrator drives exactly one activity (no fan-out that crosses a file boundary)"
    - "A failing test asserts the audit-trail event carries exactly {user_id_hash, document_id, ts_utc, verdict} and no raw PII"
  artifacts:
    - path: "tests/azure_functions/test_jobs_dir_locality.py"
      provides: "AUD-08 locality + single-activity structural assertions"
      contains: "def test_"
    - path: "tests/backend/test_audit_trail.py"
      provides: "AUD-07 4-field contract + no-PII redaction assertions"
      contains: "def test_"
  key_links:
    - from: "tests/azure_functions/test_jobs_dir_locality.py"
      to: "audit_pipeline.run_audit / function_app._audit_orchestration"
      via: "DI fakes (AuditDeps callables) + structural import"
      pattern: "run_audit|_audit_orchestration"
    - from: "tests/backend/test_audit_trail.py"
      to: "scripts/audit_trail.emit_document_access"
      via: "import of the not-yet-built emit helper"
      pattern: "audit_trail|emit_document_access"
---

<objective>
Create the two Wave 0 test scaffolds the rest of the phase implements against: the JOBS_BASE_DIR locality test (AUD-08) and the audit-trail contract test (AUD-07). These tests MUST exist and MUST fail (RED) before any production code in later waves makes them pass. They lock the AUD-07 4-field event contract and the AUD-08 single-activity invariant as executable specifications.

Purpose: Satisfy the Nyquist rule — every later AUD-07/AUD-08 task verifies against a pre-existing automated test instead of "MISSING". Pins contracts before implementation so executors receive the spec, not a scavenger hunt.
Output: Two new pytest files using the repo's DI/fake patterns (no Durable runtime, no live SDK).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-PATTERNS.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-VALIDATION.md
</context>

<artifacts_produced>
New symbols/files this plan creates (none exist yet):
- `tests/azure_functions/test_jobs_dir_locality.py` (NEW test file) — AUD-08
- `tests/backend/test_audit_trail.py` (NEW test file) — AUD-07
These tests REFERENCE a not-yet-built symbol `scripts/audit_trail.emit_document_access(*, oid, document_id, verdict=None)`, which Plan 01-04 produces. Until then, the AUD-07 test is expected RED (import error / unmet contract) — that is the intended Wave 0 state.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create JOBS_BASE_DIR locality test scaffold (AUD-08)</name>
  <files>tests/azure_functions/test_jobs_dir_locality.py</files>
  <read_first>
    - .planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md (§Code Examples "Single-activity locality assertion test (AUD-08)", lines ~299-320 — verbatim starting point)
    - .planning/phases/01-deep-code-audit-critical-fixes/01-PATTERNS.md (§"tests/azure_functions/test_jobs_dir_locality.py (NEW)" — two-test assertion list)
    - azure_functions/shared/audit_pipeline.py (AuditDeps dataclass + run_audit signature — the DI shape to inject fakes into)
    - azure_functions/function_app.py:170-198 (_run_activity, _audit_orchestration — the single-activity structure to assert against; note context.call_activity is called exactly once)
    - tests/azure_functions/test_function_app.py and tests/azure_functions/test_audit_pipeline.py (existing conftest path bootstrap + fake-context style for this test dir)
  </read_first>
  <action>Create tests/azure_functions/test_jobs_dir_locality.py with two tests. Test 1 (locality): monkeypatch.setenv("JOBS_BASE_DIR", str(tmp_path)); inject fake download/ocr callables via AuditDeps into run_audit; record the directory each stage sees; assert download() and ocr() observe the same JOBS_BASE_DIR/{document_id} directory within one run_audit call. Test 2 (single-activity structural): drive function_app._audit_orchestration with a fake context exposing get_input() and a call_activity recorder; assert call_activity is invoked exactly once and only with the activity name "activity_run_audit" — guarding against a future fan-out that would pass a file path across an activity boundary (the AUD-08 anti-pattern in RESEARCH). Use only stdlib + pytest + the injected fakes; no azure.durable_functions import, no live SDK. Use the tests/azure_functions conftest path bootstrap (do not re-add sys.path manually if conftest already provides it).</action>
  <verify>
    <automated>pytest tests/azure_functions/test_jobs_dir_locality.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File tests/azure_functions/test_jobs_dir_locality.py exists and is collected by pytest (no collection error).
    - Contains at least two test functions: one asserting download/ocr share JOBS_BASE_DIR/{doc_id}, one asserting exactly one call_activity("activity_run_audit").
    - Tests run with NO azure.durable_functions / live SDK import (DI fakes only).
    - At Wave 0 this is RED or partially-RED until Plan 01-05 confirms/locks the invariant; collection MUST succeed (import errors are not acceptable).
  </acceptance_criteria>
  <done>tests/azure_functions/test_jobs_dir_locality.py collects cleanly and encodes both AUD-08 assertions; failing assertions (not import errors) are the acceptable RED state.</done>
</task>

<task type="auto">
  <name>Task 2: Create audit-trail contract test scaffold (AUD-07)</name>
  <files>tests/backend/test_audit_trail.py</files>
  <read_first>
    - .planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md (§Code Examples "App Insights custom event with custom dimensions (AUD-07)" lines ~241-262; locked 4-field contract {user_id_hash, document_id, ts_utc, verdict})
    - .planning/phases/01-deep-code-audit-critical-fixes/01-PATTERNS.md (§"tests/backend/test_audit_trail.py (NEW)" — assert list incl. user_id_hash == hash_id(oid), no raw oid/UPN/email/client-name leak)
    - tests/backend/test_safe_logger_redaction.py (FAKE_JWT/FAKE_EMAIL/FAKE_IBAN constants + MagicMock/patch style)
    - tests/backend/test_graph_obo.py:8 (sys.path.insert scripts bootstrap pattern for tests/backend)
    - scripts/feature_flags.py (hash_id signature — to assert user_id_hash == hash_id(oid))
  </read_first>
  <action>Create tests/backend/test_audit_trail.py asserting the AUD-07 contract against the not-yet-built scripts/audit_trail.emit_document_access(*, oid, document_id, verdict=None). Patch the APPINSIGHTS env gate so emit is active; capture the emitted dimensions (patch the underlying log_security or logger sink). Assert: (a) emitted event carries EXACTLY the keys user_id_hash, document_id, ts_utc, verdict — no extra free-form text fields; (b) user_id_hash == hash_id(oid) (SHA-256 of oid, no salt) and the raw oid is NOT present in any dimension; (c) feeding a poisoned PII value (use a FAKE_EMAIL-style constant as a client name or UPN) results in that value being redacted/absent in the emitted dimensions; (d) ts_utc is a UTC ISO-8601 string. Use the tests/backend sys.path bootstrap. No live Azure Monitor exporter — assert on the seam's pre-emit dimension dict. Mark the test so it does not crash collection if the module is absent at Wave 0 (e.g. import inside the test or pytest.importorskip is NOT allowed — instead let the import fail as a clear RED; the module lands in Plan 01-04).</action>
  <verify>
    <automated>pytest tests/backend/test_audit_trail.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File tests/backend/test_audit_trail.py exists.
    - Encodes the exact 4-field contract assertion (set of dimension keys == {user_id_hash, document_id, ts_utc, verdict}).
    - Encodes user_id_hash == hash_id(oid) and absence-of-raw-oid assertion.
    - Encodes a no-PII-leak assertion routing a poisoned value through the emit path.
    - At Wave 0 the test is RED (ModuleNotFoundError for scripts.audit_trail or unmet contract) — expected until Plan 01-04 ships the helper.
  </acceptance_criteria>
  <done>tests/backend/test_audit_trail.py exists and fully specifies the AUD-07 4-field + no-PII contract; RED state (missing audit_trail module) is the intended Wave 0 outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-event producer -> telemetry sink | The 4-field record crosses from app memory toward App Insights; must carry zero raw PII (AUD-07/06). |
| activity boundary (download -> downstream stage) | If a file path crosses an activity boundary, a stateless multi-VM activity may land on a different VM with an empty JOBS_BASE_DIR (AUD-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-W0-01 | Information disclosure | audit-trail event dimensions | mitigate | Test 2 asserts only the 4 contracted fields exist and a poisoned PII value is redacted/absent before emit. |
| T-01-W0-02 | Tampering / DoS | JOBS_BASE_DIR cross-activity file loss | mitigate | Test 1 + structural test assert exactly one activity and shared artifact dir; fan-out is blocked by the structural assertion. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan; slopcheck/legitimacy gate not triggered (tests use stdlib + pytest only). |
</threat_model>

<verification>
- `pytest tests/azure_functions/test_jobs_dir_locality.py tests/backend/test_audit_trail.py` collects with no import/collection errors for the locality test, and a clear RED (failing assertion or missing-module) signal that later waves resolve.
- No `azure.durable_functions` or live Azure SDK import in either file.
</verification>

<success_criteria>
- Both new test files exist and are committed.
- Locality test (AUD-08) collects cleanly; audit-trail test (AUD-07) defines the exact 4-field + no-PII contract.
- Tests run in well under 60s with DI fakes only.
</success_criteria>

<output>
Create `.planning/phases/01-deep-code-audit-critical-fixes/01-01-SUMMARY.md` when done.
</output>
