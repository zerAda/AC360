---
phase: 01-deep-code-audit-critical-fixes
plan: 07
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-04, 01-05, 01-06]
files_modified:
  - docs/security/SECURITY_POSTURE.md
autonomous: false
requirements: [AUD-01]
must_haves:
  truths:
    - "Every 'Addressed' mitigation from CONCERNS.md is re-checked under the N>1-instance threat model and its disposition recorded"
    - "The full pytest suite is green under the single-worker assumptions (no regressions from the phase's fixes)"
    - "A written security-posture document exists in docs/security/ recording the re-validation, owner_hash/oid IDOR closure, OBO 503 fix, redaction surface, the audit-trail field contract, and the single-instance-pin rationale"
    - "The 'immutable log' control is framed honestly (append-only ingestion + long retention + RBAC + workspace resource lock; NO table-level WORM)"
  artifacts:
    - path: "docs/security/SECURITY_POSTURE.md"
      provides: "Phase 1 security-posture deliverable feeding Phase 5 (RGPD & Security Evidence Pack)"
      min_lines: 60
      contains: "owner_hash"
  key_links:
    - from: "docs/security/SECURITY_POSTURE.md"
      to: ".planning/codebase/CONCERNS.md + the phase fixes"
      via: "re-validation table: each Addressed mitigation -> N>1 disposition"
      pattern: "single-instance|owner_hash|immutab"
---

<objective>
Close the phase with the AUD-01 re-validation: run the full test suite to confirm no regressions from the phase's fixes under the single-worker topology, and author the written security-posture document — an explicit Phase 1 deliverable that feeds Phase 5 (RGPD & Security Evidence Pack). The document records each "Addressed" mitigation re-checked under the N>1-instance threat model and why the single-instance pin closes the in-memory-state threats, the owner_hash/oid IDOR analysis, the audit-trail field contract, the OBO 503 fix, and the redaction posture — framing "immutability" honestly (no Log Analytics WORM).

Purpose: AUD-01 is the audit/re-validation requirement and the security-posture doc is the mandatory hand-off artifact to Phase 5. Placing it last means it documents the ACTUAL landed state of all fixes, not intentions.
Output: docs/security/SECURITY_POSTURE.md + a green full-suite re-validation run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-PATTERNS.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-02-SUMMARY.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-03-SUMMARY.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-04-SUMMARY.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-05-SUMMARY.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-06-SUMMARY.md
</context>

<artifacts_produced>
New symbols/files this plan creates:
- `docs/security/SECURITY_POSTURE.md` (NEW document) — the Phase 1 security-posture deliverable feeding Phase 5.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: AUD-01 full-suite re-validation under the single-worker topology</name>
  <files>(no source files modified — regression run only)</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (the authoritative bug/tech-debt inventory; identify each "Addressed"/"Current mitigation" item and the launch-blocking subset)
    - .planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md (§"Re-validation under single-instance topology"; §Common Pitfalls; §Security Domain known-threat table lines ~419-427)
    - .planning/phases/01-deep-code-audit-critical-fixes/01-VALIDATION.md (Per-Task Verification Map; AUD-01 regression command; Sampling Rate phase gate)
    - all five prior SUMMARYs (01-02..01-06) for what actually landed
  </read_first>
  <action>Run the full regression suite `pytest tests/backend tests/security tests/azure_functions` and confirm green — this is the AUD-01 re-validation that the existing IDOR, rate-limit, path-traversal, and isolation guards still hold after the phase's fixes, under the single-worker assumptions documented in Plan 01-05. For each "Addressed" mitigation / "Current mitigation" entry in CONCERNS.md, record its disposition under the N>1-instance threat model into a working list (carried into Task 2's document): (a) closed-by-fix-this-phase (owner_hash/oid IDOR, OBO 502->503, error/telemetry redaction, audit-trail emit), (b) closed-by-single-instance-pin (rate-limit store thread-safety, JWKS cache race — pinned, not lock-rewritten, per locked decision), or (c) explicitly deferred non-launch-blocking debt (symlink path-traversal hardening, Fabric fallback, fuzzy-match indexing, broad except sweep outside hot paths). Do NOT fix deferred items — only classify them. If any regression appears, stop and report it as a gap rather than papering over it.</action>
  <verify>
    <automated>pytest tests/backend tests/security tests/azure_functions</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/backend tests/security tests/azure_functions` exits 0 (full green; no regressions from the phase fixes).
    - Each CONCERNS.md "Addressed"/"Current mitigation" item has a recorded disposition: closed-by-fix, closed-by-pin, or deferred — captured for the posture document.
    - No deferred (out-of-scope) item was modified.
  </acceptance_criteria>
  <done>The full suite is green under the single-worker topology and every Addressed mitigation has an N>1 disposition ready to document.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/security/SECURITY_POSTURE.md (AUD-01 deliverable)</name>
  <files>docs/security/SECURITY_POSTURE.md</files>
  <read_first>
    - docs/security/SECURITY_AUDIT_STAGING.md (existing doc in the same docs/security/ register — match the location and the French security-doc style)
    - .planning/phases/01-deep-code-audit-critical-fixes/01-PATTERNS.md (§"docs/security/SECURITY_POSTURE.md (NEW)" — required coverage list + honest immutability framing)
    - .planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md (Pitfall 3 lines ~208-211 + A2 lines ~338 — Log Analytics has NO WORM; describe append-only + retention + RBAC + resource lock honestly; §Security Domain ASVS table + known-threat table)
    - .planning/codebase/CONCERNS.md (the re-validation source list) and the disposition list produced in Task 1
    - all five prior SUMMARYs (01-02..01-06) for the landed-state facts to cite
  </read_first>
  <action>Create docs/security/SECURITY_POSTURE.md (structure at Claude's discretion, consistent with the existing docs/security/ French security-doc style). It MUST cover: (1) AUD-01 re-validation results — a table of each CONCERNS.md "Addressed"/"Current mitigation" item with its N>1-instance disposition (closed-by-fix / closed-by-single-instance-pin / deferred-non-launch-blocking), explaining for each pinned item WHY single-instance closes it (rate-limit bypass, JWKS divergence). (2) The single-instance pin as load-bearing for in-memory state — reference infra/main.bicep capacity=1 + gunicorn --workers 1 (Plan 01-05). (3) The owner_hash/oid IDOR analysis — why oid (immutable per-tenant GUID) closes the UPN-reuse bug, and that the durable owner_hash check is the authoritative gate (in-memory map cache only). (4) The OBO posture — bounded-backoff transient-only retry, 503-not-502 on exhaustion, non-transient errors not retried. (5) The redaction surface — single audited safe_logger.redact() applied to HTTPException detail + telemetry dimensions + the audit-event dims. (6) The audit-trail field contract — exactly {user_id_hash, document_id, ts_utc, verdict}, env-gated emit seam, exporter wired in Phase 3 (OBS-01). (7) The "immutable log" control framed HONESTLY: append-only ingestion (no in-place edit API) + long Log Analytics retention + RBAC + workspace resource lock — explicitly NOT table-level WORM; note the workspace retention/lock is Phase 2/3 infra and that the WORM-vs-resource-lock evidence bar must be confirmed with the DPO in Phase 5 (carry assumption A2). (8) A short note carrying the open OBO delegated Graph scope verification item (resolved or deferred in Plan 01-03) forward to Phase 2 INF-06. State clearly that this document feeds Phase 5 (RGPD & Security Evidence Pack: SEC-01..SEC-05).</action>
  <verify>
    <automated>python -c "import pathlib,sys; p=pathlib.Path('docs/security/SECURITY_POSTURE.md'); t=p.read_text(encoding='utf-8'); needed=['owner_hash','oid','single-instance','503','redact','user_id_hash','RBAC']; missing=[k for k in needed if k.lower() not in t.lower()]; print('MISSING',missing); sys.exit(1 if missing or len(t.splitlines())<60 else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/security/SECURITY_POSTURE.md exists, >= 60 lines.
    - Contains the eight required coverage areas, including a re-validation disposition table sourced from CONCERNS.md.
    - "Immutability" is framed honestly (append-only + retention + RBAC + resource lock; explicitly no Log Analytics WORM).
    - References the single-instance pin, owner_hash/oid IDOR closure, OBO 503 fix, redaction surface, and the 4-field audit contract.
    - States it feeds Phase 5 and carries the OBO-scope open item to Phase 2 INF-06.
    - The automated keyword/length check exits 0.
  </acceptance_criteria>
  <done>A complete, honest security-posture document exists in docs/security/, re-validating every Addressed mitigation under the N>1 model and recording the phase's IDOR/OBO/redaction/audit-trail posture for the Phase 5 evidence pack.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Human sign-off that the security-posture doc reflects the landed code</name>
  <action>PAUSE for human verification. Do not modify files in this task — the human reviews docs/security/SECURITY_POSTURE.md against the landed code (per the how-to-verify steps) and approves it as Phase 5 evidence or requests corrections.</action>
  <what-built>The full phase is landed: oid-based IDOR closure, OBO bounded-backoff + 503, error/telemetry redaction, audit-trail emit seam, single-instance Bicep pin, and the written security-posture document. This checkpoint is the human sign-off that the security-posture doc accurately reflects the landed code before it is relied upon as Phase 5 evidence.</what-built>
  <how-to-verify>
    1. Open docs/security/SECURITY_POSTURE.md and confirm the AUD-01 re-validation table matches reality: spot-check 2-3 CONCERNS.md "Addressed" items against their recorded disposition.
    2. Confirm the "immutable log" section does NOT claim Log Analytics WORM (it should describe append-only + retention + RBAC + resource lock).
    3. Confirm the owner_hash section states oid (not UPN) and that the durable check is authoritative.
    4. Run `pytest` once and confirm the full suite is green.
    5. Confirm the OBO delegated Graph scope open item is recorded as carried to Phase 2 INF-06 (with whatever was resolved in Plan 01-03).
  </how-to-verify>
  <resume-signal>Type "approved" to accept the security-posture document as Phase 5 input, or describe required corrections.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo code -> production topology (N>1 assumption) | Mitigations validated in dev under one process must be re-checked against the documented single-instance prod pin (AUD-01). |
| Phase 1 evidence -> Phase 5 compliance pack | The security-posture doc is a hand-off artifact; overstated controls (e.g. false WORM claim) would mislead the RGPD evidence pack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Repudiation / Info disclosure | mis-stated security control in evidence doc | mitigate | Honest framing mandated: no Log Analytics WORM claim; append-only + retention + RBAC + resource lock; human checkpoint verifies doc matches landed code. |
| T-01-07-02 | Tampering | regression silently reintroducing a closed threat | mitigate | Full `pytest` gate re-validates IDOR/rate-limit/path-traversal/isolation guards; any regression is reported as a gap, not papered over. |
| T-01-07-03 | Information disclosure | deferred non-launch-blocking debt unrecorded | accept | Deferred items (symlink hardening, Fabric fallback, fuzzy index, broad-except sweep) are classified and recorded as accepted/deferred risk for the Phase 5 known-issues register (SEC-05), not fixed this phase. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan; documentation + regression run only. Legitimacy gate not triggered. |
</threat_model>

<verification>
- `pytest tests/backend tests/security tests/azure_functions` exits 0 (and a full `pytest` at the checkpoint).
- docs/security/SECURITY_POSTURE.md exists, >= 60 lines, passes the keyword check, and is human-approved.
</verification>

<success_criteria>
- Full suite green under the single-worker topology (AUD-01 re-validation).
- Security-posture document complete, honest, and human-approved as Phase 5 input.
- Every Addressed mitigation has a recorded N>1 disposition; deferred debt classified, not fixed.
</success_criteria>

<output>
Create `.planning/phases/01-deep-code-audit-critical-fixes/01-07-SUMMARY.md` when done.
</output>
