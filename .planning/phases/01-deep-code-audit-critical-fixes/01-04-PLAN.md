---
phase: 01-deep-code-audit-critical-fixes
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - scripts/audit_trail.py
  - tests/backend/test_audit_trail.py
autonomous: true
requirements: [AUD-07]
must_haves:
  truths:
    - "A document-access audit event carries EXACTLY {user_id_hash, document_id, ts_utc, verdict} and no raw PII"
    - "user_id_hash == hash_id(oid) (SHA-256 of oid, no salt); the raw oid never appears in a dimension"
    - "Emit is inert unless the existing APPINSIGHTS env gate is set (seam only; real exporter is Phase 3 OBS-01)"
    - "All dimension values are routed through the safe_logger dict-value redaction helper before emit"
  artifacts:
    - path: "scripts/audit_trail.py"
      provides: "emit_document_access emit seam with the locked 4-field contract"
      exports: ["emit_document_access"]
      contains: "def emit_document_access"
  key_links:
    - from: "scripts/audit_trail.emit_document_access"
      to: "scripts/feature_flags.hash_id + scripts/safe_logger redaction helper"
      via: "hash_id(oid) for user_id_hash; redact dimensions before emit; env-gated"
      pattern: "hash_id|redact"
---

<objective>
Build the AUD-07 document-access audit-trail emit seam as a new module scripts/audit_trail.py that makes the Wave 0 contract test (Plan 01-01 Task 2) pass. It emits exactly the four locked fields {user_id_hash, document_id, ts_utc, verdict}, computes user_id_hash via hash_id(oid) (no raw PII), routes all dimension values through the safe_logger dict-value redaction helper (from Plan 01-02), and stays inert behind the existing APPINSIGHTS env gate so Phase 3 (OBS-01) attaches the real configure_azure_monitor exporter without touching call sites.

Purpose: Satisfy the locked decision "this phase wires the emit path and field contract" without adding a new pip dependency in an audit-only phase (the azure-monitor-opentelemetry exporter lands in Phase 3). Turns the RED AUD-07 test GREEN.
Output: scripts/audit_trail.py (new emit seam) + the AUD-07 test passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md
@.planning/phases/01-deep-code-audit-critical-fixes/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@scripts/usage_tracker.py
@scripts/feature_flags.py
@.planning/phases/01-deep-code-audit-critical-fixes/01-02-SUMMARY.md
</context>

<artifacts_produced>
New symbols this plan creates:
- `scripts/audit_trail.py` (NEW module) — the AUD-07 emit seam.
- `scripts/audit_trail.emit_document_access(*, oid: str, document_id: str, verdict: str | None = None) -> None` (NEW public function) — computes hash_id(oid), stamps UTC ISO-8601 ts, redacts dimensions, emits behind the APPINSIGHTS gate.
- `__all__` declaring the public API of the new module.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/audit_trail.py emit seam (AUD-07)</name>
  <files>scripts/audit_trail.py, tests/backend/test_audit_trail.py</files>
  <read_first>
    - .planning/phases/01-deep-code-audit-critical-fixes/01-RESEARCH.md (§Code Examples "App Insights custom event with custom dimensions (AUD-07)" lines ~241-262; Open Q2 lines ~349-352 emit-seam-only this phase; State-of-the-Art note lines ~330-331 that the legacy AppInsights line is NOT a real exporter)
    - .planning/phases/01-deep-code-audit-critical-fixes/01-PATTERNS.md (§"scripts/audit_trail.py (NEW)" + §Shared Patterns "Env-gated emit" + "Deterministic ownership hash")
    - scripts/usage_tracker.py (build_usage_event keyword-only event-builder shape + track() — the analog for the seam)
    - scripts/feature_flags.py (hash_id — SHA-256 of oid, no salt; the exact hash to reuse)
    - scripts/safe_logger.py (the dict-value redaction helper added in Plan 01-02; reuse it — do NOT add new redaction logic)
    - tests/backend/test_audit_trail.py (the Wave 0 contract test this module must satisfy)
    - scripts/api_server.py:84 (existing APPINSIGHTS_INSTRUMENTATIONKEY emit gate — match this env-gate pattern)
  </read_first>
  <behavior>
    - emit_document_access(oid=..., document_id=..., verdict="CONFORME") with APPINSIGHTS gate set -> emits dimensions whose key set is EXACTLY {user_id_hash, document_id, ts_utc, verdict}.
    - user_id_hash == hash_id(oid); raw oid is absent from every dimension.
    - With the APPINSIGHTS gate UNSET -> no emit occurs (inert seam).
    - verdict omitted -> verdict dimension is "" (empty string), not missing.
    - A poisoned PII value passed anywhere -> redacted/absent in emitted dimensions (dimensions routed through the safe_logger redaction helper).
    - ts_utc is a UTC ISO-8601 string (datetime.now(timezone.utc).isoformat()).
  </behavior>
  <action>Create scripts/audit_trail.py with a module docstring (purpose + design intent, French-allowed for domain) and from __future__ import annotations. Define emit_document_access(*, oid: str, document_id: str, verdict: str | None = None) -> None following the keyword-only convention. Build the dimension dict with exactly the four contracted keys: user_id_hash = hash_id(oid) (import from feature_flags), document_id, ts_utc = datetime.now(timezone.utc).isoformat(), verdict = verdict or "". Route the dimension dict through the safe_logger dict-value redaction helper (from Plan 01-02) before emit. Emit ONLY when the APPINSIGHTS_INSTRUMENTATIONKEY (or connection-string) env var is set — mirror the existing gate at api_server.py:84 — by logging the custom event via log_security/logger with the microsoft.custom_event.name="ac360_document_access" dimension shape from the RESEARCH example; do NOT call configure_azure_monitor and do NOT add azure-monitor-opentelemetry (Open Q2: seam only, exporter in Phase 3). Declare __all__. Verify/complete tests/backend/test_audit_trail.py so the four behavior cases pass (the file was scaffolded RED in Plan 01-01).</action>
  <verify>
    <automated>pytest tests/backend/test_audit_trail.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/audit_trail.py` exists, exports emit_document_access via __all__, with from __future__ import annotations.
    - Emitted dimension key set is EXACTLY {user_id_hash, document_id, ts_utc, verdict} (asserted).
    - user_id_hash == hash_id(oid) and raw oid absent from dimensions (asserted).
    - No emit when the APPINSIGHTS gate is unset (asserted).
    - Dimensions routed through the safe_logger redaction helper — no per-call-site regex in audit_trail.py.
    - NO `azure-monitor-opentelemetry` / configure_azure_monitor reference; no new entry added to requirements.txt.
    - `pytest tests/backend/test_audit_trail.py -x` exits 0 (was RED in Wave 0).
  </acceptance_criteria>
  <done>scripts/audit_trail.py emits the locked 4-field, PII-free, env-gated document-access event reusing hash_id and the safe_logger redaction helper, with no new dependency; the AUD-07 contract test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app memory -> audit/telemetry sink | The access record crosses toward App Insights/Log Analytics; it must carry only the 4 contracted, PII-free fields (AUD-07/06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Repudiation / audit completeness | document-access trail | mitigate | emit_document_access writes a deterministic per-access record (hash, doc id, UTC ts, verdict); the immutable-retention sink is Phase 2/3 infra (documented honestly in the posture doc, Plan 01-07). |
| T-01-04-02 | Information disclosure | event dimensions | mitigate | Exactly 4 fields, no free-form text; user_id_hash is a one-way SHA-256 of oid; all values pass the safe_logger redaction helper before emit. |
| T-01-04-03 | Tampering (supply chain) | new telemetry dependency | accept | No azure-monitor-opentelemetry installed this phase (seam only); exporter + its legitimacy gate deferred to Phase 3 OBS-01 per Open Q2. Avoids a new dep in an audit-only phase. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan; the [ASSUMED] azure-monitor-opentelemetry package is deferred to Phase 3 (its checkpoint:human-verify legitimacy gate fires there). |
</threat_model>

<verification>
- `pytest tests/backend/test_audit_trail.py -x` exits 0.
- grep confirms scripts/audit_trail.py contains no `azure.monitor` / `configure_azure_monitor` import and no new redaction regex (reuses safe_logger helper).
- `requirements.txt` (root + azure_functions/) unchanged by this plan.
</verification>

<success_criteria>
- Document-access emit seam ships with the exact 4-field PII-free contract, env-gated, redaction-routed, no new dependency.
- AUD-07 contract test GREEN.
</success_criteria>

<output>
Create `.planning/phases/01-deep-code-audit-critical-fixes/01-04-SUMMARY.md` when done.
</output>
