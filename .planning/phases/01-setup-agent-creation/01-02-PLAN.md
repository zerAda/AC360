---
phase: 01
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - CFA-01
  - CFA-02
  - CFA-03
  - CFA-04
must_haves:
  truths:
    - "SharePoint site connected as knowledge source"
    - "Agent can read documents from SharePoint folders"
    - "Permissions are respected (CFA-03, CFA-04)"
  artifacts:
    - path: "SharePoint Knowledge Source"
      provides: "Connected SharePoint site in agent"
      status: "Configured in Copilot Studio"
  key_links:
    - from: "Copilot Studio Agent (Plan 01)"
      to: "SharePoint"
      via: "Knowledge source configuration"
      pattern: "Add SharePoint URL to knowledge sources"
---

<objective>
Connect SharePoint as knowledge source to Copilot Studio agent.

Purpose: Enable the agent to access and search client folder documents.

Output: Agent connected to SharePoint with sample client folders.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/phases/01-setup-agent-creation/01-RESEARCH.md
@.planning/phases/01-setup-agent-creation/01-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>task 1: Add SharePoint as Knowledge Source</name>
  <files>N/A - Copilot Studio portal</files>
  <action>
Connect SharePoint site to agent:

1. In Copilot Studio agent, go to "Knowledge" tab
2. Click "Add knowledge source"
3. Select "SharePoint" connector
4. Enter SharePoint site URL:
   - Use test site URL: `{your-sharepoint-site-url}`
   - Example: `https://contoso.sharepoint.com/sites/ClientFolders`
5. Configure:
   - Max URLs: 4 per agent (7MB each without M365 Copilot)
   - Select specific client folders to include
6. Save configuration
7. Wait for indexing (may take time based on SharePoint search)

**Note:** Ensure SharePoint site has sample client folders for testing.
  </action>
  <verify>
  <automated>Manual - Check knowledge source status in Copilot Studio</automated>
  </verify>
  <done>SharePoint connected, indexing status shows ready or in-progress</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>task 2: Verify Permissions Model</name>
  <files>N/A - SharePoint permissions</files>
  <action>
Verify that agent respects SharePoint permissions:

1. Identify test user with known SharePoint permissions
2. Confirm SharePoint site has varied permissions:
   - At least one folder accessible to test user
   - At least one folder NOT accessible to test user
3. In Copilot Studio, verify:
   - Agent uses "Run as" behavior inheriting user's permissions
   - No override of SharePoint permission model

**This satisfies CFA-03 (respect permissions) and CFA-04 (filter by access).**
  </action>
  <verify>
  <automated>Manual - Test with different user accounts in Teams</automated>
  </verify>
  <done>Agent only returns content the requesting user has access to</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>task 3: Test Content Retrieval</name>
  <files>N/A - Teams chat</files>
  <action>
Test that agent can retrieve content from SharePoint:

1. Open Microsoft Teams
2. Find the agent (will be available after Plan 03 publishes)
3. Send test query: "What documents are in the [client name] folder?"
4. Verify agent returns document list from SharePoint

**Prerequisite:** Plan 03 must be completed first for Teams channel.
If testing before Plan 03, use Copilot Studio's "Test" pane.
  </action>
  <verify>
  <automated>Manual - Check agent responds with content from SharePoint</automated>
  </verify>
  <done>Agent successfully retrieves and displays SharePoint document information</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Agent → SharePoint | User query triggers SharePoint access via agent |
| SharePoint → Agent → User | Document content flows through agent to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | SharePoint Permissions | mitigate | Agent inherits user's SharePoint permissions - CFA-03/04 satisfied by SharePoint model |
| T-01-05 | Elevation of Privilege | Knowledge Source | mitigate | Agent only accesses specified SharePoint URLs, not entire tenant |
| T-01-06 | Resource Exhaustion | Large SharePoint Sites | accept | Limit to 4 URLs, specific folders - controlled by configuration |
</threat_model>

<verification>
- [ ] SharePoint URL added to knowledge sources
- [ ] Indexing status shows ready or in-progress
- [ ] Test with restricted user shows permission filtering works
- [ ] Agent can retrieve document information
</verification>

<success_criteria>
- SharePoint site connected to agent
- Agent respects SharePoint user permissions (CFA-03)
- Agent filters content by user's access rights (CFA-04)
- Agent can read documents from SharePoint (CFA-02)
- Test query returns document information
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-agent-creation/01-02-SUMMARY.md`
</output>