---
phase: 02-production-infrastructure-provisioning
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/provision.ps1
autonomous: true
requirements: [INF-01]
must_haves:
  truths:
    - "A single orchestrator script runs blocking pre-flight gates (az login, correct subscription, RP registration, Flex+DocIntel region availability) before any mutating action, and fails closed if any gate fails"
    - "The orchestrator sequences provisioning in dependency-correct order and defaults to what-if (no live apply this phase)"
    - "Residency, admin-consent, KV-public-access flip, and live apply are surfaced as explicit operator checkpoints, not silently executed"
  artifacts:
    - path: "scripts/provision.ps1"
      provides: "Dependency-ordered provisioning orchestrator with fail-closed pre-flight gates"
      min_lines: 90
  key_links:
    - from: "scripts/provision.ps1"
      to: "infra/main.bicep + infra/prod.parameters.json"
      via: "az deployment group what-if -f infra/main.bicep -p @infra/prod.parameters.json"
      pattern: "az deployment group what-if"
    - from: "scripts/provision.ps1"
      to: "scripts/provision_app_registrations.ps1"
      via: "dot-source / invocation in dependency order"
      pattern: "provision_app_registrations"
---

<objective>
Create `scripts/provision.ps1`: the dependency-correct provisioning orchestrator with blocking, fail-closed pre-flight gates. It is the single entry point an operator runs; it asserts login/subscription/RP-registration/region-availability, then sequences RG → app-regs → what-if → (operator apply) → secret→KV + admin-consent → KV-public-access flip → verify. It defaults to what-if only — nothing live is applied this phase.

Purpose: INF-01 (EU region + explicit locations) is enforced here as a blocking residency/region pre-flight; the orchestrator encodes the locked dependency order and the operator-checkpoint boundary so the artifacts (Bicep + app-reg script) are run correctly and safely.
Output: `scripts/provision.ps1`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-production-infrastructure-provisioning/02-RESEARCH.md
@.planning/phases/02-production-infrastructure-provisioning/02-PATTERNS.md
@scripts/deploy_azure_ocr.ps1
@scripts/package_release.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/provision.ps1 — orchestrator + blocking pre-flight gates</name>
  <files>scripts/provision.ps1</files>
  <read_first>
    - `scripts/deploy_azure_ocr.ps1` lines 23-36 (az presence + `az account show`/`az login` guard — copy verbatim).
    - `scripts/package_release.ps1` lines 11-16 (`param(...)` + `$ErrorActionPreference = "Stop"`) and lines 82-93 (`-DryRun`/what-if default, collect → `exit 1` before any mutating action).
    - RESEARCH.md §Code Examples lines 572-599 (the ready-made `provision.ps1` pre-flight + dependency-ordered sequence). PATTERNS.md lines 113-126 (composite analog: pre-flight gate + interleaved steps + fail-closed gate).
    - CONTEXT.md `<decisions>` "Provisioning Execution Model" + `<specifics>` (blocking pre-flight: az login + subscription; EU residency M365 geo / Fabric region / DocIntel S0; admin-consent success). RESEARCH.md Pitfalls 2 (KV flip ordering), 6 (region availability).
  </read_first>
  <action>
    Write `scripts/provision.ps1` with French comment-help; `param([string]$ResourceGroup = 'rg-ac360-prod', [string]$Location = 'francecentral', [string]$ExpectedSubscription, [string]$BicepFile = 'infra/main.bicep', [string]$ParamFile = 'infra/prod.parameters.json', [switch]$WhatIfOnly = $true)`; `$ErrorActionPreference = "Stop"`; the `Write-Host -ForegroundColor Cyan/Green/Red/Yellow` status convention. BLOCKING PRE-FLIGHT (fail-fast, nothing mutates until all pass): copy the az-presence + login guard; `$sub = az account show --query id -o tsv`; if `$ExpectedSubscription` provided and `$sub -ne $ExpectedSubscription` then red `throw "Wrong subscription: $sub"`; `az provider register -n Microsoft.App` and `-n Microsoft.KeyVault` (Flex VNet integration + KV prereqs, RESEARCH Pitfall 4); Flex region availability: `$flex = az functionapp list-flexconsumption-locations --query "[?name=='$($Location.Replace(\" \",\"\"))'] | length(@)" -o tsv`; if `$flex -eq '0'` then red `throw 'Flex not available in <Location> — escalate (no EU Flex fallback chosen).'`; DocIntel S0 region probe with a yellow OPERATOR-CHECKPOINT note: if S0 absent in France Central, instruct setting `docIntelLocation=westeurope` (locked fallback) — do NOT silently substitute; EU residency (M365 tenant geo / Fabric capacity region / Power Platform env region) printed as an explicit yellow OPERATOR CHECKPOINT requiring manual confirmation before continuing. DEPENDENCY-CORRECT SEQUENCE (each step a colored section header; mutating steps guarded behind `if (-not $WhatIfOnly)`): (1) `az group create -n $ResourceGroup -l $Location`; (2) invoke `scripts/provision_app_registrations.ps1` to create apps + request scopes (note KV must exist before the secret-set step, so split: create apps now, set secret after deploy); (3) ALWAYS-RUN `az deployment group what-if -g $ResourceGroup -f $BicepFile -p "@$ParamFile"` and print "OPERATOR: review the what-if diff above and attach as evidence"; (4) OPERATOR CHECKPOINT — `az deployment group create` (only when `-not $WhatIfOnly`); (5) set OBO secret into KV (re-invoke the app-reg script's secret-set path now that KV exists) + `az ad app permission admin-consent` (operator); (6) SECOND-PASS OPERATOR CHECKPOINT — flip KV `publicNetworkAccess=Disabled` ONLY after VNet integration + PE confirmed (RESEARCH Pitfall 2; staging README warns of this exact failure mode); (7) verify — `az ad app permission list-grants`, MI role assignments present, a KV reference resolves. Default behavior (`-WhatIfOnly`) must perform NO mutating `az` call beyond `what-if`/region probes; live apply, consent, residency, and the KV flip are operator checkpoints.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "[System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/provision.ps1), [ref]$null, [ref]$null) | Out-Null; $t=Get-Content scripts/provision.ps1 -Raw; if ($t -match 'az deployment group what-if' -and $t -match 'list-flexconsumption-locations' -and $t -match 'WhatIfOnly' -and $t -match 'Microsoft.App') { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - Script parses without syntax errors (PowerShell AST parse succeeds).
    - Pre-flight asserts login (`az account show`), optional subscription match, RP registration (`az provider register -n Microsoft.App` and `-n Microsoft.KeyVault`), and Flex region availability (`az functionapp list-flexconsumption-locations`) — fail-closed (`throw`) on any failure.
    - Default `-WhatIfOnly` mode runs `az deployment group what-if` and performs NO mutating apply; `az deployment group create`, admin-consent, the KV-public-access flip, and residency confirmation are gated behind `-not $WhatIfOnly` / explicit OPERATOR CHECKPOINT messages.
    - The KV `publicNetworkAccess=Disabled` flip is sequenced AFTER VNet integration + PE (RESEARCH Pitfall 2).
  </acceptance_criteria>
  <done>provision.ps1 encodes the blocking pre-flight gates and the locked dependency order, defaults to what-if, and surfaces every live action as an operator checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Azure subscription | Orchestrator asserts the correct subscription/login before any mutation |
| repo → live Azure resources | All mutating `az` calls gated behind `-not $WhatIfOnly`; default is dry-run only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering / wrong-target deploy | provision.ps1 subscription targeting | mitigate | Pre-flight asserts `az account show` and an optional `$ExpectedSubscription` match; `throw` (fail-closed) on mismatch before any `az group create`/deploy |
| T-02-09 | DoS / availability | wrong-region provisioning | mitigate | `az functionapp list-flexconsumption-locations` gate + DocIntel S0 probe; West Europe DocIntel fallback surfaced; Flex-absent escalation throw — no silent region substitution (INF-01) |
| T-02-10 | Information disclosure | KV references break / public KV window | mitigate | KV `publicNetworkAccess=Disabled` flip ordered strictly AFTER PE + VNet integration (RESEARCH Pitfall 2); a what-if review checkpoint precedes the flip |
| T-02-11 | Compliance | data residency violation | mitigate | EU residency (M365 geo / Fabric region / Power Platform env) surfaced as a blocking OPERATOR CHECKPOINT requiring manual confirmation (INF-01, RGP-06 precursor) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase (RESEARCH line 142) — no supply-chain surface |
</threat_model>

<verification>
- PowerShell AST parse of `scripts/provision.ps1` succeeds.
- Offline grep: `az deployment group what-if`, `list-flexconsumption-locations`, `WhatIfOnly`, `Microsoft.App` all present.
- No mutating apply runs in default `-WhatIfOnly` mode.
</verification>

<success_criteria>
- INF-01: blocking residency/region pre-flight encoded; explicit EU locations enforced before provisioning.
- Dependency-correct sequence with the KV-flip ordered after PE/VNet integration.
- Live apply/consent/residency are operator checkpoints; default is what-if only.
</success_criteria>

<artifacts_this_phase_produces>
**New file (this plan):**
- `scripts/provision.ps1` — orchestrator + blocking pre-flight gates + dependency-ordered sequence (what-if default).

**New parameters/switches:** `-ResourceGroup`, `-Location`, `-ExpectedSubscription`, `-BicepFile`, `-ParamFile`, `-WhatIfOnly`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-production-infrastructure-provisioning/02-03-SUMMARY.md` when done.
</output>
