---
phase: 02-production-infrastructure-provisioning
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-04"]
files_modified:
  - infra/main.bicep
autonomous: true
requirements: [INF-07, INF-08, INF-09]
must_haves:
  truths:
    - "Storage is GRS with blob & container soft-delete + point-in-time restore (versioning + change feed), allowSharedKeyAccess is false in prod, and the Functions host uses identity-based AzureWebJobsStorage (managedidentity)"
    - "The Functions MI holds the Durable trio (Storage Blob/Queue/Table Data Contributor) on the storage account and Cognitive Services User on DocIntel; SharePoint OBO is NOT a Managed Identity role"
    - "Key Vault has a Private Endpoint behind a minimal VNet with the privatelink.vaultcore.azure.net DNS zone, both apps are VNet-integrated, and every secret app setting resolves via a @Microsoft.KeyVault reference (zero cleartext)"
    - "A unique production Durable Task Hub name is decided and recorded (distinct from staging)"
  artifacts:
    - path: "infra/main.bicep"
      provides: "Storage hardening, identity-based storage + Durable role trio, Cognitive Services User, VNet + KV Private Endpoint + DNS, VNet integration, KV-reference app settings"
      contains: "privatelink.vaultcore.azure.net"
  key_links:
    - from: "infra/main.bicep functionApp MI"
      to: "storage (Durable trio) + docIntel (Cognitive Services User)"
      via: "roleAssignments loop scoped per resource"
      pattern: "ba92f5b4-2d11-453d-a403-e96b0029c9fe"
    - from: "infra/main.bicep keyVault"
      to: "privateEndpoint (groupIds vault) + privatelink.vaultcore.azure.net"
      via: "privateDnsZoneGroups + virtualNetworkSubnetId integration"
      pattern: "groupIds"
    - from: "infra/main.bicep app settings"
      to: "Key Vault secrets"
      via: "@Microsoft.KeyVault(SecretUri=...)"
      pattern: "@Microsoft.KeyVault"
---

<objective>
Complete `infra/main.bicep` IN PLACE with the production identity, storage hardening, RBAC, and network perimeter: GRS + soft-delete + PITR storage with identity-based `AzureWebJobsStorage` (`allowSharedKeyAccess=false`); the Durable role trio + Cognitive Services User on the Functions MI; a minimal VNet + Key Vault Private Endpoint + `privatelink.vaultcore.azure.net` DNS + VNet integration on both apps; and `@Microsoft.KeyVault(...)` references for every secret app setting (zero cleartext). Decide and record the unique prod Durable Task Hub name. All gated by new params defaulting to the staging shape.

Purpose: INF-07 (MI role assignments — least privilege; SharePoint OBO is delegated consent, NOT a role), INF-08 (KV references + Private Endpoint + minimal VNet + zero cleartext), INF-09 (GRS + soft-delete + PITR + identity-based AzureWebJobsStorage + unique Task Hub).
Output: extended `infra/main.bicep` (storage/RBAC/network/secrets section); depends on plan 02-04's compute section in the same file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-production-infrastructure-provisioning/02-RESEARCH.md
@.planning/phases/02-production-infrastructure-provisioning/02-PATTERNS.md
@.planning/phases/02-production-infrastructure-provisioning/02-VALIDATION.md
@infra/main.bicep
@infra/prod.parameters.json
@scripts/validate_infra.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden storage (GRS + soft-delete + PITR) + identity-based AzureWebJobsStorage + Durable role trio + Cognitive Services User</name>
  <files>infra/main.bicep</files>
  <read_first>
    - `infra/main.bicep` lines 57-68 (storage block — `Standard_LRS`, `allowSharedKeyAccess: true`, the French `// requis par Durable...` comment), lines 91-99 (the `kvRoleAssignments` loop — THE analog for the new role loops), line 45 (`kvSecretsUserRoleId` var placement), lines 162-177 (functionApp from plan 02-04, where the AzureWebJobsStorage app settings + functionAppConfig live).
    - RESEARCH.md Pattern 4 (lines 370-422) — storageSku/blobServices/PITR + identity-based `AzureWebJobsStorage__accountName`/`__credential=managedidentity` + Durable trio; §Code Examples lines 544-569 (ready role-loop excerpt); the role GUIDs table (lines 113-122). PATTERNS.md lines 53, 147-148.
    - New params from prod.parameters.json (plan 02-01): `storageSku`, `enableIdentityStorage`, `blobSoftDeleteDays`, `containerSoftDeleteDays`, `pointInTimeRestoreDays`.
  </read_first>
  <action>
    Declare new params (defaults = staging shape): `storageSku string = 'Standard_LRS'`, `enableIdentityStorage bool = false`, `blobSoftDeleteDays int = 7`, `containerSoftDeleteDays int = 7`, `pointInTimeRestoreDays int = 6`. On the storage resource: change `sku: { name: 'Standard_LRS' }` to `sku: { name: storageSku }` (INF-09 GRS via prod params); change `allowSharedKeyAccess: true` to `allowSharedKeyAccess: !enableIdentityStorage` (prod=false → identity-only); update the French comment to note identity-based connections remove the shared-key requirement. Add a sibling `Microsoft.Storage/storageAccounts/blobServices@2023-05-01` child (`parent: storage`, name `default`) with `isVersioningEnabled: true`, `changeFeed: { enabled: true }`, `deleteRetentionPolicy: { enabled: true, days: blobSoftDeleteDays }`, `containerDeleteRetentionPolicy: { enabled: true, days: containerSoftDeleteDays }`, `restorePolicy: { enabled: true, days: pointInTimeRestoreDays }` (PITR window must be less than the soft-delete window — RESEARCH A6). On the Flex `functionApp` siteConfig.appSettings (added by plan 02-04's functionAppConfig task), add the identity-based host-storage settings: `AzureWebJobsStorage__accountName` set to `storage.name` and `AzureWebJobsStorage__credential` set to `managedidentity`; ensure NO `AzureWebJobsStorage` connection-string app setting remains. Declare the Durable-trio role-GUID vars beside `kvSecretsUserRoleId` (line 45): Storage Blob Data Contributor `ba92f5b4-2d11-453d-a403-e96b0029c9fe`, Storage Queue Data Contributor `974c5e8b-45b9-4653-ba55-5f855dd0fb88`, Storage Table Data Contributor `0a9a7e1f-b9d0-4cc4-a60d-0319b160aaa3`, Cognitive Services User `a97b65f3-24c7-4388-baec-2e87135dc908`. Add a `funcStorageRoles` loop over the trio (mirror the `kvRoleAssignments` shape exactly: `guid(storage.id, functionApp.identity.principalId, r)`, `scope: storage`, `subscriptionResourceId(...)`, `principalType: 'ServicePrincipal'`) and a single `funcDocIntelRole` assignment (scope `docIntel`, Cognitive Services User, functionApp MI). Do NOT add any "SharePoint OBO" roleAssignment — SharePoint is delegated consent handled in plan 02-02 (RESEARCH line 462). Decide the unique prod Durable Task Hub name `ac360prodhub` and RECORD it in the SUMMARY for Phase 3 host.json (it must differ from staging — RESEARCH Open Q5); optionally surface it as a `taskHubName` param/app-setting note. Run `az bicep build` then `scripts/validate_infra.ps1`.
  </action>
  <verify>
    <automated>az bicep build -f infra/main.bicep --stdout 1>NUL 2>NUL && powershell -NoProfile -File scripts/validate_infra.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `az bicep build -f infra/main.bicep` exits 0.
    - Storage uses `storageSku` (prod GRS), `allowSharedKeyAccess: !enableIdentityStorage`, and a `blobServices` child with versioning + changeFeed + blob/container deleteRetention + restorePolicy (INF-09).
    - Functions app has `AzureWebJobsStorage__accountName` + `AzureWebJobsStorage__credential == managedidentity`; no AzureWebJobsStorage connection-string setting (INF-09).
    - The Durable trio (Blob/Queue/Table Data Contributor) is assigned to the Functions MI scoped to storage; Cognitive Services User scoped to docIntel; NO "SharePoint" roleAssignment exists (INF-07).
    - Unique prod Task Hub name `ac360prodhub` recorded in SUMMARY, distinct from staging.
  </acceptance_criteria>
  <done>Storage is hardened (GRS/soft-delete/PITR/identity-based), the Functions MI holds the Durable trio + Cognitive Services User, SharePoint OBO is not a role, and a unique prod Task Hub name is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Add minimal VNet + Key Vault Private Endpoint + private DNS + VNet integration + KV-reference app settings</name>
  <files>infra/main.bicep</files>
  <read_first>
    - `infra/main.bicep` lines 73-89 (Key Vault — `publicNetworkAccess: keyVaultPublicNetworkAccess` + conditional `defaultAction` already prod-ready), lines 36-42 (naming vars), lines 162-201 (both `sites` from plan 02-04 where `virtualNetworkSubnetId` + KV-reference app settings attach).
    - RESEARCH.md Pattern 3 (lines 295-368) — VNet with 3 subnets (snet-pe PE-policies-off; snet-fx delegation `Microsoft.App/environments` /27; snet-gw delegation `Microsoft.Web/serverFarms` /28), `privateEndpoints` groupIds `['vault']`, `privatelink.vaultcore.azure.net` zone (EXACT literal), `virtualNetworkLinks`, `privateDnsZoneGroups`; §Code Examples line 539 (KV-reference app setting). RESEARCH Pitfalls 2 (flip ordering) + 4 (delegation). PATTERNS.md lines 55, 61, 170-176 (no analog — use RESEARCH Pattern 3 directly).
    - New params from prod.parameters.json (plan 02-01): `enablePrivateNetworking`; CIDR ranges are at Claude's discretion (CONTEXT.md `<decisions>` Claude's Discretion).
  </read_first>
  <action>
    Declare `enablePrivateNetworking bool = false` (default staging-off) and discretionary CIDR params (`vnetAddressPrefix string = '10.42.0.0/24'`, `subnetPePrefix = '10.42.0.0/27'`, `subnetFxPrefix = '10.42.0.32/27'`, `subnetGwPrefix = '10.42.0.64/28'`). Add a network section GATED by `enablePrivateNetworking` (so staging stays unaffected): a `Microsoft.Network/virtualNetworks` with three subnets — `snet-pe` (`privateEndpointNetworkPolicies: 'Disabled'`), `snet-fx` (delegation `Microsoft.App/environments`, the Flex requirement — NOT serverFarms), `snet-gw` (delegation `Microsoft.Web/serverFarms`); no underscores in subnet names (RESEARCH Pitfall 4). Add `Microsoft.Network/privateDnsZones` named EXACTLY `privatelink.vaultcore.azure.net` (literal, not interpolated — PATTERNS.md line 35), a `virtualNetworkLinks` child (`registrationEnabled: false`, link to the VNet), a `Microsoft.Network/privateEndpoints` for the vault (`subnet` = snet-pe, `privateLinkServiceConnections` → `privateLinkServiceId: keyVault.id`, `groupIds: ['vault']`), and a `privateDnsZoneGroups@2023-11-01` child wiring the PE to the DNS zone. Add `virtualNetworkSubnetId` to BOTH `sites`: functionApp → snet-fx (Flex), gatewayApp → snet-gw (App Service). All network resources use conditional deployment `= if (enablePrivateNetworking) { ... }`. Convert the secret app settings to `@Microsoft.KeyVault(SecretUri=...)` references resolved by MI (INF-08): the gateway's `OBO_CLIENT_SECRET` becomes `@Microsoft.KeyVault(SecretUri=...obo secret uri)`, plus any OCR/Fabric/APPINSIGHTS secrets the apps consume — using the secret names `scripts/config.py` reads UNCHANGED (`OBO_CLIENT_SECRET`, `APPLICATIONINSIGHTS_CONNECTION_STRING`, OCR/Fabric creds; RESEARCH Runtime State line 496). Ensure NO literal secret value appears in any app setting. Reference the OBO secret by constructing the SecretUri against the existing `keyVault` resource (the secret itself is created by plan 02-02's script at runtime — use the deterministic `https://${kvName}${environment().suffixes.keyvaultDns}/secrets/OBO-CLIENT-SECRET` form, or an `existing` secret reference, so the app setting resolves once the operator has run the app-reg script). Do NOT flip KV `publicNetworkAccess` in Bicep beyond what prod.parameters.json already sets (`Disabled`) — the operator applies the flip as the final ordered step (provision.ps1 step 6, RESEARCH Pitfall 2). Run `az bicep build` then `scripts/validate_infra.ps1`.
  </action>
  <verify>
    <automated>az bicep build -f infra/main.bicep --stdout 1>NUL 2>NUL && powershell -NoProfile -File scripts/validate_infra.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `az bicep build -f infra/main.bicep` exits 0 (accepts the network section + KV references).
    - A `privateEndpoints` resource for the vault exists with `groupIds: ['vault']`; a `privateDnsZones` named exactly `privatelink.vaultcore.azure.net` with a `virtualNetworkLinks` child and a `privateDnsZoneGroups` child exist (INF-08).
    - VNet has snet-fx delegated `Microsoft.App/environments` and snet-gw delegated `Microsoft.Web/serverFarms`; both `sites` set `virtualNetworkSubnetId` (INF-08).
    - Every secret app setting uses `@Microsoft.KeyVault(`; no literal secret value present (`scripts/validate_infra.ps1` zero-cleartext assertion passes) (INF-08).
    - All network resources are gated by `enablePrivateNetworking` so `az bicep build` with default (staging) params still exits 0.
  </acceptance_criteria>
  <done>Key Vault is private behind a minimal VNet with correct subnet delegations and the privatelink.vaultcore.azure.net DNS zone, both apps are VNet-integrated, and every secret resolves via a Key Vault reference with zero cleartext.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Functions MI → storage (blob/queue/table) | Data-plane access via MI, no shared key |
| apps → Key Vault | Private Endpoint over VNet; no public data-plane path |
| Functions MI → DocIntel | Cognitive Services User (Entra-only OCR) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Spoofing / Info disclosure | shared-key storage access | mitigate | `allowSharedKeyAccess=false` (prod) + identity-based `AzureWebJobsStorage__credential=managedidentity` + Durable data-role trio scoped to the account (INF-09) |
| T-02-17 | Elevation of privilege | over-privileged Functions MI | mitigate | Only least-privilege DATA roles (Blob/Queue/Table Data Contributor, Cognitive Services User) scoped per resource — never Owner/Contributor; SharePoint OBO is NOT an MI role (INF-07) |
| T-02-18 | Information disclosure | public Key Vault exposure | mitigate | Private Endpoint (`groupIds: ['vault']`) + `privatelink.vaultcore.azure.net` DNS + VNet integration; KV `publicNetworkAccess=Disabled` (prod params), flipped last by the operator after PE/integration (INF-08) |
| T-02-19 | Information disclosure | cleartext secret in app settings | mitigate | Every secret app setting is a `@Microsoft.KeyVault(SecretUri=...)` reference resolved by MI; validator asserts zero literal secrets (INF-08) |
| T-02-20 | Tampering | cross-environment Durable state collision | mitigate | Unique prod Task Hub name `ac360prodhub` decided + recorded, distinct from staging (INF-09) |
| T-02-21 | DoS / availability | KV references break on private flip | mitigate | DNS zone group auto-maintains the PE A-record; the public-access flip is ordered AFTER PE + VNet integration (provision.ps1 step 6; RESEARCH Pitfall 2) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase (RESEARCH line 142) |
</threat_model>

<verification>
- `az bicep build -f infra/main.bicep` exits 0 with the network section + KV references.
- `scripts/validate_infra.ps1` passes the INF-07/08/09 assertions (GRS, soft-delete/PITR, identity storage, Durable trio + Cognitive Services User, no SharePoint role, PE + privatelink zone, zero cleartext).
- `az bicep build` with default (staging) params exits 0 (network gated off).
</verification>

<success_criteria>
- INF-07: Durable trio + Cognitive Services User on the Functions MI, scoped per resource; SharePoint OBO not a role.
- INF-08: KV Private Endpoint + minimal VNet + privatelink.vaultcore.azure.net DNS + VNet integration; every secret via @Microsoft.KeyVault reference; zero cleartext.
- INF-09: GRS + blob/container soft-delete + PITR (versioning + change feed) + identity-based AzureWebJobsStorage + unique prod Task Hub name.
</success_criteria>

<artifacts_this_phase_produces>
**Modified file (this plan):**
- `infra/main.bicep` — storage hardening (GRS/soft-delete/PITR via `blobServices`), identity-based AzureWebJobsStorage app settings, Durable-trio + Cognitive Services User role assignments, minimal VNet (3 delegated subnets), KV Private Endpoint + `privatelink.vaultcore.azure.net` DNS zone + link + zone group, VNet integration on both apps, `@Microsoft.KeyVault` secret app settings.

**New Bicep params:** `storageSku`, `enableIdentityStorage`, `blobSoftDeleteDays`, `containerSoftDeleteDays`, `pointInTimeRestoreDays`, `enablePrivateNetworking`, `vnetAddressPrefix`, `subnetPePrefix`, `subnetFxPrefix`, `subnetGwPrefix`.

**New Bicep vars:** `storageBlobDataContributor`, `storageQueueDataContributor`, `storageTableDataContributor`, `cognitiveServicesUserRoleId` (role GUIDs beside `kvSecretsUserRoleId`).

**New Bicep resources:** `blobSvc` (blobServices), `funcStorageRoles` (loop), `funcDocIntelRole`, `vnet`, `kvDnsZone`, `kvDnsLink`, `kvPe`, `kvPeDnsGroup`.

**Recorded decision:** unique prod Durable Task Hub name `ac360prodhub` (consumed by Phase 3 host.json).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-production-infrastructure-provisioning/02-05-SUMMARY.md` when done.
</output>
