---
phase: 02-production-infrastructure-provisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/prod.parameters.json
  - scripts/validate_infra.ps1
  - infra/bicepconfig.json
autonomous: true
requirements: [INF-01]
must_haves:
  truths:
    - "A prod parameter file selects France Central, prod SKUs, GRS storage, identity storage, private networking, and KV public access Disabled — without touching staging.parameters.json"
    - "An offline static-assertion validator compiles main.bicep and asserts the per-INF properties, failing closed on any violation"
  artifacts:
    - path: "infra/prod.parameters.json"
      provides: "Production parameter values feeding main.bicep"
      contains: "francecentral"
    - path: "scripts/validate_infra.ps1"
      provides: "Offline az bicep build + per-INF static assertion gate"
      min_lines: 60
  key_links:
    - from: "infra/prod.parameters.json"
      to: "infra/main.bicep"
      via: "az deployment -p @infra/prod.parameters.json"
      pattern: "\"value\""
    - from: "scripts/validate_infra.ps1"
      to: "infra/main.bicep"
      via: "az bicep build -f infra/main.bicep"
      pattern: "az bicep build"
---

<objective>
Create the production parameter file (`infra/prod.parameters.json`) and the offline static-assertion validator (`scripts/validate_infra.ps1`) that every later wave compiles against. This is the Wave-0 foundation: the prod parameter surface and the test runner the IaC phase otherwise lacks.

Purpose: INF-01 demands all Bicep `location` params set explicitly for an EU region; the prod parameter file is where that (and every prod-only opt-in) lands without breaking staging. The validator closes the "IaC has no test runner" gap so each later task has an offline, fail-closed verify.
Output: `infra/prod.parameters.json`, `scripts/validate_infra.ps1`, optional `infra/bicepconfig.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-production-infrastructure-provisioning/02-RESEARCH.md
@.planning/phases/02-production-infrastructure-provisioning/02-PATTERNS.md
@.planning/phases/02-production-infrastructure-provisioning/02-VALIDATION.md
@infra/main.bicep
@infra/staging.parameters.json
@scripts/package_release.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create infra/prod.parameters.json with the full prod parameter surface</name>
  <files>infra/prod.parameters.json</files>
  <read_first>
    - `infra/staging.parameters.json` (exact analog — copy `$schema`, `contentVersion`, `parameters` object with `{ "value": ... }` wrappers; PATTERNS.md lines 69-87).
    - `infra/main.bicep` lines 14-34 (current param names: `location`, `namePrefix`, `environmentName`, `keyVaultPublicNetworkAccess`, `docIntelDisableLocalAuth`, `keyVaultSecretsReaderPrincipalIds`, `gatewayOutboundIps`).
    - RESEARCH.md §recommended structure (lines 180-198) and Pattern 1/3/4 param lists; CONTEXT.md `<decisions>` (locked values).
  </read_first>
  <action>
    Write `infra/prod.parameters.json` mirroring the staging file's JSON shape exactly. Set the locked prod deltas per D-decisions: `environmentName: "prod"`; `location: "francecentral"` (INF-01, explicit EU region); a separate `docIntelLocation: "francecentral"` value (West Europe is the operator-applied fallback if the residency checkpoint fails — note this in a JSON-adjacent comment is not possible, so document it in the SUMMARY); `keyVaultPublicNetworkAccess: "Disabled"` (INF-08); `docIntelDisableLocalAuth: true` (INF-04); `storageSku: "Standard_GRS"` (INF-09); `enableIdentityStorage: true` (INF-09); `enablePrivateNetworking: true` (INF-08); the Flex params `funcMaxInstanceCount` (small for one team, e.g. 40), `funcInstanceMemoryMB: 2048`, `funcRuntimeVersion: "3.12"`, `deploymentContainerName: "app-package"`; the B1 capacity pin is carried in main.bicep's `sku` literal (no param needed) per D-AUD-04 carry-forward; blob/PITR retention values `blobSoftDeleteDays: 7`, `containerSoftDeleteDays: 7`, `pointInTimeRestoreDays: 6` (PITR window < soft-delete window, RESEARCH A6); and `keyVaultSecretsReaderPrincipalIds: []` + `gatewayOutboundIps: []` (operator populates from live outputs per infra/README). Include ONLY parameter names that this plan's later sibling plans (02-02 main.bicep work) will actually declare in main.bicep; do NOT invent params main.bicep will not consume — the validator (Task 2) and `az bicep build` will reject unknown params, which is the intended cross-check. Keep `staging.parameters.json` UNTOUCHED.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Get-Content infra/prod.parameters.json -Raw | ConvertFrom-Json | Out-Null; if ((Get-Content infra/prod.parameters.json -Raw) -match 'francecentral' -and (Get-Content infra/prod.parameters.json -Raw) -match '\"environmentName\"') { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `infra/prod.parameters.json` parses as valid JSON (ConvertFrom-Json exits 0).
    - File sets `location` to `francecentral` and `environmentName` to `prod`.
    - File sets `keyVaultPublicNetworkAccess: "Disabled"`, `docIntelDisableLocalAuth: true`, `storageSku: "Standard_GRS"`, `enableIdentityStorage: true`, `enablePrivateNetworking: true`.
    - `infra/staging.parameters.json` is byte-identical to its pre-task state (not modified).
  </acceptance_criteria>
  <done>prod.parameters.json exists with the locked prod parameter surface; staging.parameters.json unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/validate_infra.ps1 — offline build + per-INF static-assertion gate</name>
  <files>scripts/validate_infra.ps1, infra/bicepconfig.json</files>
  <read_first>
    - `scripts/package_release.ps1` lines 11-16 (param + `$ErrorActionPreference = "Stop"`) and lines 64-93 (collect-violations → red print → `exit 1`, else green `exit 0`) — the fail-closed analog (PATTERNS.md lines 130-140).
    - RESEARCH.md §Validation Map (lines 674-684) and VALIDATION.md Per-Requirement table (lines 39-49) — the exact per-INF assertions.
    - CLAUDE.md PowerShell conventions (French comment-help, colored Write-Host status).
  </read_first>
  <action>
    Write `scripts/validate_infra.ps1` following the package_release fail-closed structure: French `<# .SYNOPSIS #>` help block, `param([string]$BicepFile = "infra/main.bicep")`, `$ErrorActionPreference = "Stop"`. Step 1: run `az bicep build -f $BicepFile --outfile $env:TEMP\ac360_main.json` (offline, no auth) and `exit 1` with a red message if the build fails. Step 2: load the compiled ARM JSON (`Get-Content ... | ConvertFrom-Json`) and accumulate a `$violations` array by asserting the per-INF properties that are offline-checkable: gateway plan `sku.name == 'B1'` and `capacity == 1` (INF-02); gatewayApp `siteConfig.alwaysOn == true` and `appCommandLine` contains `--workers 1` (INF-02/AUD-04); func plan `sku.name == 'FC1'`/tier `FlexConsumption` and `functionAppConfig.runtime.version == '3.12'` (INF-03); docIntel `sku.name == 'S0'` and `disableLocalAuth == true` (INF-04); storage `sku.name == 'Standard_GRS'`, `allowSharedKeyAccess == false`, a `blobServices` child with `deleteRetentionPolicy`+`containerDeleteRetentionPolicy`+`restorePolicy`+`isVersioningEnabled`+`changeFeed` (INF-09); a `AzureWebJobsStorage__credential` app setting equal to `managedidentity` (INF-09); every app-setting whose name implies a secret (`*SECRET*`, `*KEY*`, `APPLICATIONINSIGHTS_CONNECTION_STRING`, `OBO_CLIENT_SECRET`, OCR/Fabric creds) has a value starting with `@Microsoft.KeyVault(` and NO literal secret value (INF-08); a private DNS zone named exactly `privatelink.vaultcore.azure.net` and a privateEndpoint with `groupIds` containing `vault` (INF-08); the Durable trio role GUIDs present scoped to storage and Cognitive Services User scoped to docIntel (INF-07); and assert NO roleAssignment exists for a "SharePoint" principal (SharePoint OBO is delegated consent, NOT an MI role — RESEARCH line 462). Because main.bicep does not exist yet in this wave, make the validator tolerant of an absent/empty compile by exiting 0 with a yellow "main.bicep not yet extended — assertions deferred" message ONLY when the build produces no resources matching the prod shape (so this plan's own verify passes), but fail closed (`exit 1`) once the prod resources are present and any assertion is violated. Print each violation in red, then `exit 1` if `$violations.Count -gt 0`, else green + `exit 0`. Optionally also write `infra/bicepconfig.json` enabling the Bicep linter `core` ruleset at `warning` so `az bicep build` surfaces lint issues; keep it minimal.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/validate_infra.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validate_infra.ps1` runs offline (no `az login`) and exits 0 against the current baseline main.bicep (assertions deferred while prod resources absent).
    - The script invokes `az bicep build -f infra/main.bicep` and fails closed (`exit 1`) on build error.
    - The script contains assertion logic for every offline-checkable INF (02,03,04,07,08,09) per VALIDATION.md lines 41-49, including the "SharePoint OBO is NOT a roleAssignment" negative check.
    - On a violated assertion (once prod resources exist) the script exits 1 and prints the violation in red.
  </acceptance_criteria>
  <done>validate_infra.ps1 compiles main.bicep offline and enforces the per-INF static assertions, fail-closed; passes against the current baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → repo | Parameter/validator authoring; no secrets cross this boundary (all secrets are KV references, never literal values) |
| repo → Azure control plane (later, operator) | prod.parameters.json drives `az deployment`; this plan only authors it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | prod.parameters.json | mitigate | No literal secret values in the parameter file; all secret app settings are `@Microsoft.KeyVault(...)` references defined in main.bicep — validator Task 2 asserts no literal secrets (INF-08) |
| T-02-02 | Tampering | infra/main.bicep drift | mitigate | validate_infra.ps1 is a fail-closed static gate asserting the hardened prod shape (B1 capacity=1, S0 disableLocalAuth, allowSharedKeyAccess=false, KV PE) — prevents silent SKU/posture regression |
| T-02-03 | Elevation of privilege | over-broad MI roles | mitigate | Validator negative-asserts that "SharePoint OBO" is NOT a roleAssignment and that only least-privilege data roles are present (INF-07) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero external packages (RESEARCH §Standard Stack line 92 / line 142); no supply-chain surface — nothing to slopcheck |
</threat_model>

<verification>
- `az bicep build -f infra/main.bicep` exits 0 (offline).
- `powershell -File scripts/validate_infra.ps1` exits 0 against baseline.
- `infra/prod.parameters.json` parses and sets `francecentral` + the locked prod deltas.
- `infra/staging.parameters.json` unmodified.
</verification>

<success_criteria>
- Prod parameter surface authored (INF-01 explicit EU location + all prod opt-ins) without touching staging params.
- Offline fail-closed validator exists and enforces the per-INF static assertions for INF-02/03/04/07/08/09.
</success_criteria>

<artifacts_this_phase_produces>
**New files (this plan):**
- `infra/prod.parameters.json` — prod parameter values (francecentral, prod SKUs, GRS, identity storage, private networking, KV Disabled).
- `scripts/validate_infra.ps1` — offline `az bicep build` + per-INF static-assertion fail-closed gate.
- `infra/bicepconfig.json` (optional) — Bicep linter ruleset.

**New parameter names referenced (declared in main.bicep by plan 02-02):** `docIntelLocation`, `storageSku`, `enableIdentityStorage`, `enablePrivateNetworking`, `funcMaxInstanceCount`, `funcInstanceMemoryMB`, `funcRuntimeVersion`, `deploymentContainerName`, `blobSoftDeleteDays`, `containerSoftDeleteDays`, `pointInTimeRestoreDays`.

**New PowerShell functions:** `Test-InfraAssertions` (or equivalent collect-violations function in validate_infra.ps1).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-production-infrastructure-provisioning/02-01-SUMMARY.md` when done.
</output>
