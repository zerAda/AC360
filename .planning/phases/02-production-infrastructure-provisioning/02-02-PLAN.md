---
phase: 02-production-infrastructure-provisioning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/provision_app_registrations.ps1
autonomous: true
requirements: [INF-05, INF-06]
user_setup:
  - service: entra-id
    why: "App registrations and admin consent live in Entra (not git); creation + admin-consent require an authenticated operator with Global/Privileged Role Admin in the GEREP prod tenant"
    env_vars:
      - name: OBO-CLIENT-SECRET
        source: "Generated by this script via az ad app credential reset, written ONLY to Key Vault (az keyvault secret set), never to a file or log"
    dashboard_config:
      - task: "Grant admin consent for the OBO app's delegated Graph scopes"
        location: "Entra admin center → App registrations → AC360-OBO-prod → API permissions → Grant admin consent (or az ad app permission admin-consent)"
must_haves:
  truths:
    - "An idempotent script creates two prod Entra app registrations: an API audience app with NO client secret, and an OBO confidential client whose generated secret is written only to Key Vault"
    - "Delegated Graph scope GUIDs are resolved at runtime from the live Graph service principal (never hardcoded), and admin consent is requested for the OBO app"
    - "The OBO secret is never echoed to logs, stdout, or any file"
  artifacts:
    - path: "scripts/provision_app_registrations.ps1"
      provides: "Idempotent az-based app-reg + delegated-scope + admin-consent + secret→KV script"
      min_lines: 80
  key_links:
    - from: "scripts/provision_app_registrations.ps1"
      to: "Key Vault"
      via: "az keyvault secret set --name OBO-CLIENT-SECRET"
      pattern: "az keyvault secret set"
    - from: "scripts/provision_app_registrations.ps1"
      to: "Microsoft Graph SP"
      via: "az ad sp show --id 00000003-0000-0000-c000-000000000000"
      pattern: "00000003-0000-0000-c000-000000000000"
---

<objective>
Create `scripts/provision_app_registrations.ps1`: the idempotent, az-driven script that creates the two production Entra app registrations (API audience app with no secret; OBO confidential client with secret → Key Vault), resolves delegated Graph scope GUIDs from the live tenant, requests them on the OBO app, and runs admin consent. Entra app objects and consent are not ARM resources, so this is the imperative complement to the Bicep IaC.

Purpose: INF-05 (prod app registrations) and INF-06 (admin consent for delegated Graph scopes, blocking pre-flight, no AADSTS65001). SharePoint OBO is satisfied here as delegated consent — NOT as a Managed Identity role.
Output: `scripts/provision_app_registrations.ps1`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-production-infrastructure-provisioning/02-RESEARCH.md
@.planning/phases/02-production-infrastructure-provisioning/02-PATTERNS.md
@scripts/deploy_azure_ocr.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/provision_app_registrations.ps1 (idempotent app-reg + scopes + secret→KV)</name>
  <files>scripts/provision_app_registrations.ps1</files>
  <read_first>
    - `scripts/deploy_azure_ocr.ps1` lines 1-21 (French `<# .SYNOPSIS #>` + typed `param(...)`), lines 23-36 (az-presence + `az account show`/`az login` guard — copy verbatim), lines 57-61 (CI secret masking `::add-mask::` / `##vso[task.setsecret]`). PATTERNS.md lines 91-109 explicitly say to ADAPT but NOT copy the `.env.generated` write (lines 63-66).
    - RESEARCH.md Pattern 5 (lines 424-462) — the ready-made az-ad-app excerpt: check-then-create by displayName, API app no `credential reset`, OBO `az ad app credential reset` → KV, `Get-Scope` via `az ad sp show`, `az ad app permission add` then `admin-consent`.
    - RESEARCH.md §Graph well-known IDs (lines 124-140): Graph appId `00000003-0000-0000-c000-000000000000`; scope set `Files.Read.All`,`Sites.Read.All`,`Tasks.ReadWrite`,`offline_access`,`User.Read` (the `[ASSUMED]` GUIDs are RESOLVED AT RUNTIME, not hardcoded).
    - CLAUDE.md: secrets NEVER logged; no cleartext.
  </read_first>
  <action>
    Write `scripts/provision_app_registrations.ps1` with: French comment-help; `param([string]$KeyVaultName, [string]$ApiAppName = 'AC360-API-prod', [string]$OboAppName = 'AC360-OBO-prod', [int]$SecretYears = 1)`; `$ErrorActionPreference = "Stop"`. Copy the az-presence + login guard from deploy_azure_ocr lines 23-36. (1) API audience app (INF-05, no secret): check-then-create by displayName via `az ad app list --filter "displayName eq '$ApiAppName'" --query "[0].appId" -o tsv`, create with `az ad app create --display-name $ApiAppName --sign-in-audience AzureADMyOrg` if absent; set `az ad app update --id $apiAppId --identifier-uris "api://$apiAppId"` so tokens carry the `api://<appId>` audience; expose the `Audit.Trigger` delegated scope (set `api.oauth2PermissionScopes`). Issue NO `az ad app credential reset` for this app (asserted by the validator/Pester). (2) OBO confidential client (INF-05/06): check-then-create by displayName; define `function Get-Scope($v) { az ad sp show --id 00000003-0000-0000-c000-000000000000 --query "oauth2PermissionScopes[?value=='$v'].id | [0]" -o tsv }` so scope GUIDs are tenant-correct, never hardcoded (per D — resolve at runtime); loop `Files.Read.All`,`Sites.Read.All`,`Tasks.ReadWrite`,`offline_access`,`User.Read` calling `az ad app permission add --id $oboAppId --api 00000003-0000-0000-c000-000000000000 --api-permissions "$gid=Scope" --only-show-errors`; ensure the OBO SP exists (`az ad sp create --id $oboAppId` if needed — prevents AADSTS65001 per RESEARCH Pitfall 5). Generate the secret with `az ad app credential reset --id $oboAppId --append --display-name 'obo-prod' --years $SecretYears --query password -o tsv`, immediately `az keyvault secret set --vault-name $KeyVaultName --name 'OBO-CLIENT-SECRET' --value $secret 1>$null` then `$secret = $null` (CLAUDE.md secret hygiene; PATTERNS.md lines 107/162-164). Emit the CI masks (`::add-mask::`) BEFORE any write but NEVER `Write-Host` the secret value or write it to a file. (3) Admin consent: call `az ad app permission admin-consent --id $oboAppId` (this is the live operator step — the script issues it but the actual grant requires Global/Privileged Role Admin; mark in a yellow status line that consent must be re-verified via `az ad app permission list-grants`). Print the API appId and OBO appId (NOT the secret) as the script's machine-readable output so the operator can wire them into prod.parameters.json / app settings.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ast=[System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/provision_app_registrations.ps1), [ref]$null, [ref]$null); $t=Get-Content scripts/provision_app_registrations.ps1 -Raw; if ($t -match 'az keyvault secret set' -and $t -match '00000003-0000-0000-c000-000000000000' -and $t -notmatch 'Write-Host\s+\$secret' -and $t -notmatch '\.env') { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - Script parses without syntax errors (PowerShell AST parse succeeds).
    - Script writes the OBO secret via `az keyvault secret set` and contains NO `.env` write and NO `Write-Host $secret` (offline secret-hygiene grep, INF-05/08).
    - Script resolves Graph scope GUIDs at runtime via `az ad sp show --id 00000003-0000-0000-c000-000000000000` (no hardcoded scope GUID constants).
    - The API audience app is created with NO `az ad app credential reset` (only the OBO app gets a credential).
    - Script issues `az ad app permission admin-consent` for the OBO app (INF-06).
  </acceptance_criteria>
  <done>provision_app_registrations.ps1 idempotently creates both prod app regs, resolves+requests delegated scopes, writes the OBO secret to Key Vault only, and requests admin consent — with zero secret leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Entra control plane | `az ad app create/permission/admin-consent` — privileged identity required for consent |
| script → Key Vault | OBO secret written via MI/operator creds; secret crosses here and ONLY here |
| OBO app → Microsoft Graph (runtime, per-user) | Delegated, per-user SharePoint/Planner access — not an app-level role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information disclosure | OBO client secret | mitigate | Secret generated in-memory, written ONLY to Key Vault via `az keyvault secret set ... 1>$null`, then `$secret=$null`; never echoed, never written to a file (verify greps for `.env`/`Write-Host $secret`) (INF-05/08) |
| T-02-05 | Elevation of privilege | OBO delegated Graph scopes | mitigate | Least-privilege delegated scopes only (`Files.Read.All`,`Sites.Read.All`,`Tasks.ReadWrite`,`offline_access`,`User.Read`); resolved from the live Graph SP, never an app-level (`=Role`) Graph permission (INF-06) |
| T-02-06 | Spoofing / DoS | missing admin consent (AADSTS65001) | mitigate | Script ensures the OBO SP exists then issues `admin-consent`; the live grant is a blocking operator checkpoint (plan 02-04) verified via `az ad app permission list-grants` (INF-06) |
| T-02-07 | Spoofing | API audience app credential | mitigate | API audience app carries NO secret (asserted: no `credential reset`); only the confidential OBO client holds a credential (INF-05) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase (RESEARCH line 142) — no supply-chain surface |
</threat_model>

<verification>
- PowerShell AST parse of `scripts/provision_app_registrations.ps1` succeeds.
- Offline secret-hygiene grep: `az keyvault secret set` present; no `.env` write; no `Write-Host $secret`.
- Runtime scope resolution via `az ad sp show --id 00000003-...` present (no hardcoded scope GUIDs).
</verification>

<success_criteria>
- INF-05: both prod app regs scripted idempotently (API no secret; OBO secret → KV).
- INF-06: delegated scopes requested + admin-consent issued; live grant deferred to operator checkpoint (plan 02-04).
- Zero secret leakage to logs/files.
</success_criteria>

<artifacts_this_phase_produces>
**New file (this plan):**
- `scripts/provision_app_registrations.ps1` — idempotent az-based app-reg/consent + secret→KV.

**New PowerShell functions:** `Get-Scope` (runtime Graph delegated-scope GUID resolver).

**New Entra objects (operator-created at runtime, not in repo):** `AC360-API-prod` (no secret), `AC360-OBO-prod` (secret in Key Vault as `OBO-CLIENT-SECRET`).

**New Key Vault secret name:** `OBO-CLIENT-SECRET`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-production-infrastructure-provisioning/02-02-SUMMARY.md` when done.
</output>
