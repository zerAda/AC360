---
phase: 02-production-infrastructure-provisioning
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - infra/main.bicep
autonomous: true
requirements: [INF-02, INF-03, INF-04]
must_haves:
  truths:
    - "The gateway plan is App Service B1 with explicit capacity=1 and Always On, preserving the load-bearing single-instance comment and gunicorn --workers 1 start command"
    - "The Functions app runs on a new Flex Consumption (FC1) plan with a functionAppConfig block (blobContainer deployment via SystemAssignedIdentity, scale/concurrency, python 3.12 runtime) and no legacy Flex-incompatible settings"
    - "Document Intelligence runs on S0 with disableLocalAuth=true in its own location param (West Europe fallback)"
    - "az bicep build accepts functionAppConfig; staging.parameters.json still compiles because every prod behavior is a new param defaulting to the staging shape"
  artifacts:
    - path: "infra/main.bicep"
      provides: "Prod-tier compute SKUs (B1 gateway, FC1 Flex functions, S0 DocIntel) parameterized staging-safe"
      contains: "FC1"
  key_links:
    - from: "infra/main.bicep functionApp"
      to: "funcPlan (FC1/FlexConsumption)"
      via: "serverFarmId + functionAppConfig"
      pattern: "functionAppConfig"
    - from: "infra/main.bicep gwPlan"
      to: "B1 capacity=1"
      via: "sku literal"
      pattern: "'B1'"
---

<objective>
Extend `infra/main.bicep` IN PLACE with the production compute tier: promote the gateway plan to App Service B1 (explicit `capacity=1`, Always On — landing the Phase-1-deferred pin), replace the Y1 Functions plan/app with a new Flex Consumption (FC1) plan + a `functionAppConfig` block (Y1→Flex in-place is unsupported), and promote Document Intelligence to S0 with `disableLocalAuth=true` in its own location param. Every prod behavior is a NEW param defaulting to the staging value so `staging.parameters.json` keeps compiling.

Purpose: INF-02 (B1 + Always On + capacity=1), INF-03 (Flex Consumption new app), INF-04 (DocIntel S0 + disableLocalAuth). This is the largest new IaC construct (the Flex `functionAppConfig`), so it gets its own plan ahead of the storage/network/RBAC wiring.
Output: extended `infra/main.bicep` (compute section).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-production-infrastructure-provisioning/02-RESEARCH.md
@.planning/phases/02-production-infrastructure-provisioning/02-PATTERNS.md
@.planning/phases/02-production-infrastructure-provisioning/02-VALIDATION.md
@infra/main.bicep
@infra/prod.parameters.json
@scripts/validate_infra.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 apiVersion build-check — confirm Microsoft.Web/sites accepts functionAppConfig</name>
  <files>infra/main.bicep</files>
  <read_first>
    - `infra/main.bicep` lines 162-201 (current `functionApp`/`gatewayApp` on `Microsoft.Web/sites@2023-12-01`).
    - RESEARCH.md Pattern 1 (lines 200-263), Pitfall 1 (lines 503-506), Open Q2 (lines 632-634), Assumptions A1 — the repo is on `2023-12-01`; if it rejects `functionAppConfig`, bump `sites` to `2024-04-01`.
  </read_first>
  <action>
    Before adding the full Flex shape, add a MINIMAL `functionAppConfig: { runtime: { name: 'python', version: '3.12' } }` stub to the existing `functionApp` resource (still `kind: 'functionapp,linux'`) and run `az bicep build -f infra/main.bicep`. If the build errors on `functionAppConfig` being an unrecognized property, bump the `Microsoft.Web/sites` apiVersion on BOTH `functionApp` and `gatewayApp` from `2023-12-01` to `2024-04-01` (leave `serverfarms`, `storageAccounts`, `roleAssignments`, `keyvault`, `cognitiveservices` apiVersions UNCHANGED — RESEARCH Pitfall 1). Re-run `az bicep build` until it exits 0. Record the resolved apiVersion in the SUMMARY (closes Open Q2). Do NOT remove the existing hardening (`httpsOnly`, `minTlsVersion`, `ftpsState`, ingress `ipSecurityRestrictions`) and do NOT touch the AUD-04 comment block yet.
  </action>
  <verify>
    <automated>az bicep build -f infra/main.bicep --stdout 1>NUL 2>NUL && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `az bicep build -f infra/main.bicep` exits 0 with a `functionAppConfig` block present on the Functions app.
    - The resolved `Microsoft.Web/sites` apiVersion that accepts `functionAppConfig` is recorded (Open Q2 closed); only the `sites` apiVersion is changed if a bump is needed.
    - No existing hardening property is removed.
  </acceptance_criteria>
  <done>The minimum Microsoft.Web/sites apiVersion accepting functionAppConfig is confirmed via az bicep build and pinned in main.bicep.</done>
</task>

<task type="auto">
  <name>Task 2: Promote gateway to B1 + capacity=1 + Always On (preserve AUD-04 pin)</name>
  <files>infra/main.bicep</files>
  <read_first>
    - `infra/main.bicep` lines 127-157 (the AUD-04 LOAD-BEARING comment block + gwPlan F1 + the line-153-156 note that capacity=1 lands "en Phase 2 (INF-02, B1)") and lines 182-201 (gatewayApp + the `gunicorn --workers 1` appCommandLine + its load-bearing comment lines 194-197).
    - RESEARCH.md Pattern 2 (lines 265-293) — B1 sku literal + alwaysOn; anti-pattern: any autoscale rule with maximum>1 (breaks AUD-04). PATTERNS.md lines 59-61.
  </read_first>
  <action>
    Change `gwPlan.sku` from `{ name: 'F1', tier: 'Free' }` to `{ name: 'B1', tier: 'Basic', capacity: 1 }` (explicit `capacity=1` — the Phase-1-deferred pin per D-AUD-04; F1 refused explicit capacity). PRESERVE the entire AUD-04 LOAD-BEARING comment block (lines 128-150) verbatim — update ONLY the inline `// F1/Free : ...` note on the sku line (line 154) to state that the explicit capacity=1 pin is now landed on B1. Add `alwaysOn: true` to `gatewayApp.siteConfig` (INF-02). PRESERVE the `gunicorn --workers 1` appCommandLine (line 198) and its load-bearing comment (lines 194-197) verbatim. Add NO `Microsoft.Insights/autoscaleSettings` targeting `gwPlan` (anti-pattern — would break AUD-04). Keep `gatewayApp` `linuxFxVersion: 'Python|3.12'` (gateway is NOT Flex — only the Functions app drops linuxFxVersion). Run `az bicep build` after the change.
  </action>
  <verify>
    <automated>az bicep build -f infra/main.bicep --stdout 1>NUL 2>NUL && powershell -NoProfile -Command "$t=Get-Content infra/main.bicep -Raw; if ($t -match \"name:\s*'B1'\" -and $t -match 'capacity:\s*1' -and $t -match 'alwaysOn:\s*true' -and $t -match '--workers 1') { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `az bicep build` exits 0.
    - `gwPlan.sku` is `{ name: 'B1', tier: 'Basic', capacity: 1 }`; `gatewayApp.siteConfig.alwaysOn == true`.
    - The AUD-04 load-bearing comment block and the `gunicorn --workers 1` appCommandLine are preserved verbatim.
    - No `autoscaleSettings` resource targets `gwPlan` (grep confirms none added).
  </acceptance_criteria>
  <done>Gateway runs on B1 with explicit capacity=1 and Always On; the AUD-04 single-instance pin is preserved and now explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Replace Y1 funcPlan/functionApp with Flex (FC1) + functionAppConfig; promote DocIntel to S0</name>
  <files>infra/main.bicep</files>
  <read_first>
    - `infra/main.bicep` lines 104-115 (DocIntel F0 block — `disableLocalAuth` already parameterized), lines 120-125 (funcPlan Y1), lines 162-177 (functionApp).
    - RESEARCH.md Pattern 1 (lines 200-263) — FC1 `sku: { name: 'FC1', tier: 'FlexConsumption' }`, `functionAppConfig` (deployment.storage blobContainer + `authentication: { type: 'SystemAssignedIdentity' }`, scaleAndConcurrency, runtime python 3.12); anti-pattern: do NOT set `linuxFxVersion` / `FUNCTIONS_EXTENSION_VERSION` / `WEBSITE_RUN_FROM_PACKAGE` on Flex (lines 466). PATTERNS.md lines 57, 61. RESEARCH §Architectural Responsibility Map (Flex host config = Infra).
    - New params declared in prod.parameters.json (plan 02-01): `funcMaxInstanceCount`, `funcInstanceMemoryMB`, `funcRuntimeVersion`, `deploymentContainerName`, `docIntelLocation`.
  </read_first>
  <action>
    Declare the new params at the top of main.bicep beside the existing param block (decorator-then-param form, defaults = STAGING shape so staging.parameters.json keeps compiling): `funcMaxInstanceCount int = 40`, `funcInstanceMemoryMB int = 2048` (`@allowed([512,2048,4096])`), `funcRuntimeVersion string = '3.12'`, `deploymentContainerName string = 'app-package'`, `docIntelLocation string = location` (so staging stays in `location`; prod sets `westeurope` fallback if needed). Change `funcPlan.sku` from `{ name: 'Y1', tier: 'Dynamic' }` to `{ name: 'FC1', tier: 'FlexConsumption' }` keeping `properties: { reserved: true }`. On `functionApp`: keep `kind: 'functionapp,linux'`, `identity: { type: 'SystemAssigned' }`, `httpsOnly: true`; REMOVE `linuxFxVersion: 'Python|3.12'` from the Functions app siteConfig (Flex-incompatible — runtime now lives in functionAppConfig); KEEP `minTlsVersion`, `ftpsState`, ingress `ipSecurityRestrictions`; add the full `functionAppConfig` per Pattern 1: `deployment.storage` = `{ type: 'blobContainer', value: '${storage.properties.primaryEndpoints.blob}${deploymentContainerName}', authentication: { type: 'SystemAssignedIdentity' } }`, `scaleAndConcurrency: { maximumInstanceCount: funcMaxInstanceCount, instanceMemoryMB: funcInstanceMemoryMB }`, `runtime: { name: 'python', version: funcRuntimeVersion }`. (Identity-based AzureWebJobsStorage app settings + the Durable role trio land in plan 02-05 — do NOT add them here; this keeps the file compiling but note in SUMMARY that the Flex app's storage auth wiring is completed in 02-05.) Promote DocIntel: change `docIntel.sku` `{ name: 'F0' }` → `{ name: 'S0' }`; change `location: location` → `location: docIntelLocation`; `disableLocalAuth: docIntelDisableLocalAuth` already parameterized (prod sets true). Run `az bicep build` and then `scripts/validate_infra.ps1`.
  </action>
  <verify>
    <automated>az bicep build -f infra/main.bicep --stdout 1>NUL 2>NUL && powershell -NoProfile -Command "$t=Get-Content infra/main.bicep -Raw; if ($t -match \"name:\s*'FC1'\" -and $t -match 'functionAppConfig' -and $t -match 'SystemAssignedIdentity' -and $t -match \"name:\s*'S0'\" -and $t -notmatch 'linuxFxVersion.*Python\|3\.12.*\n.*functionAppConfig') { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `az bicep build -f infra/main.bicep` exits 0.
    - `funcPlan.sku` is `{ name: 'FC1', tier: 'FlexConsumption' }`; the Functions app has a `functionAppConfig` with `deployment.storage.authentication.type == 'SystemAssignedIdentity'`, scaleAndConcurrency, and `runtime.version == '3.12'`.
    - The Functions app no longer sets `linuxFxVersion` (Flex-incompatible); the gateway still does.
    - `docIntel.sku.name == 'S0'` and `docIntel.location == docIntelLocation`.
    - `staging.parameters.json` still compiles (`az bicep build` with no params, defaults = staging shape, exits 0).
  </acceptance_criteria>
  <done>The Functions app is a new Flex Consumption (FC1) app with a functionAppConfig; DocIntel is S0 with disableLocalAuth parameterized; staging still compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Functions app → deployment storage container | Flex deployment auth via SystemAssignedIdentity (no connection string) |
| client → DocIntel (OCR) | S0 with local-auth disabled — Entra-only |
| internet → gateway | B1 App Service, httpsOnly/TLS1.2, single instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Spoofing | DocIntel local-auth key | mitigate | `docIntel.disableLocalAuth=true` in prod (Entra-only); Functions MI gets Cognitive Services User in plan 02-05 (INF-04) |
| T-02-13 | Tampering | Flex deployment storage auth | mitigate | `functionAppConfig.deployment.storage.authentication.type = 'SystemAssignedIdentity'` — no connection string; couples with `allowSharedKeyAccess=false` (plan 02-05) (INF-03/09) |
| T-02-14 | DoS / state-corruption | gateway multi-instance | mitigate | Explicit `sku.capacity=1` + Always On + `--workers 1` preserved; no autoscale>1 (AUD-04 invariant) (INF-02) |
| T-02-15 | Tampering | staging param regression | mitigate | Every prod behavior is a new param defaulting to the staging shape; `az bicep build` with default params confirms staging compiles |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase (RESEARCH line 142) |
</threat_model>

<verification>
- `az bicep build -f infra/main.bicep` exits 0 (functionAppConfig accepted).
- `scripts/validate_infra.ps1` passes the INF-02/03/04 assertions.
- `az bicep build` with default (staging) params exits 0.
- B1 capacity=1, Always On, gunicorn --workers 1, FC1, functionAppConfig, S0 all present (grep).
</verification>

<success_criteria>
- INF-02: gateway on B1, explicit capacity=1, Always On; AUD-04 pin preserved.
- INF-03: new Flex Consumption (FC1) Functions app with functionAppConfig (python 3.12).
- INF-04: DocIntel S0 + disableLocalAuth parameterized (West Europe fallback via docIntelLocation).
</success_criteria>

<artifacts_this_phase_produces>
**Modified file (this plan):**
- `infra/main.bicep` — compute section: B1 gwPlan (capacity=1) + alwaysOn; FC1 funcPlan + functionAppConfig functionApp; S0 docIntel + docIntelLocation.

**New Bicep params:** `funcMaxInstanceCount`, `funcInstanceMemoryMB`, `funcRuntimeVersion`, `deploymentContainerName`, `docIntelLocation`.

**Resolved open item:** minimum `Microsoft.Web/sites` apiVersion accepting `functionAppConfig` (Open Q2).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-production-infrastructure-provisioning/02-04-SUMMARY.md` when done.
</output>
