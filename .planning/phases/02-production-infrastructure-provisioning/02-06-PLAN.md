---
phase: 02-production-infrastructure-provisioning
plan: 06
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05"]
files_modified: []
autonomous: false
requirements: [INF-06]
must_haves:
  truths:
    - "An operator confirms EU data residency (M365 tenant geo, Fabric capacity region, Power Platform env region) and Flex + DocIntel S0 availability in France Central before any live provisioning"
    - "An operator runs and reviews a what-if against the live subscription as documented provisioning evidence (live apply remains Phase 3 / OPS-01)"
    - "An operator grants OBO admin consent in the production tenant and confirms the expected delegated Graph scopes resolve with no AADSTS65001"
    - "An operator confirms the Fabric/OneLake read grant mechanism for the Functions MI (workspace grant vs Azure RBAC)"
  artifacts:
    - path: ".planning/phases/02-production-infrastructure-provisioning/02-06-SUMMARY.md"
      provides: "Operator checkpoint evidence: residency confirmation, what-if diff, admin-consent grant, Fabric grant mechanism"
  key_links:
    - from: "operator"
      to: "live GEREP tenant + Azure subscription"
      via: "provision.ps1 pre-flight + az ad app permission admin-consent"
      pattern: "admin-consent"
---

<objective>
Run the locked operator checkpoints that cannot be executed without a live Azure subscription / GEREP tenant: confirm EU residency + region availability, run and review a Bicep what-if as provisioning evidence, grant OBO admin consent (INF-06) and verify no AADSTS65001, and confirm the Fabric/OneLake read grant mechanism for the Functions MI. These are the carried open-verification items; they gate go-live but do NOT block the IaC/script artifacts (already produced in waves 1-3). Live `az deployment` apply is deferred to Phase 3 / OPS-01.

Purpose: INF-06 (admin consent, blocking pre-flight) plus the residency/region/Fabric verifications that RESEARCH and STATE.md flagged as live-only. Each is a blocking operator checkpoint with explicit how-to-verify and resume-signal.
Output: `02-06-SUMMARY.md` capturing operator evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-production-infrastructure-provisioning/02-RESEARCH.md
@.planning/phases/02-production-infrastructure-provisioning/02-VALIDATION.md
@scripts/provision.ps1
@scripts/provision_app_registrations.ps1
@infra/main.bicep
@infra/prod.parameters.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm EU residency + Flex/DocIntel S0 availability in France Central</name>
  <files>infra/prod.parameters.json</files>
  <action>Operator checkpoint — no Claude action; requires a live GEREP tenant. The operator runs the provision.ps1 pre-flight to confirm Flex availability and DocIntel S0 in France Central, and manually confirms M365 tenant geo, Fabric capacity region, and Power Platform env region are EU, per the how-to-verify steps. If DocIntel S0 is absent in France Central, set docIntelLocation to westeurope (the locked fallback) in prod.parameters.json. Record all values in 02-06-SUMMARY.md. This addresses INF-01 / RGP-06 precursor residency.</action>
  <what-built>The `scripts/provision.ps1` pre-flight gates probe Flex region availability and DocIntel S0; the residency confirmations require live tenant inspection that no script can assert offline.</what-built>
  <how-to-verify>
    1. `az login` to the GEREP production subscription; confirm the subscription id matches the intended prod subscription.
    2. Run `scripts/provision.ps1 -ExpectedSubscription <prod-sub-id> -WhatIfOnly` and confirm the Flex pre-flight passes: `az functionapp list-flexconsumption-locations` includes `francecentral`. If it does NOT, STOP and escalate (no EU Flex fallback is chosen — RESEARCH Environment Availability).
    3. Probe DocIntel S0 in France Central. If S0 is unavailable there, set `docIntelLocation` to `westeurope` in `infra/prod.parameters.json` (the locked fallback) and note it — do NOT silently substitute any other region.
    4. Confirm EU residency manually: M365 tenant geo (Entra → tenant properties / Microsoft 365 admin center → data location), Fabric capacity region, and Power Platform environment region are all EU. Record each value.
  </how-to-verify>
  <verify>
    <human-check>Operator records, in 02-06-SUMMARY.md: Flex available in France Central (yes/no), DocIntel S0 region used (francecentral or westeurope fallback), and the confirmed EU region for M365 geo / Fabric capacity / Power Platform env.</human-check>
  </verify>
  <acceptance_criteria>
    - Flex availability in France Central confirmed (or escalation recorded).
    - DocIntel S0 region recorded; `docIntelLocation` set to `westeurope` only if S0 is absent in France Central.
    - M365 tenant geo, Fabric capacity region, and Power Platform env region each confirmed EU and recorded (INF-01 / RGP-06 precursor).
  </acceptance_criteria>
  <resume-signal>Type "residency-confirmed" with the recorded values, or describe the blocker (e.g., Flex absent → escalate).</resume-signal>
  <done>EU residency confirmed across M365/Fabric/Power Platform; Flex + DocIntel S0 availability in France Central confirmed (or West Europe DocIntel fallback applied); all values recorded in 02-06-SUMMARY.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run and review the Bicep what-if against the live subscription (provisioning evidence)</name>
  <files>infra/main.bicep, infra/prod.parameters.json</files>
  <action>Operator checkpoint — no Claude action; requires a live Azure subscription. The operator runs `az deployment group what-if` with prod.parameters.json against rg-ac360-prod, reviews the diff against the phase success criteria, and attaches the output as provisioning evidence. NO live apply this phase — apply is deferred to Phase 3 / OPS-01.</action>
  <what-built>`infra/main.bicep` + `infra/prod.parameters.json` compile clean offline (`az bicep build` + `scripts/validate_infra.ps1`); the what-if diff against the live subscription is the documented provisioning evidence and the phase gate.</what-built>
  <how-to-verify>
    1. Ensure `az login` + correct subscription (from Task 1).
    2. Run `az group create -n rg-ac360-prod -l francecentral` (or via `provision.ps1`).
    3. Run `az deployment group what-if -g rg-ac360-prod -f infra/main.bicep -p @infra/prod.parameters.json` (or `scripts/provision.ps1 -WhatIfOnly`).
    4. Review the diff: confirm B1 plan (capacity=1), FC1 Flex app (python 3.12, functionAppConfig), DocIntel S0 (disableLocalAuth), Standard_GRS storage with soft-delete/PITR, identity-based AzureWebJobsStorage, the Durable-trio + Cognitive Services User role assignments, the VNet + KV Private Endpoint + privatelink.vaultcore.azure.net zone. Confirm NO autoscale rule with maximum>1 on the gateway plan.
    5. Capture the what-if output and attach it as evidence (do NOT apply — live apply is Phase 3 / OPS-01).
  </how-to-verify>
  <verify>
    <human-check>Operator attaches/links the what-if output in 02-06-SUMMARY.md and confirms the prod shape matches the success criteria; no apply performed.</human-check>
  </verify>
  <acceptance_criteria>
    - `az deployment group what-if` runs successfully against the live subscription.
    - The diff shows the expected prod resources/SKUs and no AUD-04-violating autoscale.
    - What-if evidence is captured; no live apply executed this phase.
  </acceptance_criteria>
  <resume-signal>Type "what-if-reviewed" with the evidence attached, or describe discrepancies to fix in main.bicep.</resume-signal>
  <done>A what-if against the live subscription is reviewed and attached as evidence; the prod shape matches the success criteria; no live apply performed (deferred to Phase 3 / OPS-01).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Grant OBO admin consent and verify scopes (INF-06) + confirm Fabric/OneLake read grant</name>
  <files>scripts/provision_app_registrations.ps1</files>
  <action>Operator checkpoint — no Claude action; requires a Global/Privileged Role Administrator in the prod tenant. The operator runs provision_app_registrations.ps1 to create the prod app regs, reconciles the OBO delegated scope set against the live staging app, grants admin consent, verifies via list-grants (no AADSTS65001), and confirms the Fabric/OneLake read grant mechanism for the Functions MI. This closes INF-06 and the carried OBO-scope open item.</action>
  <what-built>`scripts/provision_app_registrations.ps1` creates the prod app regs, resolves + requests the delegated Graph scopes, writes the OBO secret to Key Vault, and issues `az ad app permission admin-consent`; the live consent grant + scope verification + Fabric grant require a privileged operator.</what-built>
  <how-to-verify>
    1. As a Global/Privileged Role Administrator, run `scripts/provision_app_registrations.ps1 -KeyVaultName <prod-kv>` to create `AC360-API-prod` (no secret) and `AC360-OBO-prod` (secret → Key Vault as `OBO-CLIENT-SECRET`).
    2. Confirm the exact consented delegated scope set against the live STAGING OBO app first (`az ad app permission list` on staging) and replicate to prod — verify the prod set matches: `Files.Read.All`, `Sites.Read.All`, `Tasks.ReadWrite`, `offline_access`, `User.Read` (adjust to whatever staging actually consented — this is the carried open item).
    3. Run / confirm `az ad app permission admin-consent --id <obo-appId>`, then `az ad app permission list-grants --id <obo-appId>` shows the expected scopes. A subsequent OBO smoke exchange (Phase 3) must NOT return AADSTS65001.
    4. Confirm the Fabric/OneLake read grant mechanism for the Functions MI: determine whether it is a Fabric workspace role assignment (Fabric portal/API) or an Azure RBAC role. If a workspace grant, perform it (or record the exact steps for the runbook) — it is NOT a Bicep roleAssignment.
    5. Confirm NO literal OBO secret was written to any file or log (only Key Vault).
  </how-to-verify>
  <verify>
    <human-check>Operator records in 02-06-SUMMARY.md: the confirmed prod delegated scope set, that `az ad app permission list-grants` shows them, no AADSTS65001 on smoke, and the Fabric/OneLake grant mechanism used for the Functions MI.</human-check>
  </verify>
  <acceptance_criteria>
    - Both prod app regs created idempotently; OBO secret in Key Vault only (INF-05).
    - Admin consent granted; `az ad app permission list-grants` shows the expected delegated scopes; no AADSTS65001 (INF-06).
    - The prod scope set is verified against the live staging OBO app (carried open item closed).
    - Fabric/OneLake read grant mechanism for the Functions MI confirmed and recorded (workspace grant vs Azure RBAC).
  </acceptance_criteria>
  <resume-signal>Type "consent-granted" with the scope list + Fabric grant mechanism recorded, or describe the AADSTS65001 / scope mismatch to resolve.</resume-signal>
  <done>OBO admin consent granted in the prod tenant; expected delegated scopes verified via list-grants with no AADSTS65001; Fabric/OneLake read grant mechanism for the Functions MI confirmed and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live GEREP tenant | Privileged admin grants consent; confirms residency |
| operator → live Azure subscription | what-if review (no apply this phase) |
| Functions MI → Fabric/OneLake | Data-plane / workspace grant confirmed live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-22 | Spoofing / DoS | missing/incorrect admin consent (AADSTS65001) | mitigate | Blocking operator checkpoint: `admin-consent` then `list-grants` verification; scope set reconciled against live staging app before prod (INF-06) |
| T-02-23 | Elevation of privilege | Fabric/OneLake over-grant | mitigate | Operator confirms the minimal read grant mechanism (workspace read vs Azure RBAC); recorded, not silently broadened (INF-07) |
| T-02-24 | Compliance | data residency violation | mitigate | Blocking residency confirmation (M365 geo / Fabric / Power Platform / Bicep locations EU) before any live provisioning (INF-01, RGP-06 precursor) |
| T-02-25 | Tampering / wrong deploy | unreviewed live apply | mitigate | What-if review is a mandatory gate; live apply explicitly deferred to Phase 3 / OPS-01 (no apply this phase) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages installed this phase (RESEARCH line 142) |
</threat_model>

<verification>
- Operator checkpoints completed and evidence recorded in 02-06-SUMMARY.md.
- Residency + Flex/S0 availability confirmed; what-if reviewed (no apply); admin consent granted (no AADSTS65001); Fabric grant mechanism confirmed.
</verification>

<success_criteria>
- INF-06: OBO admin consent granted in the prod tenant; expected delegated scopes verified; no AADSTS65001.
- EU residency + France Central Flex/DocIntel S0 availability confirmed (West Europe DocIntel fallback applied only if needed).
- What-if reviewed and attached as provisioning evidence; live apply deferred to Phase 3.
- Fabric/OneLake read grant mechanism for the Functions MI confirmed.
</success_criteria>

<artifacts_this_phase_produces>
**Operator evidence (this plan):**
- `02-06-SUMMARY.md` — residency confirmation values, what-if diff/evidence link, admin-consent + scope-grant verification, Fabric/OneLake grant mechanism.

**No code/IaC files modified** — this plan is operator checkpoints over the artifacts produced in waves 1-3.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-production-infrastructure-provisioning/02-06-SUMMARY.md` when done.
</output>
