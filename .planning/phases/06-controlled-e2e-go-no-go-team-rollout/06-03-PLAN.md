---
phase: 06-controlled-e2e-go-no-go-team-rollout
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/e2e_smoke.py
autonomous: true
requirements: [GO-01]
must_haves:
  truths:
    - "The harness builds a synthetic /api/audit request, reads job_id, polls status, and classifies the verdict per scenario"
    - "All five scenarios (CONFORME happy + CLIENT_NON_TROUVE + ECART+FIC + OCR-timeout + Fabric-down) are scripted with expected outcomes"
    - "The harness is offline-unit-testable with injected HTTP fakes (NO live call) and uses clearly-synthetic data only"
  artifacts:
    - path: "scripts/e2e_smoke.py"
      provides: "Env-parameterized prod E2E driver (audit→status→result) with per-scenario verdict classification, injectable http_post/http_get, post-run no-PII KQL pointer"
      contains: "CLIENT_NON_TROUVE"
  key_links:
    - from: "scripts/e2e_smoke.py"
      to: "prod gateway /api/audit + /api/audit/{job_id}/status"
      via: "env-parameterized base URL + bearer token; injectable http_post/http_get for offline tests"
      pattern: "/api/audit"
    - from: "scripts/e2e_smoke.py"
      to: "docs/security/GUARDRAILS_VALIDATION.md §2"
      via: "post-run no-PII telemetry (KQL) check pointer"
      pattern: "GUARDRAILS_VALIDATION"
---

<objective>
GREEN GO-01 (harness): create scripts/e2e_smoke.py — a synthetic-data E2E driver that hits the real prod gateway (`/api/audit` → poll `/api/audit/{job_id}/status` → result) env-parameterized by gateway URL + a test bearer token, classifies the returned verdict against the per-scenario expectation, and emits a post-run no-PII telemetry check pointer (KQL) linked to GUARDRAILS_VALIDATION §2. HTTP is injectable (`http_post`/`http_get`) so the logic is unit-tested offline against mocked responses with NO live call. The live run is an operator checkpoint (06-04).

Purpose: prove the full stack across happy + failure paths on synthetic data, and make the no-PII telemetry assertion the live proof of AUD-06/RGP-04. Turns the 06-01 E2E RED spec GREEN.
Output: scripts/e2e_smoke.py (driver + scenario map + classifier + no-PII check pointer).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-controlled-e2e-go-no-go-team-rollout/06-CONTEXT.md
@scripts/run_demo.py
@tests/backend/test_e2e_smoke.py
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/e2e_smoke.py — synthetic E2E driver + scenario verdict classifier (offline-tested)</name>
  <read_first>
    - tests/backend/test_e2e_smoke.py (the RED spec from 06-01) — it defines the harness API: `run_scenario(scenario, *, http_post, http_get)`, the five scenario keys, the status-payload fields consumed, and the mismatch FAIL case. Implement to satisfy these asserts.
    - scripts/run_demo.py (analog: argparse `main`, injectable `printer`, embedded synthetic fixtures `_DEMO_OCR`/`_DEMO_REF`, `from __future__ import annotations`, sys.path bootstrap for direct + imported execution).
    - scripts/api_server.py lines 356-464 + 720-753 + `_shape_status_response` lines 330-353 — the exact request/response contract: POST `/api/audit` body `{document_id, client_context}` → `{status:"accepted", job_id, statusQueryGetUri}`; GET `/api/audit/{job_id}/status` → `{job_id, status, verdict, client_document, fic_available, audit_status, error}`.
    - docs/security/GUARDRAILS_VALIDATION.md §2 (the live no-PII telemetry evidence row, AUD-06/GO-01) — the harness's post-run check points here.
  </read_first>
  <behavior>
    - SCENARIOS map (5 entries) with synthetic request inputs + expected outcomes: "conforme"→CONFORME, "client_non_trouve"→CLIENT_NON_TROUVE, "ecart_fic"→ECART (and expects fic_available True), "ocr_timeout"→expected FAILED/error (not a verdict), "fabric_down"→expected INCERTAIN-or-error. Synthetic identifiers only (e.g. client "TESTCLIENT_SYNTH", a clearly-fake SharePoint document id) — documented as fake in the module docstring.
    - `run_scenario(scenario, *, http_post, http_get, base_url=None, token=None)` POSTs the synthetic audit request, reads `job_id` from the response, polls the status endpoint via `http_get` (bounded attempts; in tests the fake returns Completed immediately), and returns a result object with {scenario, expected, actual, passed: bool}.
    - The classifier sets passed=True when actual matches expected (verdict for the 3 verdict scenarios; a FAILED/error result for the 2 failure scenarios) and passed=False on mismatch — a discriminating check, not always-pass.
    - `main()` (argparse): reads `--base-url` and a token from env (e.g. `AC360_E2E_GATEWAY_URL` / `AC360_E2E_TOKEN`), runs all scenarios against the LIVE gateway using a real httpx-backed http_post/http_get default, prints a per-scenario PASS/FAIL report (injectable `printer`), and prints the no-PII KQL pointer + GUARDRAILS_VALIDATION §2 cross-reference at the end. The live run path is exercised only by the operator (06-04), never in CI.
    - The default http_post/http_get use httpx; tests inject fakes so NO network call happens offline.
  </behavior>
  <action>
    Implement the driver mirroring run_demo.py conventions (argparse main, injectable seams, sys.path bootstrap, `from __future__ import annotations`, full type hints). Keep the HTTP I/O behind injectable `http_post`/`http_get` params (DI seam exactly like graph_obo's `http_post`) so the 06-01 test drives it with `_FakeResp` fakes and zero network. Use clearly-synthetic request data only (no real client name/doc) and state that in the module docstring (T-06-02). Print the post-run no-PII check as a KQL query string the operator runs against App Insights, with a comment pointing to docs/security/GUARDRAILS_VALIDATION.md §2 (AUD-06/RGP-04). Do NOT make any live call at import or under pytest. No fenced code blocks in this plan — name the function signatures, scenario keys, verdict literals, env var names, and the §2 cross-reference above; the executor writes the code.
  </action>
  <verify>
    <automated>python -m pytest tests/backend/test_e2e_smoke.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/backend/test_e2e_smoke.py -x` exits 0 (GREEN — the 06-01 RED spec now passes; mocked HTTP, no live call).
    - File exists and is importable offline without network: `python -c "import sys; sys.path.insert(0,'scripts'); import e2e_smoke"` exits 0.
    - All five scenarios present: `grep CLIENT_NON_TROUVE scripts/e2e_smoke.py`, `grep CONFORME scripts/e2e_smoke.py`, `grep ECART scripts/e2e_smoke.py`, `grep ocr_timeout scripts/e2e_smoke.py`, `grep fabric_down scripts/e2e_smoke.py` all match.
    - Injectable HTTP seam: `grep "http_post" scripts/e2e_smoke.py` and `grep "http_get" scripts/e2e_smoke.py` match (DI for offline testing).
    - No-PII evidence link present: `grep GUARDRAILS_VALIDATION scripts/e2e_smoke.py` matches (the post-run KQL no-PII check points to §2).
    - Synthetic-only: `grep -i synth scripts/e2e_smoke.py` matches; no real client name committed.
  </acceptance_criteria>
  <done>scripts/e2e_smoke.py drives audit→status→result env-parameterized, classifies all five synthetic scenarios (CONFORME / CLIENT_NON_TROUVE / ECART+FIC / OCR-timeout / Fabric-down) with discriminating pass/fail, exposes injectable http_post/http_get for offline testing (the 06-01 spec is GREEN, no live call), and prints a post-run no-PII KQL check linked to GUARDRAILS_VALIDATION §2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e_smoke → prod gateway | The driver crosses into prod over Entra-gated TLS with a test bearer token (operator-supplied at run time, never committed) |
| e2e_smoke → App Insights (KQL no-PII check) | The post-run check reads telemetry to prove no PII leaked — live proof of AUD-06/RGP-04 |
| test harness → injected fakes | Offline path: fakes replace the gateway; no network, no real PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information disclosure | synthetic E2E data + telemetry | mitigate | clearly-fake client/doc only (docstring + grep gate); post-run KQL no-PII check (→ GUARDRAILS_VALIDATION §2) confirms App Insights traces are redacted |
| T-06-06 | Information disclosure | test token in env | mitigate | token read from env (AC360_E2E_TOKEN), never hardcoded/committed; default httpx path only runs under operator `main`, never in pytest |
| T-06-07 | Tampering | live call under CI | mitigate | http_post/http_get injectable; no live call at import or under pytest; the live run is the 06-04 operator checkpoint |
| T-06-SC | Tampering | dependency installs | accept | httpx already a project dependency; no new package |
</threat_model>

<verification>
- `python -m pytest tests/backend/test_e2e_smoke.py -x` GREEN.
- `python -m pytest tests/backend tests/security -x` GREEN (no plaintext-secret leak; the module imports clean, no network at import).
- Greps in acceptance_criteria pass (five scenarios, http_post/http_get seam, GUARDRAILS_VALIDATION link, synthetic marker).
- No live network call occurs during any automated run.
</verification>

<success_criteria>
scripts/e2e_smoke.py proves the full audit→status→result flow against the prod contract across the happy CONFORME path and the four failure paths on synthetic data, classifies each verdict discriminatingly, is offline-unit-tested with injected HTTP (no live call), and carries the post-run no-PII telemetry check linked to GUARDRAILS_VALIDATION §2. GO-01 harness offline-verified; the live run is the 06-04 operator checkpoint.
</success_criteria>

<output>
Create `.planning/phases/06-controlled-e2e-go-no-go-team-rollout/06-03-SUMMARY.md` when done.
</output>
