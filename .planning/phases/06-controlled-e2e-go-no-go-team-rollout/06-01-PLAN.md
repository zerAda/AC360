---
phase: 06-controlled-e2e-go-no-go-team-rollout
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/backend/test_feature_flags_allowlist.py
  - tests/backend/test_e2e_smoke.py
autonomous: true
requirements: [GO-01, GO-02]
must_haves:
  truths:
    - "Allowlist UNSET asserts as no-restriction (backward-compat) before any allowlist code exists"
    - "Allowlist SET asserts as deny-by-default for unlisted users/teams (RED, fails until 06-02)"
    - "E2E harness request-build + verdict-classification asserted against mocked HTTP (RED, fails until 06-03)"
  artifacts:
    - path: "tests/backend/test_feature_flags_allowlist.py"
      provides: "RED allowlist fail-safe spec (unset=allow, set=deny-unlisted)"
      contains: "AC360_ALLOWED_TEAMS"
    - path: "tests/backend/test_e2e_smoke.py"
      provides: "RED E2E harness spec (mocked HTTP, no live call)"
      contains: "CLIENT_NON_TROUVE"
  key_links:
    - from: "tests/backend/test_feature_flags_allowlist.py"
      to: "scripts/feature_flags.py"
      via: "import feature_flags + monkeypatch AC360_ALLOWED_* env"
      pattern: "AC360_ALLOWED_(TEAMS|USERS_HASHED)"
    - from: "tests/backend/test_e2e_smoke.py"
      to: "scripts/e2e_smoke.py"
      via: "import e2e_smoke with injected http_post/http_get fakes"
      pattern: "e2e_smoke"
---

<objective>
Wave 0 RED: author the two failing test scaffolds that pin Phase 6's offline contracts as executable specs BEFORE any implementation — the allowlist fail-safe behavior (GO-02) and the E2E harness request-build + verdict-classification logic (GO-01). Both files import modules that do not yet exist (or behavior that does not yet exist), so the suite is RED on this wave by design.

Purpose: lock the deny-by-default-when-set / no-restriction-when-unset fail-safe semantics and the synthetic-scenario verdict map as tests first, so 06-02 and 06-03 are GREEN-driven and the FAIL-OPEN/FAIL-CLOSED correctness threat (T-06-01) is caught by a test, not by inspection.
Output: tests/backend/test_feature_flags_allowlist.py, tests/backend/test_e2e_smoke.py (both RED).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-controlled-e2e-go-no-go-team-rollout/06-CONTEXT.md
@.planning/phases/06-controlled-e2e-go-no-go-team-rollout/06-VALIDATION.md
@scripts/feature_flags.py
@tests/backend/test_graph_obo.py
@tests/backend/conftest.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — allowlist fail-safe spec (tests/backend/test_feature_flags_allowlist.py)</name>
  <read_first>
    - scripts/feature_flags.py (current block-list model: `is_allowed`, `is_user_blocked`, `is_team_blocked`, `_csv_set`, `hash_id`, `_GLOBAL_ENV`, `FEATURE_ENV`) — the allowlist will be added alongside these.
    - tests/backend/test_graph_obo.py (analog: monkeypatch-free DI/fake style; here use pytest `monkeypatch.setenv`/`delenv` for env-gated flags as in `test_obo_configured_reflects_env`).
    - tests/backend/conftest.py (PYTHONPATH wiring: `scripts/` on sys.path; absolute `import feature_flags`).
  </read_first>
  <behavior>
    - UNSET (both AC360_ALLOWED_TEAMS and AC360_ALLOWED_USERS_HASHED absent/empty): is_allowed("audit", user_id_hash=any, team_id=any) returns (True, None) — backward-compatible no-restriction (FAIL-SAFE: an unset allowlist must NOT lock anyone out).
    - SET team allowlist (AC360_ALLOWED_TEAMS="team-target"): a listed team_id is allowed; an unlisted team_id returns (False, "team_not_allowlisted") — deny-by-default.
    - SET user allowlist (AC360_ALLOWED_USERS_HASHED="<hash_id('oid-pilot')>"): a listed user_id_hash is allowed; an unlisted hash returns (False, "user_not_allowlisted") — deny-by-default.
    - Precedence: an existing block (AC360_BLOCKED_TEAMS / global-disabled) still denies even when the entity is allowlisted (block overrides allow) — no regression to the existing kill-switch.
    - blocked_message("team_not_allowlisted") and blocked_message("user_not_allowlisted") return non-empty French strings (no KeyError, falls through to the default if not yet mapped).
  </behavior>
  <action>
    Author the failing test module. Use pytest `monkeypatch` to set/delete AC360_ALLOWED_TEAMS and AC360_ALLOWED_USERS_HASHED per the `test_obo_configured_reflects_env` env-toggle analog. Import `feature_flags` absolutely (conftest puts scripts/ on sys.path). Compute pilot hashes via `feature_flags.hash_id("oid-pilot")` so the test mirrors the api_server call site (`hash_id(oid)`). Assert the five behaviors above against `feature_flags.is_allowed` and `feature_flags.blocked_message`. The new reason codes "team_not_allowlisted" / "user_not_allowlisted" do not exist yet, so the deny-by-default assertions FAIL — this is the intended RED. Do NOT add a sleep/network call; pure env-driven. No fenced implementation of feature_flags here — only the test.
  </action>
  <verify>
    <automated>python -m pytest tests/backend/test_feature_flags_allowlist.py -x ; if ($LASTEXITCODE -eq 0) { Write-Error "Expected RED (non-zero) before 06-02 lands the allowlist" }</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `Test-Path tests/backend/test_feature_flags_allowlist.py` is True.
    - Contains the env var names: `grep AC360_ALLOWED_TEAMS tests/backend/test_feature_flags_allowlist.py` and `grep AC360_ALLOWED_USERS_HASHED tests/backend/test_feature_flags_allowlist.py` each match.
    - The unset-allows assertion and the set-denies-unlisted assertion are both present (grep `team_not_allowlisted` and an assert on `(True, None)` for the unset case).
    - `python -m pytest tests/backend/test_feature_flags_allowlist.py -x` exits NON-ZERO on this wave (RED — deny-by-default reasons not yet implemented). This is the pass condition for Wave 0.
  </acceptance_criteria>
  <done>The allowlist fail-safe spec exists, names both env vars, asserts unset=allow and set=deny-unlisted, and fails (RED) because the allowlist code is not yet written.</done>
</task>

<task type="tdd">
  <name>Task 2: RED — E2E harness spec (tests/backend/test_e2e_smoke.py)</name>
  <read_first>
    - scripts/api_server.py lines 356-464 (`/api/audit` POST returns `{status:"accepted", job_id, statusQueryGetUri, requested_by}`) and lines 720-753 + `_shape_status_response` lines 330-353 (`/api/audit/{job_id}/status` returns `{job_id, status, verdict, client_document, fic_available, error, ...}`). These are the exact response shapes the harness consumes.
    - scripts/run_demo.py (analog E2E driver: argparse main, injectable `printer`, synthetic embedded fixtures `_DEMO_OCR`/`_DEMO_REF` — clearly-fake "GEREP SA" / "Contrat_DEMO_2026.pdf").
    - tests/backend/test_graph_obo.py (`_FakeResp`, `_sequence_post` DI seam — the harness must accept injected `http_post`/`http_get` so the test does NO live call).
    - scripts/fabric_audit_engine.py lines 294-309 (verdict literals: CONFORME / ECART / INCERTAIN / CLIENT_NON_TROUVE).
  </read_first>
  <behavior>
    - run_scenario(scenario, *, http_post, http_get) POSTs a synthetic audit request (clearly-fake client/document id, e.g. "TESTCLIENT_SYNTH" / a fake SharePoint doc id), reads job_id from the POST response, then polls the status endpoint via http_get and returns the classified verdict.
    - Happy path scenario "conforme": mocked status returns verdict="CONFORME" → harness reports PASS with expected="CONFORME".
    - Scenario "client_non_trouve": status verdict="CLIENT_NON_TROUVE" → PASS with expected="CLIENT_NON_TROUVE".
    - Scenario "ecart_fic": status verdict="ECART" and fic_available=True → PASS, and the harness asserts fic_available is True for this scenario.
    - Failure scenario "ocr_timeout": the audit fails (status="Failed" / audit_status surfaces an OCR timeout) → harness classifies as the documented expected outcome (a FAILED/error result, NOT a verdict) and reports it as an expected-failure PASS.
    - Failure scenario "fabric_down": dependency-down surfaces as INCERTAIN or an error result → harness reports the documented expected outcome.
    - Verdict mismatch (expected≠actual) → harness reports FAIL for that scenario (a discriminating assert, not always-pass).
    - The whole flow runs with injected fakes — zero real network. (The live run is an operator checkpoint, planned in 06-04.)
  </behavior>
  <action>
    Author the failing test module importing `e2e_smoke` (does not exist yet). Build `_FakeResp`-style fakes (reuse the shape from test_graph_obo) and a sequenced `http_get` that returns the per-scenario status payloads keyed to `_shape_status_response`'s fields (`status`, `verdict`, `fic_available`, `audit_status`, `error`). Drive each of the five scenarios (conforme, client_non_trouve, ecart_fic, ocr_timeout, fabric_down) plus one mismatch case proving the classifier can FAIL. Assert the harness reads `job_id` from the POST response and polls the status URL. Use clearly-synthetic identifiers in the request (document the fake-data intent in a module docstring — NO real client PII). Import absolutely (conftest adds scripts/). No fenced implementation of e2e_smoke — only the test.
  </action>
  <verify>
    <automated>python -m pytest tests/backend/test_e2e_smoke.py -x ; if ($LASTEXITCODE -eq 0) { Write-Error "Expected RED (non-zero) before 06-03 lands e2e_smoke.py" }</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `Test-Path tests/backend/test_e2e_smoke.py` is True.
    - Covers all five scenarios + a mismatch: grep finds `CONFORME`, `CLIENT_NON_TROUVE`, `ECART`, `ocr_timeout`, `fabric_down` in the file.
    - Uses injected fakes (no real network): grep finds `http_get` and `http_post` injected into the harness call; grep finds NO `httpx.post(` / `httpx.get(` direct live call in the test.
    - Synthetic-only marker present: grep finds `synth` (case-insensitive) or a "fake"/"synthetic" docstring note; no real client name committed.
    - `python -m pytest tests/backend/test_e2e_smoke.py -x` exits NON-ZERO (RED — `import e2e_smoke` fails / harness absent). This is the pass condition for Wave 0.
  </acceptance_criteria>
  <done>The E2E harness spec exists, covers the happy CONFORME path + CLIENT_NON_TROUVE + ECART(+FIC) + OCR-timeout + Fabric-down + a mismatch FAIL, uses injected HTTP fakes (no live call) with synthetic-only data, and fails (RED) because scripts/e2e_smoke.py does not yet exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → feature_flags env | Tests drive AC360_ALLOWED_* via monkeypatch; the fail-safe contract is asserted here, not in prod |
| test harness → e2e_smoke injected HTTP | Fakes stand in for the prod gateway; NO real network or real PII crosses this boundary offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation | allowlist fail-open/fail-closed | mitigate | This wave's RED spec asserts BOTH unset=no-restriction (no lockout) AND set=deny-unlisted (no over-exposure); 06-02 must turn it GREEN |
| T-06-02 | Information disclosure | synthetic E2E data | mitigate | test_e2e_smoke uses clearly-fake identifiers only; grep gate forbids real client names; no live call offline |
| T-06-03 | Tampering | test importing non-existent modules | accept | RED-by-design on Wave 0; the suite is expected non-zero until 06-02/06-03 land — documented, not a defect |
</threat_model>

<verification>
- `python -m pytest tests/backend/test_feature_flags_allowlist.py tests/backend/test_e2e_smoke.py -x` runs and is RED (non-zero) — both specs collected, both fail because implementation is absent.
- Structural greps in each task's acceptance_criteria pass (env var names, scenario literals, injected-fake usage, synthetic-only marker).
- No real network or real client PII in either test file.
</verification>

<success_criteria>
Both RED scaffolds exist, are collected by pytest, assert the load-bearing fail-safe and verdict-classification behaviors, and fail because 06-02 (allowlist) and 06-03 (e2e_smoke) have not yet been implemented. Wave 0 complete.
</success_criteria>

<output>
Create `.planning/phases/06-controlled-e2e-go-no-go-team-rollout/06-01-SUMMARY.md` when done.
</output>
