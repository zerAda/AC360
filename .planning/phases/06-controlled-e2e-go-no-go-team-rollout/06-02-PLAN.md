---
phase: 06-controlled-e2e-go-no-go-team-rollout
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/feature_flags.py
autonomous: true
requirements: [GO-02]
must_haves:
  truths:
    - "When an allowlist env var is SET, only listed teams/users are allowed (deny-by-default)"
    - "When both allowlist env vars are UNSET, behavior is unchanged (no-restriction, backward-compatible) — no lockout"
    - "An existing block (global-disabled / blocked team/user) still denies even an allowlisted entity"
  artifacts:
    - path: "scripts/feature_flags.py"
      provides: "Allowlist mode (AC360_ALLOWED_TEAMS / AC360_ALLOWED_USERS_HASHED), deny-by-default when set, no-restriction when unset, woven into is_allowed"
      contains: "AC360_ALLOWED_TEAMS"
  key_links:
    - from: "scripts/feature_flags.py is_allowed"
      to: "AC360_ALLOWED_TEAMS / AC360_ALLOWED_USERS_HASHED"
      via: "_csv_set + allowlist check after the block checks"
      pattern: "AC360_ALLOWED_(TEAMS|USERS_HASHED)"
    - from: "scripts/api_server.py /api/audit"
      to: "scripts/feature_flags.py is_allowed"
      via: "existing is_allowed('audit', user_id_hash=hash_id(oid)) call site — signature unchanged"
      pattern: "is_allowed\\(\"audit\""
---

<objective>
GREEN GO-02: add an allowlist mode to scripts/feature_flags.py — AC360_ALLOWED_TEAMS and AC360_ALLOWED_USERS_HASHED — that is deny-by-default when set (only listed teams/users pass) and no-restriction when unset (current block-list behavior preserved, zero regression). The check is woven into the existing `is_allowed` flow AFTER the global/feature/block gates, so a block always overrides an allow. The existing api_server call site `is_allowed("audit", user_id_hash=hash_id(oid))` is unchanged — the new gate piggybacks on the same signature.

Purpose: this is the launch gating lever (GO-02) — restrict access to exactly the target team at launch — and it is the highest-stakes correctness item in the phase: an unset allowlist must NOT brick the service, and a set allowlist must NOT leak to everyone (T-06-01). The 06-01 RED spec turns GREEN here.
Output: scripts/feature_flags.py (allowlist mode + reason codes + messages).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-controlled-e2e-go-no-go-team-rollout/06-CONTEXT.md
@scripts/feature_flags.py
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allowlist mode in feature_flags.py (deny-by-default when set, no-restriction when unset)</name>
  <read_first>
    - scripts/feature_flags.py in full — extend it: reuse `_csv_set`, `hash_id`, the `_BLOCKED_*_ENV` constant pattern, the `is_allowed` ordering (global → feature → user_blocked → team_blocked → return True), and the `_USER_MESSAGES` map. Mirror the block-list constants for the allowlist constants.
    - tests/backend/test_feature_flags_allowlist.py (the RED spec from 06-01) — it defines the exact reason codes ("team_not_allowlisted", "user_not_allowlisted"), the env var names, and the precedence (block overrides allow). Implement to satisfy these asserts.
    - .planning/codebase/CONVENTIONS.md (snake_case, `from __future__ import annotations`, UPPERCASE module constants, mypy-strict module — feature_flags.py is in the strict list, so keep full type hints: `Optional[str]`, return `Tuple[bool, Optional[str]]`).
  </read_first>
  <behavior>
    - Add module constants `_ALLOWED_TEAMS_ENV = "AC360_ALLOWED_TEAMS"` and `_ALLOWED_USERS_ENV = "AC360_ALLOWED_USERS_HASHED"`.
    - Add `is_team_allowlisted(team_id) -> bool` and `is_user_allowlisted(user_id_hash) -> bool` that: if the corresponding env CSV set is EMPTY → return True (no restriction / fail-safe backward-compat); if NON-EMPTY → return membership (deny-by-default). A None/empty identifier against a non-empty allowlist returns False (cannot be on the list).
    - In `is_allowed`, AFTER the existing global/feature/user_blocked/team_blocked checks (so a block overrides allow), add: if not is_team_allowlisted(team_id) → (False, "team_not_allowlisted"); if not is_user_allowlisted(user_id_hash) → (False, "user_not_allowlisted"); else fall through to (True, None).
    - Add French messages for "team_not_allowlisted" and "user_not_allowlisted" to `_USER_MESSAGES`.
    - Keep the existing public signatures byte-identical (api_server calls `is_allowed("audit", user_id_hash=...)` with no team_id — that path must still return (True, None) when no allowlist is set, and respect a user allowlist when one is set even if team_id is None).
  </behavior>
  <action>
    Implement the allowlist exactly as the 06-01 spec asserts. Critical fail-safe rule (T-06-01): EMPTY set ⇒ allow (no lockout, backward-compat); NON-EMPTY set ⇒ deny-by-default. Place the allowlist checks LAST in `is_allowed` so the existing kill-switch (global-disabled, blocked user/team) always wins. Reuse `_csv_set` for parsing and `hash_id` is already the user-hash basis (api_server passes `hash_id(oid)`; AC360_ALLOWED_USERS_HASHED holds those same hashes). Keep mypy-strict types. Do not change `is_user_blocked`/`is_team_blocked`/`is_feature_enabled`. No fenced code blocks — name the constants, functions, reason codes, and ordering above; the executor writes the code.
  </action>
  <verify>
    <automated>python -m pytest tests/backend/test_feature_flags_allowlist.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/backend/test_feature_flags_allowlist.py -x` exits 0 (GREEN — the 06-01 RED spec now passes).
    - `grep AC360_ALLOWED_TEAMS scripts/feature_flags.py` and `grep AC360_ALLOWED_USERS_HASHED scripts/feature_flags.py` both match.
    - allowlist-unset-allows asserted: with both env vars unset, `is_allowed("audit", user_id_hash=hash_id("anyone"))` returns (True, None) (covered by the GREEN test).
    - allowlist-set-denies-unlisted asserted: with AC360_ALLOWED_TEAMS set to one team, an unlisted team returns (False, "team_not_allowlisted") (covered by the GREEN test).
    - No regression: `python -m pytest tests/backend -x -k "feature_flags or admin_controls"` exits 0 (existing block-list tests still pass).
    - mypy clean on the module: `python -m mypy scripts/feature_flags.py` exits 0 (strict-list module).
  </acceptance_criteria>
  <done>feature_flags.py has allowlist mode: unset=no-restriction (no lockout), set=deny-by-default, block-overrides-allow; the 06-01 allowlist spec is GREEN; existing block-list tests and mypy still pass; the api_server `is_allowed` call site is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app settings (env) → feature_flags | An admin sets AC360_ALLOWED_TEAMS to gate launch; a commercial cannot self-allowlist (env is admin-only, per 05-killswitch) |
| feature_flags is_allowed → api_server /api/audit | The gate decision crosses into the request path; a wrong decision either bricks the team or exposes everyone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | allowlist deny-by-default correctness | mitigate | EMPTY set ⇒ allow (no lockout); NON-EMPTY set ⇒ deny-unlisted; asserted by the 06-01 GREEN test (unset-allows + set-denies-unlisted) |
| T-06-04 | Spoofing | user allowlist via hash | mitigate | AC360_ALLOWED_USERS_HASHED holds `hash_id(oid)` SHA-256 hashes (not raw UPN/oid); reuses the AUD-02 oid-based hashing, no PII in env |
| T-06-05 | Denial of Service | block bypass via allowlist | mitigate | Allowlist checks run LAST in is_allowed, so global-disabled / blocked team/user always override an allow (kill-switch precedence preserved) |
| T-06-SC | Tampering | dependency installs | accept | No new packages this plan (pytest/mypy already present); nothing installed |
</threat_model>

<verification>
- `python -m pytest tests/backend/test_feature_flags_allowlist.py -x` GREEN.
- `python -m pytest tests/backend tests/security -x` GREEN (no regression in block-list, no plaintext-secret leakage in env handling).
- `python -m mypy scripts/feature_flags.py` exits 0.
- `grep AC360_ALLOWED_TEAMS scripts/feature_flags.py` matches; the allowlist checks are positioned after the block checks in `is_allowed`.
</verification>

<success_criteria>
The allowlist gating is implemented and proven fail-safe by the now-GREEN 06-01 spec: unset = no restriction (backward-compatible, no team is locked out), set = deny-by-default (only the target team/users allowed), and a block still overrides an allow. GO-02 offline-verified.
</success_criteria>

<output>
Create `.planning/phases/06-controlled-e2e-go-no-go-team-rollout/06-02-SUMMARY.md` when done.
</output>
