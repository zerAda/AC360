---
phase: 06-controlled-e2e-go-no-go-team-rollout
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - docs/production/runbooks/07-go-no-go-checklist.md
  - docs/production/runbooks/08-gradual-rollout.md
autonomous: false
requirements: [GO-01, GO-02, GO-03, GO-04]
must_haves:
  truths:
    - "A Go/No-Go checklist exists that aggregates every prior-phase gate plus an operator sign-off block"
    - "A gradual-rollout runbook exists: pilot 2-5 → 24-48h clean signal → full team, with explicit clean-signal criteria and an abort path"
    - "The live E2E run, the no-PII telemetry check, the operator Go/No-Go sign-off, and the rollout are blocking operator checkpoints (gated on DPO DPIA sign-off + the live stack)"
  artifacts:
    - path: "docs/production/runbooks/07-go-no-go-checklist.md"
      provides: "Consolidated go-live punch-list (deploy verified, alerts firing, DPIA DPO-signed, E2E green+no-PII, gating to target team) + sign-off block"
      contains: "DPIA"
    - path: "docs/production/runbooks/08-gradual-rollout.md"
      provides: "Gradual-rollout runbook (pilot→clean-signal→full) using the allowlist lever, clean-signal criteria, abort → 02-rollback + 05-killswitch"
      contains: "AC360_ALLOWED_TEAMS"
  key_links:
    - from: "docs/production/runbooks/08-gradual-rollout.md"
      to: "scripts/feature_flags.py allowlist (AC360_ALLOWED_TEAMS / AC360_ALLOWED_USERS_HASHED)"
      via: "the rollout lever — pilot cohort via AC360_ALLOWED_USERS_HASHED, full team via AC360_ALLOWED_TEAMS"
      pattern: "AC360_ALLOWED_(TEAMS|USERS_HASHED)"
    - from: "docs/production/runbooks/08-gradual-rollout.md"
      to: "docs/production/runbooks/02-rollback.md + 05-killswitch.md"
      via: "abort path on failed clean signal"
      pattern: "(02-rollback|05-killswitch)"
    - from: "docs/production/runbooks/07-go-no-go-checklist.md"
      to: "scripts/e2e_smoke.py + GUARDRAILS_VALIDATION.md §2"
      via: "E2E-green + no-PII-telemetry gate rows"
      pattern: "e2e_smoke"
---

<objective>
Author the two go-live documents and gate the live actions: docs/production/runbooks/07-go-no-go-checklist.md (GO-03) — the single consolidated punch-list aggregating every prior-phase gate plus an operator sign-off block — and docs/production/runbooks/08-gradual-rollout.md (GO-04) — pilot cohort 2-5 → 24-48h clean signal → full team, using the 06-02 allowlist as the lever, with explicit clean-signal criteria and an abort path to 02-rollback + 05-killswitch. Then place the BLOCKING operator checkpoints for the live controlled E2E run + no-PII telemetry check (GO-01 live), the Go/No-Go sign-off (GO-03), and the gradual rollout (GO-04) — none of which executes here (gated on the Phase 5 DPO DPIA sign-off + the full live stack from Phases 2-4).

Purpose: convert the offline-built artifacts into an operator-executable go-live, while hard-gating the live, PII-touching, time-based steps behind explicit human checkpoints so nothing goes live before the DPIA is signed.
Output: 07-go-no-go-checklist.md, 08-gradual-rollout.md, and the blocking operator checkpoints.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-controlled-e2e-go-no-go-team-rollout/06-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/production/runbooks/02-rollback.md
@docs/production/runbooks/05-killswitch.md
@docs/security/GUARDRAILS_VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Go/No-Go checklist (07) + gradual-rollout runbook (08)</name>
  <read_first>
    - docs/production/runbooks/05-killswitch.md (the env-driven flag table + admin-only app-settings model; the rollout runbook reuses this lever and abort path; note the existing 06-copilot-publish.md so the new files are numbered 07/08).
    - docs/production/runbooks/02-rollback.md (the <10-min tag-redeploy rollback + its decision triggers; the rollout abort path references it).
    - .planning/REQUIREMENTS.md GO-01..04 + Phase 1-5 requirement IDs (the checklist aggregates: AUD-* fixes, INF-* infra, OBS-* alerts/dashboard, CD-* pipeline, OPS-* runbooks, PUB-* publish, SEC-*/RGP-* evidence incl. the RGP-02 DPIA DPO sign-off, GO-01 E2E+no-PII, GO-02 gating).
    - docs/security/GUARDRAILS_VALIDATION.md §2 (the live no-PII evidence row the checklist references) and the 06-02/06-03 artifacts (AC360_ALLOWED_TEAMS, scripts/e2e_smoke.py) the docs point at.
  </read_first>
  <action>
    Write docs/production/runbooks/07-go-no-go-checklist.md as the consolidated go-live punch-list with a checkbox row per prior-phase gate: deploy verified (CD-01/02 + OPS-01), failure alerts firing + availability test green (OBS-02/03), one-pane dashboard live (OBS-05), budget alert wired (OBS-04), DPIA DPO-signed (RGP-02 — the hard gate), record-of-processing (RGP-01), retention + PII-in-logs posture (RGP-03/04), DSR + EU-residency (RGP-05/06), security evidence pack (SEC-01..05), Copilot published 1:1 to the target team (PUB-01..05), controlled E2E green across happy + 4 failure paths (GO-01, via scripts/e2e_smoke.py), no-PII telemetry confirmed (GO-01, → GUARDRAILS_VALIDATION §2), and gating restricted to exactly the target team (GO-02, AC360_ALLOWED_TEAMS). End with an operator sign-off block (name, date, GO/NO-GO decision, signature line). State up front that EVERY live row is an operator action and the whole checklist is gated on the DPO DPIA sign-off.

    Write docs/production/runbooks/08-gradual-rollout.md as the rollout procedure: (1) Pilot — set AC360_ALLOWED_USERS_HASHED to 2-5 pilot user hashes (hash_id of their oid), keeping AC360_ALLOWED_TEAMS unset-or-narrow so deny-by-default restricts to the pilot; (2) Clean-signal window — sustain 24-48h with EXPLICIT criteria: no Sev1/Sev2 alerts, gateway/Functions error rate below the OBS threshold, no PII-leak telemetry finding, budget within bound; (3) Full team — set AC360_ALLOWED_TEAMS to the target team id (full-team enablement via the same lever); (4) Abort — defined abort criteria (any Sev1/2, error-rate breach, PII finding, budget breach) → go to docs/production/runbooks/02-rollback.md (redeploy known-good) and/or docs/production/runbooks/05-killswitch.md (instant flag cut). Mirror the existing runbook layout (objective blockquote, Principe, Procédure tables, az webapp appsettings examples consistent with 05-killswitch). Keep the markdown lint-clean (ATX headings, fenced code with language, no trailing whitespace).
  </action>
  <verify>
    <automated>if ((Test-Path docs/production/runbooks/07-go-no-go-checklist.md) -and (Test-Path docs/production/runbooks/08-gradual-rollout.md)) { python -m markdownlint_glue 2>$null; Select-String -Path docs/production/runbooks/07-go-no-go-checklist.md -Pattern "DPIA" -Quiet } else { Write-Error "missing doc" }</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist: `Test-Path docs/production/runbooks/07-go-no-go-checklist.md` and `Test-Path docs/production/runbooks/08-gradual-rollout.md` are True.
    - Checklist aggregates the DPIA gate: `docs/production/runbooks/07-go-no-go-checklist.md` contains 'DPIA' (Select-String -Pattern "DPIA" matches) and contains 'e2e_smoke' and a sign-off line (grep 'sign' or 'Signature').
    - Checklist references the no-PII evidence: grep 'GUARDRAILS_VALIDATION' in 07 matches.
    - Rollout runbook uses the allowlist lever: `docs/production/runbooks/08-gradual-rollout.md` contains 'AC360_ALLOWED_TEAMS' AND 'AC360_ALLOWED_USERS_HASHED'.
    - Rollout has the three stages + criteria + abort: grep finds 'pilot' (or '2-5'), '24-48', and both '02-rollback' and '05-killswitch' in 08.
    - Markdown structural lint passes (or `markdownlint docs/production/runbooks/07-go-no-go-checklist.md docs/production/runbooks/08-gradual-rollout.md` exits 0 if the linter is available; otherwise the structural greps above stand in).
  </acceptance_criteria>
  <done>07-go-no-go-checklist.md aggregates every prior-phase gate (incl. the DPIA hard gate, E2E-green via e2e_smoke, no-PII via GUARDRAILS_VALIDATION §2, gating via AC360_ALLOWED_TEAMS) with an operator sign-off block; 08-gradual-rollout.md documents pilot→24-48h-clean-signal→full-team via the allowlist lever with explicit clean-signal criteria and an abort path to 02-rollback + 05-killswitch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The offline artifacts are complete and committed: the allowlist gating (06-02, fail-safe verified), the E2E harness scripts/e2e_smoke.py (06-03, offline-verified), the Go/No-Go checklist (07), and the gradual-rollout runbook (08). NOTHING live has run.</what-built>
  <how-to-verify>
    This checkpoint is HARD-GATED on two prerequisites that are NOT satisfiable by Claude and must be true before the operator proceeds: (a) the Phase 5 DPO DPIA sign-off (RGP-02) is complete, and (b) the full live stack from Phases 2-4 (provision, deploy, Copilot publish) is live. Do NOT approve until BOTH hold.

    When (and only when) both hold, the operator performs the live go-live in order:
    1. Controlled E2E (GO-01): run `python scripts/e2e_smoke.py --base-url <prod-gateway-url>` with AC360_E2E_TOKEN set to a test token and a clearly-SYNTHETIC client/document — confirm the expected verdict for each scenario: CONFORME (happy), CLIENT_NON_TROUVE, ECART (+FIC available), OCR-timeout (failed/error), Fabric-down (INCERTAIN/error). Restart the Functions app between download and FIC retrieval to prove cross-instance file durability (per the roadmap risk note).
    2. No-PII telemetry (GO-01): run the post-run KQL no-PII query (printed by e2e_smoke) against App Insights for the E2E correlation id; confirm no PII in traces; record the result in docs/security/GUARDRAILS_VALIDATION.md §2.
    3. Gating (GO-02): confirm AC360_ALLOWED_TEAMS / AC360_ALLOWED_USERS_HASHED restrict access to exactly the target team/pilot (deny-by-default verified live).
    4. Go/No-Go sign-off (GO-03): complete and sign docs/production/runbooks/07-go-no-go-checklist.md.
    5. Gradual rollout (GO-04): follow docs/production/runbooks/08-gradual-rollout.md — pilot 2-5 via AC360_ALLOWED_USERS_HASHED → 24-48h clean signal → full team via AC360_ALLOWED_TEAMS; abort to 02-rollback / 05-killswitch if the clean-signal criteria are breached.
  </how-to-verify>
  <resume-signal>Type "approved" once the DPIA is signed, the live E2E is green with no PII, the checklist is signed GO, and the rollout has reached the intended cohort — or describe what failed (and which abort path was taken).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod (live E2E) | The live run touches the real stack with synthetic data; must wait until DPIA signed + stack live |
| operator → allowlist app settings | The rollout lever is set in admin-only app settings (per 05-killswitch); a commercial cannot self-enable |
| docs → prior-phase gates | The checklist asserts every upstream gate; a missing gate row would let a non-compliant launch through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Repudiation | go-live without DPIA | mitigate | The checkpoint is blocking-human and explicitly gated on the RGP-02 DPO DPIA sign-off; the checklist carries the DPIA row; no auto task goes live |
| T-06-02 | Information disclosure | live E2E data + telemetry | mitigate | synthetic client/doc only; post-run no-PII KQL check recorded in GUARDRAILS_VALIDATION §2 before sign-off |
| T-06-09 | Elevation | rollout exposes whole team early | mitigate | deny-by-default allowlist (06-02); pilot via AC360_ALLOWED_USERS_HASHED first, full team only after 24-48h clean signal; abort to 02-rollback / 05-killswitch |
| T-06-SC | Tampering | dependency installs | accept | No package installs in this plan (docs + checkpoint only) |
</threat_model>

<verification>
- Both docs exist and pass the structural greps (DPIA + e2e_smoke + GUARDRAILS_VALIDATION in 07; AC360_ALLOWED_TEAMS + AC360_ALLOWED_USERS_HASHED + 24-48 + 02-rollback + 05-killswitch in 08).
- The live E2E run, no-PII telemetry check, Go/No-Go sign-off, and gradual rollout are blocking operator checkpoints — none executed by Claude.
- The checkpoint is explicitly gated on the DPO DPIA sign-off + the full live stack.
</verification>

<success_criteria>
The consolidated Go/No-Go checklist and the gradual-rollout runbook exist and are operator-executable; the allowlist (06-02) is the documented rollout lever; the abort path routes to the existing rollback + kill-switch runbooks; and the live, PII-touching, time-based go-live steps are hard-gated behind a blocking operator checkpoint that requires the DPO DPIA sign-off first. GO-03 and GO-04 artifacts complete; GO-01/GO-02 live verification is the operator checkpoint.
</success_criteria>

<output>
Create `.planning/phases/06-controlled-e2e-go-no-go-team-rollout/06-04-SUMMARY.md` when done.
</output>
