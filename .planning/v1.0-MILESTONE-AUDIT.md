---
milestone: v1.0
audited: 2026-06-17
audit_type: re-audit (supersedes 2026-06-14; state unchanged + new integration findings)
status: gaps_found
scores:
  requirements: 44/49 satisfied (artifact-level); 5 go-live/DPO-gated unsatisfied
  phases: 1/6 passed, 5/6 human_needed (artifact-complete, operator/DPO-gated)
  integration: 6/6 cross-phase chains wired (artifact-level); 0 blockers, 3 warnings; live integration pending
  flows: 1/1 E2E flow wired artifact-level (8/8 hops, no contract mismatch); live run = GO-01 operator checkpoint
gaps:
  requirements:
    - id: "RGP-02"
      status: "unsatisfied"
      phase: "05-rgpd-security-evidence-pack"
      claimed_by_plans: ["05-05-PLAN.md"]
      completed_by_plans: []
      verification_status: "human_needed"
      evidence: "DPIA + CNIL 9-criteria (4/6/8 met) DRAFTED in docs/governance/RGP-02-DPIA.md; DPO sign-off pending. HARD GATE before Phase 6 go-live."
    - id: "RGP-01"
      status: "unsatisfied"
      phase: "05-rgpd-security-evidence-pack"
      claimed_by_plans: ["05-05-PLAN.md"]
      completed_by_plans: []
      verification_status: "human_needed"
      evidence: "Art. 30 record DRAFTED (docs/governance/RGP-01-record-of-processing.md, 7 fields); DPO finalization of controller/DPO identity fields pending."
    - id: "GO-01"
      status: "unsatisfied"
      phase: "06-controlled-e2e-go-no-go-team-rollout"
      claimed_by_plans: ["06-01-PLAN.md", "06-03-PLAN.md", "06-04-PLAN.md"]
      completed_by_plans: ["06-03-SUMMARY.md (harness)"]
      verification_status: "human_needed"
      evidence: "E2E harness built + unit-tested (scripts/e2e_smoke.py, 5 scenarios, no_pii_kql, 6 tests). Live controlled run + no-PII telemetry check require the live stack + DPO DPIA."
    - id: "GO-03"
      status: "unsatisfied"
      phase: "06-controlled-e2e-go-no-go-team-rollout"
      claimed_by_plans: ["06-04-PLAN.md"]
      completed_by_plans: ["06-04-SUMMARY.md (checklist artifact)"]
      verification_status: "human_needed"
      evidence: "Go/No-Go checklist authored (docs/production/runbooks/07-go-no-go-checklist.md); operator sign-off pending (requires DPO DPIA box checked)."
    - id: "GO-04"
      status: "unsatisfied"
      phase: "06-controlled-e2e-go-no-go-team-rollout"
      claimed_by_plans: ["06-04-PLAN.md"]
      completed_by_plans: ["06-04-SUMMARY.md (runbook artifact)"]
      verification_status: "human_needed"
      evidence: "Gradual-rollout runbook authored (docs/production/runbooks/08-gradual-rollout.md); pilot→clean-signal→full execution requires live stack + signed GO."
  integration:
    - "All 6 cross-phase chains wired end-to-end in repo (identity→IDOR→audit-trail; redaction→telemetry; infra→deploy; Copilot→gateway; allowlist+E2E-harness; retention). 0 broken contracts. Live cross-phase integration unverifiable until the operator provisions + deploys (correctly deferred to GO-01)."
  flows:
    - "Full audit E2E (Teams→Copilot→gateway→Durable→OCR/Fabric→verdict/FIC) wired artifact-level across all 8 hops with matching field contracts; the real-prod synthetic E2E (GO-01) is an operator checkpoint."
operator_dpo_backlog:
  - "DPO: sign DPIA (RGP-02, HARD GATE) + finalize Art.30 (RGP-01)"
  - "Phase 2 live: provision.ps1, OBO admin consent (no AADSTS65001), EU residency confirm (M365 geo/Fabric/Power Platform), Fabric MI read-grant"
  - "Phase 3 live: first cd-prod.yml deploy, OIDC federated creds + GitHub production Environment, Teams webhook, live alert/dashboard/budget"
  - "Phase 4 live: publish Copilot agent (1:1 personal install), manual Entra V2 + Teams SSO, known-blocked-prompt test"
  - "Phase 6 live: run e2e_smoke.py + no-PII KQL check, set allowlist to target team, sign Go/No-Go, gradual rollout"
nyquist:
  compliant_phases: []
  partial_phases: ["01", "02", "03", "04", "05", "06"]
  missing_phases: []
  overall: "PARTIAL for all 6 phases — each has a VALIDATION.md but all carry nyquist_compliant: false / status: draft / wave_0_complete: false. The validation STRATEGY drafts were authored at plan time and never flipped to compliant after execution, even though the Wave-0 tests landed and the full suite is green (230 passed/1 skipped). This is a formal Nyquist sign-off gap, NOT a test-coverage gap. Run /gsd-validate-phase {1..6} to close the sign-off retroactively."
tech_debt:
  - phase: 01-deep-code-audit-critical-fixes
    items:
      - "Accepted-deferred (classified in SEC-05, none launch-blocking): single-instance-mitigated in-memory locks, symlink hardening, Fabric fallback, fuzzy-name index."
      - "AUD-05 secondary OBO sites (resolve_document / api_create_planner_task) still use the non-retrying wrapper + 502; audit path (launch-blocking surface) is fixed. Tracked in deferred-items.md."
  - phase: 02-production-infrastructure-provisioning
    items:
      - "NEW (integration WARNING 1): infra/prod.parameters.json declares an orphaned `budgetAmount` key (line 27) that main.bicep does not accept — produces a benign 'no match for parameter' warning on az deployment against main.bicep. Budget deploy itself is correct (cd-prod.yml passes amount=200 to budget.bicep). Fix: remove budgetAmount from prod.parameters.json."
  - phase: 03-backend-deploy-observability
    items:
      - "telemetry.py code-review INFO items: private span-attr mutation + sequence-valued attributes not recursed (IN-01/02, low risk)."
  - phase: 04-copilot-studio-production-publish
    items:
      - "NEW (integration WARNING 2): LancerAudit.mcs.yml audit-trigger body omits `client_context` (lines 144-146); AuditRequest.client_context defaults to None so the flow does not break, but Copilot-triggered audits get weaker name-matching disambiguation than direct API calls. Fix: pass client_context (e.g. the doc query) in the trigger body."
      - "NEW (integration WARNING 3): LancerAudit.mcs.yml polls status once immediately after trigger; long-running audits end the dialog with a 'still pending' message and require re-invoking StatutAudit. By design (no native async loop in Copilot Studio); StatutAudit + persisted Global.AuditJobId provide the retry path. UX gap only."
  - phase: planning-hygiene
    items:
      - "STATE.md is stale: frontmatter still says status: executing, stopped_at 03-05-PLAN.md, completed_phases 5, progress 83% (body says 95%, 'Phase 06 EXECUTING Plan 1 of 4') — but all 6 phases have SUMMARY+VERIFICATION and ROADMAP marks v1.0 complete. Reconcile on milestone close."
      - "REQUIREMENTS.md traceability table (bottom) still lists Phase 4/5/6 rows as 'Pending' while the v1-requirements checkboxes show 44/49 complete — internal inconsistency in REQUIREMENTS.md. Reconcile the table (or regenerate) so status columns match the checkbox state."
      - "REQUIREMENTS.md count note: header round figure '44 total' vs enumerated 49 — reconcile the summary figure."
---

# Milestone v1.0 — Audit (AC360 Production Launch)

## Verdict: GAPS_FOUND — artifact-complete, NOT yet live (re-audit 2026-06-17)

All six phases are **artifact-complete, code-reviewed, integration-wired, and tested in-repo** (full suite 230 passed / 1 skipped). The milestone's Core Value — *"AC360 is live in production"* — is **not yet achieved**: nothing is deployed to live Azure and the DPO has not signed the DPIA. Go-live is gated on the operator/DPO backlog below.

This is a **re-audit** of the 2026-06-14 audit. The requirement-coverage state is **unchanged** (same 5 unsatisfied go-live/DPO gates). What is new in this pass:
1. The cross-phase **integration check ran clean** — all 6 wiring chains confirmed end-to-end with no broken contracts (3 non-blocking warnings added to tech debt).
2. **Nyquist** re-classified honestly: all 6 VALIDATION.md drafts are `nyquist_compliant: false` — a formal sign-off gap (tests landed and pass, but the flag was never flipped). The prior audit recorded this as "compliant at the offline level"; it is more accurately **PARTIAL** pending `/gsd-validate-phase`.
3. Two **planning-hygiene drifts** surfaced (stale STATE.md, REQUIREMENTS.md traceability table out of sync).

## Phase verifications

| Phase | Status | Note |
|-------|--------|------|
| 1 Deep Code Audit & Critical Fixes | **passed** | 8/8 AUD verified in source; 188→230 tests green; SECURITY_POSTURE.md; code-review loop clean |
| 2 Production Infrastructure Provisioning | human_needed | INF-01..09 IaC + scripts done, validator clean; live provision/consent/residency operator-gated |
| 3 Backend Deploy & Observability | human_needed | CD/OBS/OPS artifacts done; first deploy + live alerting/webhook + runbook execution operator-gated |
| 4 Copilot Studio Production Publish | human_needed | PUB-02/04 guardrail validation + URL rebind done; live publish/SSO/install operator-gated |
| 5 RGPD & Security Evidence Pack | human_needed | SEC-01..05 + RGP-03..06 done; RGP-01/02 drafted, **DPO sign-off pending (hard gate)** |
| 6 Controlled E2E, Go/No-Go & Rollout | human_needed | E2E harness + allowlist gating + runbooks done; live E2E/sign-off/rollout operator-gated |

## Requirements coverage: 44/49 satisfied (artifact-level)

3-source cross-reference (REQUIREMENTS.md checkboxes × VERIFICATION.md status/tables × SUMMARY frontmatter). No orphaned requirements — all 49 REQ-IDs appear in a phase VERIFICATION.md (8+9+12+5+11+4 = 49).

**5 unsatisfied** (all go-live execution / DPO — see `gaps` above): **RGP-01, RGP-02, GO-01, GO-03, GO-04**. The FAIL gate forces `gaps_found`.

The other 44 are satisfied at the artifact level. Several (INF-06 consent, CD-02 first deploy, OBS-02/03/04 live alerting, all OPS live execution, PUB-01/03/05 live publish, RGP-06 tenant residency) have their **artifacts complete** with the **live application** tracked as operator checkpoints — these count as satisfied for repo deliverables but remain on the go-live backlog.

> Note: the REQUIREMENTS.md traceability table at the bottom of the file still shows Phase 4/5/6 rows as "Pending" — that table is stale relative to the v1-requirements checkboxes (44 checked). Reconcile on close (tracked in tech_debt → planning-hygiene).

## Integration & flows (NEW this pass — ran clean)

All 6 cross-phase chains verified **wired end-to-end in the repo**, 0 blockers:

1. **Identity → IDOR → audit-trail** — `auth.py` returns `oid` → `api_server.py` keys on `hash_id(oid)` → `_assert_durable_owner` authoritative 403 gate → `audit_trail.emit_document_access` emits `{user_id_hash, document_id, ts_utc, verdict}` only. No UPN leakage.
2. **Redaction → telemetry** — `safe_logger.redact/redact_mapping` → `telemetry.RedactingSpanProcessor` → wired in gateway startup + function_app import. AUD-06 preserved through the OTel export path.
3. **Infra → deploy** — `main.bicep` consumes Key Vault refs + App Insights conn string; `observability.bicep`/`budget.bicep` wired; B1 capacity=1 + `--workers 1` single-instance pin consistent across phases; `cd-prod.yml` deploys it.
4. **Copilot → gateway** — all topic action endpoints target `ac360-gateway-prod`; zero residual staging host; `validate_copilot_yaml.py` asserts `useModelKnowledge=false` + `contentModeration=High`, CI-gated.
5. **Allowlist + E2E harness** — `feature_flags.py` deny-by-default-when-set / fail-safe-when-unset, called pre-trigger; `e2e_smoke.py` drives audit→status→result across 5 scenarios with a no-PII KQL check.
6. **Retention** — `main.bicep` storage `managementPolicies` blob lifecycle + `jobs_ttl.prune_jobs_dir` daily timer both target job artifacts.

**E2E user flow** (Teams 1:1 → Copilot → gateway → Durable → download → OCR → Fabric compare → verdict/FIC → status/result): all 8 hops wired, **field contracts match** between every caller and callee (`verdict`, `client_document`, `reference_fabric`, `score_nom` consumed exactly as `_shape_status_response` emits them). No broken contract.

**3 non-blocking warnings** (added to tech debt): orphaned `budgetAmount` param in prod.parameters.json; `client_context` not passed from LancerAudit (weaker name-matching); single-poll status UX for long audits.

**Live cross-phase integration and the real-prod E2E remain unverifiable until the operator deploys** — correctly deferred to GO-01.

## Nyquist coverage

| Phase | VALIDATION.md | nyquist_compliant | Class | Action |
|-------|---------------|-------------------|-------|--------|
| 01 | exists | false (draft) | PARTIAL | `/gsd-validate-phase 1` |
| 02 | exists | false (draft) | PARTIAL | `/gsd-validate-phase 2` |
| 03 | exists | false (draft) | PARTIAL | `/gsd-validate-phase 3` |
| 04 | exists | false (draft) | PARTIAL | `/gsd-validate-phase 4` |
| 05 | exists | false (draft) | PARTIAL | `/gsd-validate-phase 5` |
| 06 | exists | false (draft) | PARTIAL | `/gsd-validate-phase 6` |

The validation drafts were authored at plan time and never flipped to compliant after execution. The underlying Wave-0 tests **did** land and pass (each VERIFICATION.md confirms), so this is a **sign-off gap, not a coverage gap**. Closing it is optional before completion but recommended for a clean compliance trail.

## Go-live backlog (the punch-list)

The canonical, operator-facing version is **`docs/production/runbooks/07-go-no-go-checklist.md`**. Summary: DPO DPIA sign-off (hard gate) → Phase 2–4 live provisioning/deploy/publish → Phase 6 live E2E + gating + Go/No-Go + rollout. See `operator_dpo_backlog` above.

## Recommendation

**Do NOT complete/archive milestone v1.0 yet** — it is not live. The repo side of the milestone is **done and integration-clean**. Hold the milestone open and work the go-live backlog (DPO sign-off first — it is the hard gate, then operator provisioning/deploy/publish/E2E), then re-audit and complete after a clean pilot signal.

Quick wins worth doing now (do not require live Azure): the 3 integration warnings + planning-hygiene drifts + optional `/gsd-validate-phase` sign-offs.
